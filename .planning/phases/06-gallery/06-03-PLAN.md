---
phase: 06-gallery
plan: 3
type: execute
wave: 2
depends_on: ["06-01", "06-02"]
files_modified:
  - content/gallery/index.md
  - themes/minimal/layouts/gallery/single.html
autonomous: true
requirements:
  - GAL-01
  - GAL-02
  - GAL-03
  - GAL-04

must_haves:
  truths:
    - "content/gallery/index.md exists as a leaf-bundle index with title-only front matter (no date, no draft, no summary, no body content)"
    - "themes/minimal/layouts/gallery/single.html exists, defines the {{ define \"main\" }} block, and iterates .Resources.Match \"photos/*\" alphabetically"
    - "The template emits exactly one Process \"fill 600x400 Smart webp q75\" thumbnail and one Process \"fit 1600x1600 webp q82\" full-size per photo"
    - "Each <a class=\"gallery-item\"> wraps an <img class=\"gallery-img\"> with explicit width and height attributes from $thumb.Width/$thumb.Height"
    - "First photo carries fetchpriority=\"high\"; first 3 are loading=\"eager\"; remaining 15 are loading=\"lazy\" with decoding=\"async\""
    - "Each <a> carries a unique aria-label of the form \"Open photo {N} of {M} at full size\" with N 1-indexed via add $idx 1"
    - "Every <img> uses alt=\"\" (empty, decorative — D-18)"
    - "Template uses the {{ with .Resources.Match \"photos/*\" }} guard for empty-bundle defense"
  artifacts:
    - path: "content/gallery/index.md"
      provides: "Hugo leaf-bundle index that derives Type=gallery from the parent folder name and exposes .Resources from sibling photos/ subdir"
      contains: "title: \"Gallery\""
    - path: "themes/minimal/layouts/gallery/single.html"
      provides: "Type-derived layout for /gallery/ — emits the page-header + grid markup with eager/lazy/fetchpriority/decoding attrs and aria-label per photo"
      contains: "{{ define \"main\" }}, .gallery-grid, Process \"fill 600x400 Smart webp q75\", Process \"fit 1600x1600 webp q82\", aria-label=\"Open photo"
  key_links:
    - from: "themes/minimal/layouts/gallery/single.html"
      to: "content/gallery/photos/* (Plan 06-01 output)"
      via: ".Resources.Match \"photos/*\" glob iteration"
      pattern: "After hugo --minify (Plan 06-04): grep -c 'gallery-item' public/gallery/index.html == 18"
    - from: "themes/minimal/layouts/gallery/single.html"
      to: "themes/minimal/static/css/style.css .gallery-grid (Plan 06-02 output)"
      via: "<div class=\"gallery-grid\"> wrapper consuming the Gallery CSS rules"
      pattern: "grep -c 'class=\"gallery-grid\"' themes/minimal/layouts/gallery/single.html == 1"
    - from: "content/gallery/index.md (Type=gallery)"
      to: "themes/minimal/layouts/_default/baseof.html line 26 body class hook (Plan 06-02)"
      via: "Hugo's automatic .Type derivation from leaf-bundle parent folder + the page-{{ .Type }} body-class rendering"
      pattern: "After hugo --minify: grep -c 'class=\"page-gallery\"' public/gallery/index.html == 1"
---

<objective>
Create the two files that materialize `/gallery/` as a Hugo leaf bundle: `content/gallery/index.md` (the minimal-frontmatter index that establishes `Type=gallery` per D-01) and `themes/minimal/layouts/gallery/single.html` (the Type-derived layout per D-02 that iterates `.Resources.Match "photos/*"`, processes each photo through Hugo's image pipeline twice — thumbnail at `fill 600x400 Smart webp q75` and full-size at `fit 1600x1600 webp q82` per D-03/D-07/D-08 + GAL-03/GAL-04 — and emits the `<div class="gallery-grid">` of `<a class="gallery-item"><img class="gallery-img"></a>` markup with the locked eager/lazy/fetchpriority/decoding/aria-label attribute mix per D-05/D-06/D-18 + UI-SPEC § Copywriting Contract).

Purpose: This plan converts the prepared photo inventory (Plan 06-01) and prepared site infrastructure (Plan 06-02) into a renderable `/gallery/` page. By the end of this plan, `hugo --minify` would produce a working `/gallery/` page with 18 thumbnails, 18 full-size renditions, and zero JS — but the cold-build smoke test, weight gate, and EXIF output verification are deferred to Plan 06-04 (the dedicated verification plan). RESEARCH § "Code Excerpts" provides the verbatim template that this plan installs; PATTERNS § "themes/minimal/layouts/gallery/single.html" maps each attribute to its source decision so no logic is invented.

This is the first time this codebase introduces (a) a Type-derived layout (`layouts/gallery/single.html` resolves before `_default/single.html` per Hugo's lookup chain — RESEARCH § "Lookup Order for Type=gallery"), (b) a `.Resources.Match` + `image.Process` pipeline (existing blog posts use Markdown image syntax that bypasses the pipeline), and (c) a leaf-bundle pattern at the section level (existing leaf bundles are blog posts under `content/blog/{slug}/`).

Output:
- `content/gallery/index.md` — 3 lines: `---`, `title: "Gallery"`, `---`. No body content (per D-01 — the entire page render comes from the template iterating bundle resources).
- `themes/minimal/layouts/gallery/single.html` — ~25 lines: `{{ define "main" }}` block with `<article>` + `.page-header` + `<h1 class="page-title">{{ .Title }}</h1>` (UI-SPEC § Typography decision: KEEP the heading for SEO + a11y) + `{{ with .Resources.Match "photos/*" }}` guard + `<div class="gallery-grid">` + range loop emitting `<a class="gallery-item" aria-label="...">` wrapping `<img class="gallery-img" src="..." width="..." height="..." loading="..." [fetchpriority] [decoding] alt="">`.
- Hugo build (deferred to Plan 06-04) will consume `content/gallery/index.md` and `content/gallery/photos/*.{jpg,JPG,jpeg}` (from Plan 06-01) via this layout, producing `public/gallery/index.html` plus 36 processed WebP files at `public/gallery/photos/`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-gallery/06-CONTEXT.md
@.planning/phases/06-gallery/06-RESEARCH.md
@.planning/phases/06-gallery/06-UI-SPEC.md
@.planning/phases/06-gallery/06-PATTERNS.md
@.planning/phases/06-gallery/06-02-SUMMARY.md
@content/about.md
@themes/minimal/layouts/_default/single.html

<interfaces>
<!-- Existing analog: content/about.md (3 lines, minimal frontmatter — verified via Read in Plan 06-02 prep) -->
(— frontmatter delimiter —)
title: "About"
(— frontmatter delimiter —)

(no body content)

<!-- TARGET content/gallery/index.md (mirrors the about.md frontmatter shape — D-01 / PATTERNS § content/gallery/index.md): -->
(— frontmatter delimiter —)
title: "Gallery"
(— frontmatter delimiter —)

<!-- No body content. The template (themes/minimal/layouts/gallery/single.html) generates the page from bundle resources. -->

<!-- Existing analog: themes/minimal/layouts/_default/single.html (10 lines, verified via Read): -->
{{ define "main" }}
  <article>
    <div class="page-header">
      <h1 class="page-title">{{ .Title }}</h1>
      {{ if .Date }}
        <div class="page-date">{{ .Date.Format "January 2, 2006" }}</div>
      {{ end }}
    </div>
    <div class="page-content">
      {{ .Content }}
    </div>
  </article>
{{ end }}

<!-- TARGET themes/minimal/layouts/gallery/single.html (verbatim from RESEARCH § Code Excerpts lines 686-715 + UI-SPEC § Element Specs + PATTERNS § themes/minimal/layouts/gallery/single.html): -->
{{ define "main" }}
  <article>
    <div class="page-header">
      <h1 class="page-title">{{ .Title }}</h1>
    </div>
    {{ with .Resources.Match "photos/*" }}
      <div class="gallery-grid">
        {{ $total := len . }}
        {{ range $idx, $photo := . }}
          {{ $thumb := $photo.Process "fill 600x400 Smart webp q75" }}
          {{ $full  := $photo.Process "fit 1600x1600 webp q82" }}
          <a class="gallery-item"
             href="{{ $full.RelPermalink }}"
             aria-label="Open photo {{ add $idx 1 }} of {{ $total }} at full size">
            <img class="gallery-img"
                 src="{{ $thumb.RelPermalink }}"
                 width="{{ $thumb.Width }}"
                 height="{{ $thumb.Height }}"
                 loading="{{ if lt $idx 3 }}eager{{ else }}lazy{{ end }}"
                 {{ if eq $idx 0 }}fetchpriority="high"{{ end }}
                 {{ if ge $idx 3 }}decoding="async"{{ end }}
                 alt="">
          </a>
        {{ end }}
      </div>
    {{ end }}
  </article>
{{ end }}

<!-- Per-attribute provenance map (PATTERNS § Per-attribute provenance): -->
<!--   Process "fill 600x400 Smart webp q75"        ← D-08 / GAL-03 / ROADMAP locked verbatim -->
<!--   Process "fit 1600x1600 webp q82"             ← D-07 / GAL-04 / ROADMAP locked verbatim -->
<!--   width="{{ $thumb.Width }}" height="..."      ← Hugo provides post-processing dimensions; explicit pixel dims for CLS=0 -->
<!--   loading="eager" if $idx < 3 else "lazy"      ← D-05 (first 3 above-fold eager) -->
<!--   fetchpriority="high" if $idx == 0            ← D-06 (Web.dev LCP guidance: exactly one above-fold high-priority image) -->
<!--   decoding="async" if $idx >= 3                ← UI-SPEC § Loading below the fold (CONTEXT § Claude's Discretion: recommended yes) -->
<!--   alt=""                                       ← D-18 (decorative; aria-label on <a> carries accessible name) -->
<!--   aria-label="Open photo N of M at full size"  ← UI-SPEC § Copywriting Contract (1-indexed via add $idx 1; rationale: "Open" describes link action, "N of M" gives positional context, NOT filename-based to avoid camera-identifying info) -->
<!--   {{ with .Resources.Match "photos/*" }} guard ← Defensive empty-bundle handling (CONTEXT § Claude's Discretion: recommended yes) -->

<!-- Hugo lookup chain (RESEARCH § Lookup Order for Type=gallery): -->
<!--   1. layouts/gallery/single.html  ← THIS PLAN'S TARGET -->
<!--   2. layouts/_default/single.html ← Pre-existing fallback (NOT used because Type-derived layout wins) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create content/gallery/index.md leaf-bundle index with title-only front matter</name>
  <read_first>
    - content/about.md (analog — 3 lines, minimal frontmatter; PATTERNS § "content/gallery/index.md (NEW)" identifies this as the closest analog)
    - .planning/phases/06-gallery/06-CONTEXT.md § "Page Template & Hugo Wiring" (D-01 — leaf bundle, title-only front matter)
    - .planning/phases/06-gallery/06-PATTERNS.md § "content/gallery/index.md (NEW — content / leaf-bundle index)"
    - content/gallery/photos/ (verify Plan 06-01 has populated this directory with 18 photos — `ls content/gallery/photos/ | wc -l` returns 18; this is a Wave-1 dependency that should already be satisfied since Plan 06-01 ran in Wave 1 alongside Plan 06-02)
  </read_first>
  <files>content/gallery/index.md</files>
  <action>
**Pre-flight check (Plan 06-01 dependency):**

```bash
test -d content/gallery/photos && \
  test "$(ls content/gallery/photos/ 2>/dev/null | wc -l | tr -d ' ')" = "18" && \
  echo "PASS: 18 photos present from Plan 06-01" || \
  { echo "FAIL: content/gallery/photos/ not populated; Plan 06-01 must complete before Plan 06-03"; exit 1; }
```

If this fails, STOP — Plan 06-01 (parallel Wave 1 sibling of Plan 06-02) has not completed. The orchestrator should have ordered Wave 1 to fully complete before Wave 2 begins; surface this as an orchestration error.

**Create `content/gallery/index.md` with EXACTLY this content (3 lines + trailing newline):**

```markdown
(— frontmatter delimiter —)
title: "Gallery"
(— frontmatter delimiter —)
```

Use the Write tool. The file should be 3 lines plus a final newline (4 bytes after the `---` if you count the trailing newline). Do NOT add:
- `date:` (D-01: no date — mirrors content/about.md)
- `draft: false` (D-01: no draft — mirrors content/about.md)
- `summary: "..."` (D-01: no summary)
- `description: "..."` (D-01: no description)
- `layout: "..."` (D-02: NOT needed — Hugo's lookup chain auto-resolves `Type=gallery` → `layouts/gallery/single.html`)
- ANY body content under the closing `---` (D-01: the entire page render is template-generated from bundle resources)

**Why title-only:** Per D-01 and PATTERNS § Frontmatter pattern, mirroring `content/about.md`'s minimal-frontmatter approach keeps the data layer thin. Hugo's auto-derivation handles the rest:
- `Type` is set from the parent folder name → `"gallery"`
- `Section` is also `"gallery"` (top-level)
- `Kind` is `"page"` (leaf bundle convention)
- `URL` resolves to `/gallery/` (folder path → URL)
- `Resources` exposes `content/gallery/photos/*` for `.Resources.Match` calls

**Why this is a leaf bundle (not a list/section page):** Hugo treats `content/gallery/index.md` (filename `index.md`, NOT `_index.md`) as a leaf bundle — single page with co-located resources. Had we used `_index.md` instead, Hugo would treat `/gallery/` as a section list page and require sub-pages or different layout lookup. The `index.md` filename is critical and verified by PATTERNS § Page-bundle convention.

**File integrity check:** After writing, verify:

```bash
wc -l < content/gallery/index.md   # expect: 3
head -3 content/gallery/index.md   # expect: ---\ntitle: "Gallery"\n---
test ! -s content/gallery/index.md && echo "FAIL empty" || echo "OK non-empty"
```
  </action>
  <verify>
    <automated>[ -f content/gallery/index.md ] && [ "$(wc -l < content/gallery/index.md | tr -d ' ')" = "3" ] && [ "$(grep -c 'title: "Gallery"' content/gallery/index.md)" = "1" ] && [ "$(grep -c '^date:' content/gallery/index.md)" = "0" ] && [ "$(grep -c '^draft:' content/gallery/index.md)" = "0" ] && [ "$(grep -c '^summary:' content/gallery/index.md)" = "0" ] && [ "$(grep -c '^layout:' content/gallery/index.md)" = "0" ] && [ "$(grep -c '^---$' content/gallery/index.md)" = "2" ]</automated>
  </verify>
  <acceptance_criteria>
    - `[ -f content/gallery/index.md ]` exits 0 (file exists)
    - `wc -l < content/gallery/index.md` returns `3`
    - `grep -c 'title: "Gallery"' content/gallery/index.md` returns `1`
    - `grep -c '^---$' content/gallery/index.md` returns `2` (opening + closing frontmatter delimiters)
    - `grep -c '^date:' content/gallery/index.md` returns `0` (D-01: no date)
    - `grep -c '^draft:' content/gallery/index.md` returns `0` (D-01: no draft)
    - `grep -c '^summary:' content/gallery/index.md` returns `0` (D-01: no summary)
    - `grep -c '^layout:' content/gallery/index.md` returns `0` (D-02: layout auto-derived from Type)
    - The file has exactly 3 lines of content followed by a trailing newline (no extra body content)
    - Filename is exactly `content/gallery/index.md` (NOT `_index.md` — leaf bundle, not section list)
  </acceptance_criteria>
  <done>
`content/gallery/index.md` exists as a 3-line leaf-bundle index with `title: "Gallery"` as the only front-matter field. No body content. Filename uses `index.md` (leaf bundle), NOT `_index.md` (which would be a section list). The presence of this file plus the 18 photos at `content/gallery/photos/` (from Plan 06-01) constitute the data layer of the leaf bundle; the layout is supplied by Task 2.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create themes/minimal/layouts/gallery/single.html — Type-derived layout with grid + image pipeline</name>
  <read_first>
    - themes/minimal/layouts/_default/single.html (analog — 10 lines; PATTERNS § "Closest Analog" identifies this as the structural template)
    - themes/minimal/layouts/_default/baseof.html (Plan 06-02's edit — verifies the body-class hook on line 26 is in place; the Gallery template's `{{ define "main" }}` block plugs into baseof's `{{ block "main" . }}` slot at line 30)
    - .planning/phases/06-gallery/06-CONTEXT.md § "Page Template & Hugo Wiring" (D-01, D-02, D-03 — layout location, Type-derived lookup, .Resources.Match glob)
    - .planning/phases/06-gallery/06-CONTEXT.md § "Photo Ordering" (D-04 — alphabetical default sort)
    - .planning/phases/06-gallery/06-CONTEXT.md § "Eager / Lazy Loading & fetchpriority" (D-05, D-06)
    - .planning/phases/06-gallery/06-CONTEXT.md § "Click-Through to Full-Size" (D-07 — <a href> wrapping, no target=_blank)
    - .planning/phases/06-gallery/06-CONTEXT.md § "CSS Grid & Wider Canvas" (D-08 — wrapper class names match Plan 06-02 CSS)
    - .planning/phases/06-gallery/06-CONTEXT.md § "Image Alt Text (a11y)" (D-18 — empty alt)
    - .planning/phases/06-gallery/06-RESEARCH.md § "Iteration Pattern with $idx (per D-05/D-06)" (verified Hugo Go-template syntax for the range with index)
    - .planning/phases/06-gallery/06-RESEARCH.md § "Code Excerpts" (lines 686-715 — verbatim template text)
    - .planning/phases/06-gallery/06-UI-SPEC.md § "Visual & Interaction Contract" (full section — confirms element class names, attribute presence, Smart-crop semantics)
    - .planning/phases/06-gallery/06-UI-SPEC.md § "Copywriting Contract" (aria-label wording rationale: "Open photo N of M at full size")
    - .planning/phases/06-gallery/06-PATTERNS.md § "themes/minimal/layouts/gallery/single.html (NEW — Type-derived layout)" (full pattern map with per-attribute provenance)
  </read_first>
  <files>themes/minimal/layouts/gallery/single.html</files>
  <action>
**Pre-flight checks (Plan 06-02 + Plan 06-01 dependencies):**

```bash
# Plan 06-02 outputs — body-class hook + CSS rules
[ "$(grep -c 'class="page-{{ .Type' themes/minimal/layouts/_default/baseof.html)" = "1" ] && \
  echo "PASS: body-class hook in place from Plan 06-02" || \
  { echo "FAIL: baseof.html missing page-Type body class; Plan 06-02 must complete before Plan 06-03"; exit 1; }

[ "$(grep -c '\.gallery-grid' themes/minimal/static/css/style.css)" = "1" ] && \
  echo "PASS: .gallery-grid CSS rule present from Plan 06-02" || \
  { echo "FAIL: Gallery CSS not appended; Plan 06-02 must complete before Plan 06-03"; exit 1; }

# Plan 06-01 output — photo bundle
[ "$(ls content/gallery/photos/ 2>/dev/null | wc -l | tr -d ' ')" = "18" ] && \
  echo "PASS: 18 photos in bundle from Plan 06-01" || \
  { echo "FAIL: photo bundle empty; Plan 06-01 must complete before Plan 06-03"; exit 1; }

# Task 1 output — leaf bundle index
[ -f content/gallery/index.md ] && \
  echo "PASS: leaf bundle index from Task 1" || \
  { echo "FAIL: content/gallery/index.md missing; Task 1 of this plan must complete first"; exit 1; }
```

If any check fails, STOP. The dependency contract has been violated.

**Create the directory `themes/minimal/layouts/gallery/`:**

```bash
mkdir -p themes/minimal/layouts/gallery
```

This is the FIRST non-default layout directory in the codebase (RESEARCH § "Hugo Type-Derived Layout Lookup" — existing layouts are all under `_default/`). Plan 06-03 introduces this convention; Phase 7 may inherit it for `layouts/about/single.html` if Phase 7 converts `content/about.md` to a leaf bundle.

**Create `themes/minimal/layouts/gallery/single.html` with EXACTLY this content** (verbatim from RESEARCH § Code Excerpts lines 686-715, finalized via UI-SPEC + PATTERNS):

```go-html-template
{{ define "main" }}
  <article>
    <div class="page-header">
      <h1 class="page-title">{{ .Title }}</h1>
    </div>
    {{ with .Resources.Match "photos/*" }}
      <div class="gallery-grid">
        {{ $total := len . }}
        {{ range $idx, $photo := . }}
          {{ $thumb := $photo.Process "fill 600x400 Smart webp q75" }}
          {{ $full  := $photo.Process "fit 1600x1600 webp q82" }}
          <a class="gallery-item"
             href="{{ $full.RelPermalink }}"
             aria-label="Open photo {{ add $idx 1 }} of {{ $total }} at full size">
            <img class="gallery-img"
                 src="{{ $thumb.RelPermalink }}"
                 width="{{ $thumb.Width }}"
                 height="{{ $thumb.Height }}"
                 loading="{{ if lt $idx 3 }}eager{{ else }}lazy{{ end }}"
                 {{ if eq $idx 0 }}fetchpriority="high"{{ end }}
                 {{ if ge $idx 3 }}decoding="async"{{ end }}
                 alt="">
          </a>
        {{ end }}
      </div>
    {{ end }}
  </article>
{{ end }}
```

Use the Write tool. Do NOT use shell heredoc.

**Per-line provenance (DO NOT change any of these without revisiting the source decision):**

| Line / Region | Source | Why exact |
|---------------|--------|-----------|
| `{{ define "main" }}` ... `{{ end }}` (outer block) | _default/single.html line 1, 13 — required for baseof.html `{{ block "main" . }}` slot | Hugo template-block contract; file won't render without it |
| `<article>` wrapper | _default/single.html line 2 (semantic landmark, no class) | Matches existing convention |
| `<div class="page-header"><h1 class="page-title">{{ .Title }}</h1></div>` | _default/single.html lines 3-7 (kept) + UI-SPEC § Typography "INCLUDE the heading" decision | Drops the `.Date` block (gallery has no date); keeps the heading per UI-SPEC SEO + a11y rationale |
| `{{ with .Resources.Match "photos/*" }}` guard | D-03 + CONTEXT § Claude's Discretion (recommended yes) | Defensive empty-bundle handling |
| `<div class="gallery-grid">` | D-08 + matches CSS class from Plan 06-02 | The `.gallery-grid` rule consumer |
| `{{ $total := len . }}` | UI-SPEC § Copywriting Contract — needed for "of M" in aria-label | Captures the count BEFORE the range rebinds `.` |
| `{{ range $idx, $photo := . }}` | RESEARCH § Iteration Pattern — Hugo Go-template two-value range | Provides 0-indexed `$idx` for D-05/D-06 conditionals |
| `{{ $thumb := $photo.Process "fill 600x400 Smart webp q75" }}` | D-08 / GAL-03 / ROADMAP locked verbatim | Thumbnail rendition; `fill` crops to exact 600x400, `Smart` chooses entropy/skin-aware crop centroid, `webp q75` is format+quality |
| `{{ $full := $photo.Process "fit 1600x1600 webp q82" }}` | D-07 / GAL-04 / ROADMAP locked verbatim | Full-size rendition; `fit` scales longest side ≤ 1600px (no upscale), `webp q82` is format+quality |
| `<a class="gallery-item" href="{{ $full.RelPermalink }}" aria-label="...">` | D-07 + UI-SPEC § Copywriting Contract | Click target + accessible name |
| `aria-label="Open photo {{ add $idx 1 }} of {{ $total }} at full size"` | UI-SPEC § aria-label wording rationale | 1-indexed for screen-reader readability; "Open" describes action; "N of M" gives positional context; NOT filename-based |
| `<img class="gallery-img" ...>` | D-08 + matches CSS class from Plan 06-02 | The `.gallery-img` rule consumer |
| `src="{{ $thumb.RelPermalink }}"` | D-07 / D-08 — thumbnail src, full-size in href | Standard `<a><img></a>` click-through |
| `width="{{ $thumb.Width }}" height="{{ $thumb.Height }}"` | UI-SPEC § Performance Contract — CLS=0 guarantee | Hugo provides post-processing dimensions; explicit pixel attrs reserve cell height before image bytes arrive |
| `loading="{{ if lt $idx 3 }}eager{{ else }}lazy{{ end }}"` | D-05 — first 3 photos eager, rest lazy | Above-fold heuristic for typical desktop viewport |
| `{{ if eq $idx 0 }}fetchpriority="high"{{ end }}` | D-06 + RESEARCH § fetchpriority — Web.dev LCP guidance | Exactly one above-fold high-priority image |
| `{{ if ge $idx 3 }}decoding="async"{{ end }}` | UI-SPEC § Loading below the fold + CONTEXT § Claude's Discretion (recommended yes) | Web.dev recommendation for offscreen images; first 3 (eager) decode synchronously as part of first paint |
| `alt=""` | D-18 — decorative; aria-label on `<a>` carries accessible name | Avoids 18 generic "Personal photo" announcements; WCAG-compliant for caption-less galleries |
| `{{ end }}` (range), `{{ end }}` (with), `</div>`, `</article>`, `{{ end }}` (define) | Required closing tokens | Each `{{ X }}` opener needs its `{{ end }}` |

**What NOT to add (locked OUT):**
- NO `<div class="page-content">` wrapper around the grid (D-08: `<div class="gallery-grid">` is the wrapper; no extra layer needed; the gallery is NOT page-content prose).
- NO `target="_blank"` or `rel="noopener"` on the `<a>` (D-07: same-tab navigation; browser back button restores the gallery scroll position).
- NO `srcset` or `<picture>` element (D-20: omitted; auto-fill grid handles responsive sizing; can be added later if Lighthouse flags hi-DPI quality).
- NO `<picture>` with multiple `<source>` for AVIF + WebP (REQUIREMENTS § Out of Scope: AVIF unsupported in Hugo 0.157).
- NO inline JS, NO `onclick`, NO `data-*` attributes for hypothetical lightbox (REQUIREMENTS § Out of Scope: no JS lightbox; D-07 locks browser-native click-through).
- NO `tabindex` on `<a>` (anchors with `href` are focusable by default; explicit tabindex would be redundant or anti-pattern).
- NO `<noscript>` fallback (the entire page is server-rendered HTML; no JS, no fallback needed).
- NO front-matter `layout:` override in `content/gallery/index.md` (D-02: Hugo's lookup chain auto-resolves `Type=gallery` → `layouts/gallery/single.html`).

**File integrity check:** After writing, verify the file is a syntactically valid Hugo Go-template by counting opening/closing tokens:

```bash
# Each {{ define / range / with / if needs a matching {{ end }}
opens=$(grep -oE '\{\{ (define|range|with|if)' themes/minimal/layouts/gallery/single.html | wc -l | tr -d ' ')
closes=$(grep -c '{{ end }}' themes/minimal/layouts/gallery/single.html)
[ "$opens" = "$closes" ] && echo "PASS: $opens opens = $closes closes" || echo "FAIL: $opens opens vs $closes closes"
```

Expected: 5 opens (`define main`, `with`, `range`, `if eq`, `if lt`/`if ge` count separately) — actually need to count carefully. Let me enumerate:
- `{{ define "main" }}` × 1
- `{{ with .Resources.Match "photos/*" }}` × 1
- `{{ range $idx, $photo := . }}` × 1
- `{{ if lt $idx 3 }}` × 1 (inside `loading=`)
- `{{ if eq $idx 0 }}` × 1 (fetchpriority)
- `{{ if ge $idx 3 }}` × 1 (decoding)

Total opens: 6. Total `{{ end }}`: 6. Verify the count matches.
  </action>
  <verify>
    <automated>[ -f themes/minimal/layouts/gallery/single.html ] && [ "$(grep -c '{{ define "main" }}' themes/minimal/layouts/gallery/single.html)" = "1" ] && [ "$(grep -c 'gallery-grid' themes/minimal/layouts/gallery/single.html)" = "1" ] && [ "$(grep -c 'gallery-item' themes/minimal/layouts/gallery/single.html)" = "1" ] && [ "$(grep -c 'gallery-img' themes/minimal/layouts/gallery/single.html)" = "1" ] && [ "$(grep -c 'Process "fill 600x400 Smart webp q75"' themes/minimal/layouts/gallery/single.html)" = "1" ] && [ "$(grep -c 'Process "fit 1600x1600 webp q82"' themes/minimal/layouts/gallery/single.html)" = "1" ] && [ "$(grep -c 'aria-label="Open photo' themes/minimal/layouts/gallery/single.html)" = "1" ] && [ "$(grep -c 'fetchpriority="high"' themes/minimal/layouts/gallery/single.html)" = "1" ] && [ "$(grep -c 'decoding="async"' themes/minimal/layouts/gallery/single.html)" = "1" ] && [ "$(grep -c 'alt=""' themes/minimal/layouts/gallery/single.html)" = "1" ] && [ "$(grep -c '{{ end }}' themes/minimal/layouts/gallery/single.html)" = "6" ] && [ "$(grep -c 'target="_blank"' themes/minimal/layouts/gallery/single.html)" = "0" ] && [ "$(grep -c 'srcset' themes/minimal/layouts/gallery/single.html)" = "0" ]</automated>
  </verify>
  <acceptance_criteria>
    - `[ -f themes/minimal/layouts/gallery/single.html ]` exits 0 (file exists at the Type-derived layout path)
    - `[ -d themes/minimal/layouts/gallery ]` exits 0 (parent directory was created)
    - `grep -c '{{ define "main" }}' themes/minimal/layouts/gallery/single.html` returns `1` (block name matches baseof.html slot)
    - `grep -c '\<article\>' themes/minimal/layouts/gallery/single.html` returns `1` (semantic wrapper from analog)
    - `grep -c 'class="page-header"' themes/minimal/layouts/gallery/single.html` returns `1` (heading wrapper from analog)
    - `grep -c 'class="page-title"' themes/minimal/layouts/gallery/single.html` returns `1` (UI-SPEC § Typography decision: KEEP heading)
    - `grep -c '{{ .Title }}' themes/minimal/layouts/gallery/single.html` returns `1` (renders "Gallery" from front matter)
    - `grep -c '{{ with .Resources.Match "photos/\*" }}' themes/minimal/layouts/gallery/single.html` returns `1` (D-03 glob with defensive guard)
    - `grep -c 'class="gallery-grid"' themes/minimal/layouts/gallery/single.html` returns `1` (consumer of Plan 06-02 CSS)
    - `grep -c 'class="gallery-item"' themes/minimal/layouts/gallery/single.html` returns `1` (D-07 click target wrapper)
    - `grep -c 'class="gallery-img"' themes/minimal/layouts/gallery/single.html` returns `1` (D-08 image rule consumer)
    - `grep -c '{{ $total := len . }}' themes/minimal/layouts/gallery/single.html` returns `1` (count captured for aria-label)
    - `grep -c '{{ range $idx, $photo := . }}' themes/minimal/layouts/gallery/single.html` returns `1` (RESEARCH-verified two-value range syntax)
    - `grep -c 'Process "fill 600x400 Smart webp q75"' themes/minimal/layouts/gallery/single.html` returns `1` (GAL-03 verbatim)
    - `grep -c 'Process "fit 1600x1600 webp q82"' themes/minimal/layouts/gallery/single.html` returns `1` (GAL-04 verbatim)
    - `grep -c '{{ $thumb.RelPermalink }}' themes/minimal/layouts/gallery/single.html` returns `1` (img src)
    - `grep -c '{{ $full.RelPermalink }}' themes/minimal/layouts/gallery/single.html` returns `1` (a href)
    - `grep -c 'width="{{ $thumb.Width }}"' themes/minimal/layouts/gallery/single.html` returns `1` (CLS=0)
    - `grep -c 'height="{{ $thumb.Height }}"' themes/minimal/layouts/gallery/single.html` returns `1` (CLS=0)
    - `grep -c 'loading="{{ if lt $idx 3 }}eager{{ else }}lazy{{ end }}"' themes/minimal/layouts/gallery/single.html` returns `1` (D-05 ternary)
    - `grep -c '{{ if eq $idx 0 }}fetchpriority="high"{{ end }}' themes/minimal/layouts/gallery/single.html` returns `1` (D-06 first-only)
    - `grep -c '{{ if ge $idx 3 }}decoding="async"{{ end }}' themes/minimal/layouts/gallery/single.html` returns `1` (UI-SPEC lazy-only)
    - `grep -c 'aria-label="Open photo {{ add $idx 1 }} of {{ $total }} at full size"' themes/minimal/layouts/gallery/single.html` returns `1` (UI-SPEC verbatim wording)
    - `grep -c 'alt=""' themes/minimal/layouts/gallery/single.html` returns `1` (D-18 empty alt)
    - `grep -c '{{ end }}' themes/minimal/layouts/gallery/single.html` returns `6` (matches the 6 opening blocks: define, with, range, if-lt, if-eq, if-ge)
    - **Locked OUT (must NOT appear):**
    - `grep -c 'target="_blank"' themes/minimal/layouts/gallery/single.html` returns `0`
    - `grep -c 'srcset' themes/minimal/layouts/gallery/single.html` returns `0`
    - `grep -c 'rel="noopener"' themes/minimal/layouts/gallery/single.html` returns `0`
    - `grep -c 'tabindex' themes/minimal/layouts/gallery/single.html` returns `0`
    - `grep -c 'onclick' themes/minimal/layouts/gallery/single.html` returns `0`
    - `grep -c 'data-' themes/minimal/layouts/gallery/single.html` returns `0`
    - `grep -c 'class="page-content"' themes/minimal/layouts/gallery/single.html` returns `0` (no extra wrapper layer)
  </acceptance_criteria>
  <done>
`themes/minimal/layouts/gallery/single.html` exists at the Type-derived layout location. The template defines the `main` block, contains the `<article>` + `.page-header` + `<h1 class="page-title">{{ .Title }}</h1>` shell from `_default/single.html`, and replaces the `.page-content` block with a `{{ with .Resources.Match "photos/*" }}` guard wrapping the `<div class="gallery-grid">` range loop. Each photo emits an `<a class="gallery-item">` with the locked aria-label wording wrapping an `<img class="gallery-img">` with explicit width/height, loading=eager-or-lazy ternary on $idx<3, fetchpriority=high on first-only, decoding=async on lazy-only, and alt=empty. Locked-out attributes (target=_blank, srcset, tabindex, onclick) confirmed absent. Block-balance check passes (6 opens = 6 ends). The build is now ready for Plan 06-04's cold-build smoke test, which is the integration gate that confirms `hugo --minify` produces 36 WebP renditions and a render-clean `public/gallery/index.html`.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Hugo template parser → rendered HTML | A Go-template syntax error blocks the entire build. The `{{ end }}` count and `grep` for exact attribute strings are the in-plan parse-validity gates; `hugo --minify` in Plan 06-04 is the integration gate. |
| Hugo `image.Process` → `public/gallery/photos/` | A bad `Process` spec string (e.g., wrong action name, unsupported format) causes Hugo to error at build time. Both spec strings are RESEARCH-VERIFIED valid in Hugo 0.157+. |
| Browser HTML parser → `<img>` rendering | Mismatched quotes in attribute strings would break HTML parsing. Verbatim copy from RESEARCH § Code Excerpts mitigates by using the already-validated text. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06.3-01 | Tampering | Go-template syntax error breaks `/gallery/` and possibly all pages | mitigate | Block-balance check (6 opens = 6 ends) in Task 2's verification; Plan 06-04 `hugo --minify` is the integration gate. |
| T-06.3-02 | Tampering | Wrong layout path (e.g., `_default/gallery.html` instead of `gallery/single.html`) means Hugo falls back to `_default/single.html` and renders `{{ .Content }}` (empty) instead of the grid | mitigate | Acceptance criterion `[ -d themes/minimal/layouts/gallery ]` confirms directory exists; `[ -f themes/minimal/layouts/gallery/single.html ]` confirms file at correct path. RESEARCH § Lookup Order confirms Hugo resolves `Type=gallery` to `gallery/single.html`. |
| T-06.3-03 | Tampering | `_index.md` instead of `index.md` makes `/gallery/` a list page, layout lookup goes to `gallery/list.html` (which doesn't exist) → fallback to `_default/list.html` (wrong markup) | mitigate | Task 1 acceptance criterion verifies filename is `content/gallery/index.md` (NOT `_index.md`). |
| T-06.3-04 | Information Disclosure | Template emits `.Exif.GPSLatitude` or other source-EXIF fields | mitigate | Template uses ZERO `.Exif.*` references — all metadata comes from Hugo's `$thumb.Width` / `$thumb.Height` (post-processing dimensions, not source EXIF). Combined with Plan 06-02's `disableLatLong = true`, GPS extraction is double-blocked. |
| T-06.3-05 | Tampering | `target="_blank"` accidentally added breaks browser back-button UX | mitigate | Acceptance criterion `grep -c 'target="_blank"'` returns `0`. Locked OUT per D-07. |
| T-06.3-06 | Information Disclosure | aria-label uses filename (e.g., `aria-label="Open IMG_1646.jpeg"`) → exposes camera-identifying info that EXIF scrub stripped | mitigate | UI-SPEC § Copywriting Contract locks the aria-label wording to "Open photo N of M at full size" — explicitly NOT filename-based. Acceptance criterion verifies the verbatim string. |
| T-06.3-07 | Denial of Service | Empty `content/gallery/photos/` causes range over empty slice → empty grid renders silently | mitigate | `{{ with .Resources.Match "photos/*" }}` guard ensures the `<div class="gallery-grid">` is omitted entirely on empty bundle (no broken empty wrapper). Plan 06-01 dependency check + 18-photo count assertion in Task 2 pre-flight prevents the empty-bundle scenario. |
| T-06.3-08 | Tampering | Wrong Process spec string (e.g., `q70` instead of `q75`) silently produces wrong-quality output | mitigate | Acceptance criterion `grep -c 'Process "fill 600x400 Smart webp q75"'` returns `1` AND `'Process "fit 1600x1600 webp q82"'` returns `1`. ROADMAP locks both verbatim. Plan 06-04's measure-then-adjust authority allows q82 → q78 if weight gate fails (documented exception). |
</threat_model>

<verification>
After both tasks complete:

1. **Leaf bundle integrity:** `content/gallery/index.md` exists with `title: "Gallery"` only (3 lines); 18 photos at sibling `content/gallery/photos/` from Plan 06-01.

2. **Layout file integrity:** `themes/minimal/layouts/gallery/single.html` exists at the Type-derived path; defines the `main` block; uses `{{ with .Resources.Match "photos/*" }}` guard; iterates with `range $idx, $photo := .`; emits both Process spec strings verbatim; emits all locked attributes (loading-ternary, fetchpriority-on-first, decoding-on-lazy, empty alt, aria-label with N of M wording); block-balance check passes.

3. **Plan-internal locked-out check:** Zero `target="_blank"`, zero `srcset`, zero `tabindex`, zero `onclick`, zero `data-*`, zero `.page-content` wrapper. All confirmed absent.

4. **Cross-plan integration contract (deferred to Plan 06-04):**
   - Plan 06-04 will run `hugo --minify` — this is the integration gate that resolves:
     - The `Type=gallery` derivation (proves the layout-lookup chain works)
     - The 18 × 2 = 36 WebP rendition generation (proves Process spec strings are valid)
     - The `<body class="page-gallery">` rendering (proves Plan 06-02's body-class hook works)
     - The `body.page-gallery .site-wrapper { max-width: 1100px }` CSS application (proves Plan 06-02's CSS rule fires)
     - The EXIF cleanliness of processed WebP outputs (proves Hugo's WebP encoder strips residual EXIF as expected)
     - The total `public/gallery/` weight (≤ 3 MB target — measure-then-adjust if exceeded)
</verification>

<success_criteria>
This plan is complete when:
- `content/gallery/index.md` is a 3-line leaf-bundle index with title-only frontmatter (D-01)
- `themes/minimal/layouts/gallery/single.html` is a syntactically valid Hugo Go-template at the Type-derived layout path (D-02)
- The template emits the locked Process spec strings, attribute mix, and aria-label wording per D-03/D-05/D-06/D-07/D-08/D-18 + UI-SPEC + RESEARCH § Code Excerpts
- All grep-based acceptance criteria pass (each task's `<acceptance_criteria>` block)
- All locked-out tokens (target=_blank, srcset, tabindex, onclick, data-*, .page-content wrapper) confirmed absent
- Block-balance integrity: 6 opening Go-template blocks match 6 `{{ end }}` closers
- Plan 06-04 (cold-build smoke test) is the next step — this plan does NOT run `hugo --minify`
</success_criteria>

<output>
After completion, create `.planning/phases/06-gallery/06-03-SUMMARY.md` documenting:
- The 2 files created with line counts (content/gallery/index.md = 3 lines; layouts/gallery/single.html = ~25 lines)
- The pre-flight dependency check results (Plans 06-01 and 06-02 contracts verified)
- The block-balance check result (6 opens / 6 ends)
- The grep verification of all locked attributes (Process spec strings, aria-label wording, eager/lazy ternary, fetchpriority/decoding conditionals, empty alt)
- The grep verification of all locked-out tokens (target=_blank, srcset, etc., all returning 0)
- A note that `/gallery/` is now structurally complete: leaf bundle index + 18 photos + Type-derived layout + Plan 06-02's body-class hook + Plan 06-02's CSS rules form the full render path. The cold-build smoke test (Plan 06-04) is the integration gate.
- Cross-phase outgoing contract: the Type-derived layout pattern (`layouts/{type}/single.html`) is now usable by Phase 7 if it converts About to a leaf bundle; the `.Resources.Match "photos/*"` + `image.Process` idiom is now a validated codebase pattern for future image-heavy pages.
</output>
