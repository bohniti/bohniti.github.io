---
phase: 06-gallery
plan: 2
type: execute
wave: 1
depends_on: []
files_modified:
  - hugo.toml
  - themes/minimal/layouts/_default/baseof.html
  - themes/minimal/static/css/style.css
autonomous: true
requirements:
  - GAL-01
  - GAL-02
  - GAL-06

must_haves:
  truths:
    - "hugo.toml contains a Gallery menu entry with weight=2 and the existing About entry's weight is 3"
    - "hugo.toml contains an [imaging.exif] block with disableLatLong = true"
    - "baseof.html line 26 emits <body class=\"page-{{ .Type | default \"default\" }}\"> (one-line edit)"
    - "style.css contains a /* === Gallery === */ section with .gallery-grid, .gallery-item, .gallery-item:focus-visible, .gallery-img rules and a body.page-gallery .site-wrapper { max-width: 1100px } override"
    - "Gallery CSS contains zero .gallery-item:hover rules (locked: zero hover styling per UI-SPEC § Color)"
    - "The existing @media (max-width: 600px) breakpoint is unchanged (auto-fill handles gallery responsiveness naturally)"
  artifacts:
    - path: "hugo.toml"
      provides: "Site config with Gallery nav entry, About weight bumped, [imaging.exif] privacy belt-and-suspenders"
      contains: "name = \"Gallery\", url = \"/gallery/\", weight = 2; [imaging.exif] disableLatLong = true"
    - path: "themes/minimal/layouts/_default/baseof.html"
      provides: "Body element with page-type CSS hook for D-09 width override"
      contains: "<body class=\"page-{{ .Type | default \"default\" }}\">"
    - path: "themes/minimal/static/css/style.css"
      provides: "Gallery section with grid, focus-visible, max-width override"
      contains: "/* === Gallery === */, .gallery-grid, body.page-gallery .site-wrapper { max-width: 1100px }"
  key_links:
    - from: "themes/minimal/layouts/_default/baseof.html line 26"
      to: "themes/minimal/static/css/style.css body.page-gallery rule"
      via: "<body class=\"page-{Type}\"> CSS hook"
      pattern: "grep -c 'body.page-gallery' themes/minimal/static/css/style.css == 1"
    - from: "hugo.toml [[menu.main]] Gallery entry (weight 2)"
      to: "themes/minimal/layouts/partials/header.html range .Site.Menus.main"
      via: "Hugo menu render order (already wired — no template change)"
      pattern: "rendered nav order is Blog, Gallery, About after hugo --minify"
---

<objective>
Wire the site-level infrastructure that the gallery page bundle (Plan 06-03) will hook into: (a) add a `Gallery` entry to `hugo.toml`'s `[[menu.main]]` block at weight 2 and bump the existing `About` entry to weight 3 (D-12), (b) add the `[imaging.exif] disableLatLong = true` belt-and-suspenders block to `hugo.toml` (D-13 prong 2 — required by ROADMAP success criterion 3 even though the source-side scrub from Plan 06-01 is the load-bearing privacy guarantee), (c) modify `themes/minimal/layouts/_default/baseof.html` line 26 from `<body>` to `<body class="page-{{ .Type | default "default" }}">` (D-10 — first introduction of the body-class page-type hook, also unblocks Phase 7's About-leaf-bundle scoping), and (d) append a `/* === Gallery === */` section to `themes/minimal/static/css/style.css` with the locked CSS Grid template, focus-visible rule, and max-width override (D-08, D-09).

Purpose: This plan establishes the static-site infrastructure that makes the gallery navigable, privacy-belted, and visually scoped. None of these edits touch any photo files (Plan 06-01) or the gallery template (Plan 06-03), so this plan is wave-parallel to Plan 06-01. By the end of this plan, navigating to `/gallery/` would 404 (no leaf-bundle yet — Plan 06-03 supplies it), but the site shell is fully prepared: clicking any non-gallery page would render with `<body class="page-{type}">` and the Gallery CSS rules sit dormant in the stylesheet awaiting a `body.page-gallery` consumer.

Output:
- `hugo.toml` updated: 3 menu entries (Blog 1, Gallery 2, About 3), new `[imaging]` table with `[imaging.exif] disableLatLong = true`
- `themes/minimal/layouts/_default/baseof.html` line 26 changed from `<body>` to `<body class="page-{{ .Type | default "default" }}">`
- `themes/minimal/static/css/style.css` gains a `/* === Gallery === */` block (~25 lines) inserted between `/* === Single Post / Page === */` and `/* === Footer === */` containing: `body.page-gallery .site-wrapper { max-width: 1100px }`, `.gallery-grid`, `.gallery-item`, `.gallery-item:focus-visible`, `.gallery-img`
- Cross-page sanity: existing `/about/`, `/blog/`, `/` pages render with `body.page-page` / `body.page-blog` body classes as a side effect of D-10 (RESEARCH § Type Quirk: homepage `.Type` defaults to `"page"`, so the `default "default"` fallback never fires — this is documented surprising-but-correct behavior)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-gallery/06-CONTEXT.md
@.planning/phases/06-gallery/06-RESEARCH.md
@.planning/phases/06-gallery/06-UI-SPEC.md
@.planning/phases/06-gallery/06-PATTERNS.md
@hugo.toml
@themes/minimal/layouts/_default/baseof.html
@themes/minimal/static/css/style.css

<interfaces>
<!-- Current hugo.toml structure (verified via Read; 39 lines total) -->
<!-- Lines 20-28 contain the existing menu block: -->
[menu]
  [[menu.main]]
    name = "Blog"
    url = "/blog/"
    weight = 1
  [[menu.main]]
    name = "About"
    url = "/about/"
    weight = 2

<!-- Lines 30-38 contain the existing [privacy] block: -->
[privacy]
  [privacy.disqus]
    disable = true
  ...

<!-- TARGET hugo.toml after this plan: 3 menu entries (Blog/Gallery/About at weights 1/2/3),
     [imaging] block inserted between [menu] and [privacy] blocks (recommended placement
     per RESEARCH § Code Excerpts and PATTERNS § hugo.toml Edit 3): -->

[menu]
  [[menu.main]]
    name = "Blog"
    url = "/blog/"
    weight = 1
  [[menu.main]]
    name = "Gallery"
    url = "/gallery/"
    weight = 2
  [[menu.main]]
    name = "About"
    url = "/about/"
    weight = 3

[imaging]
  [imaging.exif]
    disableLatLong = true

[privacy]
  ...

<!-- Current baseof.html line 26 (one-line edit target — verified via Read): -->
<body>

<!-- TARGET baseof.html line 26 after this plan: -->
<body class="page-{{ .Type | default "default" }}">

<!-- All other lines (1-25, 27-58) including head IIFE (11-23), favicon partial (9), 
     {{ block "main" }} slot (30), theme-toggle script (34-56) are NOT modified. -->

<!-- Current style.css structure (verified via Read; 320 lines):
       Line 169: /* === Single Post / Page === */
       Line 274: closing `}` of `.page-content hr`
       Line 276: /* === Footer === */
       Line 313: /* === Responsive === */
       Line 314: @media (max-width: 600px) { ... }
       Line 320: end of file
     The new /* === Gallery === */ block is inserted between lines 274 and 276 — between the
     end of "Single Post / Page" and the start of "Footer" (PATTERNS § CSS Section-Comment
     Convention: by content-type reading order). -->

<!-- TARGET style.css insertion (verbatim from RESEARCH § Code Excerpts lines 717-749 + UI-SPEC § Element Specs): -->
/* === Gallery === */
body.page-gallery .site-wrapper {
  max-width: 1100px;
}

.gallery-grid {
  display: grid;
  grid-template-columns: repeat(auto-fill, minmax(220px, 1fr));
  gap: 1rem;
  margin: 0;
}

.gallery-item {
  display: block;
  line-height: 0;        /* kill baseline gap below <img> */
  border-radius: 4px;
}

.gallery-item:focus-visible {
  outline: 2px solid var(--accent);
  outline-offset: 2px;
}

.gallery-img {
  width: 100%;
  height: auto;
  display: block;
  border-radius: 4px;
}

<!-- Existing focus-visible pattern at style.css lines 117-122 (the analog this rule mirrors): -->
.site-nav a:focus-visible,
.theme-toggle:focus-visible {
  outline: 2px solid var(--accent);
  outline-offset: 2px;
  border-radius: 2px;
}
<!-- The new .gallery-item:focus-visible omits `border-radius: 2px` because the wrapper
     already has `border-radius: 4px` — outline traces a 4px-rounded rectangle aligned 
     with the photo edges. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update hugo.toml — insert Gallery menu entry, bump About weight, add [imaging.exif] disableLatLong block</name>
  <read_first>
    - hugo.toml (full current file — 39 lines)
    - .planning/phases/06-gallery/06-CONTEXT.md § "Menu Integration" (D-12)
    - .planning/phases/06-gallery/06-CONTEXT.md § "EXIF Stripping" (D-13 prong 2 — `[imaging.exif] disableLatLong`)
    - .planning/phases/06-gallery/06-CONTEXT.md § "Hugo Image Processing Config" (D-19 — no global quality default)
    - .planning/phases/06-gallery/06-PATTERNS.md § "hugo.toml (MODIFY — three changes)" (verbatim diff)
    - .planning/phases/06-gallery/06-RESEARCH.md § "Hugo `[imaging.exif]` and EXIF Strategy" (caveat: disableLatLong only controls EXIF *extraction in templates*, NOT EXIF preservation in processed output — the source-side scrub from Plan 06-01 is the load-bearing privacy guarantee)
  </read_first>
  <files>hugo.toml</files>
  <action>
**Three edits to `hugo.toml` — apply via Edit tool (NOT shell heredoc).**

**Edit 1 — Insert Gallery menu entry between Blog and About (after line 24, before line 25).**

Replace this exact block (current lines 21-28):

```toml
  [[menu.main]]
    name = "Blog"
    url = "/blog/"
    weight = 1
  [[menu.main]]
    name = "About"
    url = "/about/"
    weight = 2
```

With this:

```toml
  [[menu.main]]
    name = "Blog"
    url = "/blog/"
    weight = 1
  [[menu.main]]
    name = "Gallery"
    url = "/gallery/"
    weight = 2
  [[menu.main]]
    name = "About"
    url = "/about/"
    weight = 3
```

This combines D-12's two changes: insert Gallery (weight 2) and bump About (weight 2 → 3). The two-space indentation matches the existing pattern verified via the file Read.

**Edit 2 — Insert `[imaging]` table block between the menu block (now ending at the bumped About weight) and the existing `[privacy]` table.**

Insert this block immediately AFTER the closing `weight = 3` of About (at the new line position post-Edit-1) and BEFORE the existing `[privacy]` line, separated by blank lines:

```toml

[imaging]
  [imaging.exif]
    disableLatLong = true
```

Concretely: the resulting hugo.toml will have, in sequence:
- `[menu]` section ending with `    weight = 3` (After Edit 1)
- (blank line)
- `[imaging]`
- `  [imaging.exif]`
- `    disableLatLong = true`
- (blank line)
- `[privacy]` section (unchanged)

**Why placement matters:** Hugo accepts `[imaging]` table anywhere in the file, but logical grouping by topic (markup → menu → imaging → privacy) keeps the file readable. Per PATTERNS § hugo.toml Edit 3, this placement keeps the "site config → markup → menu → imaging → privacy" reading flow.

**Why no `[imaging] quality default`:** D-19 explicitly forbids a global `[imaging] quality = N` default; Plan 06-03's per-call `q75` / `q82` quality strings stay explicit and auditable. The `[imaging]` table contains ONLY the `[imaging.exif]` sub-table.

**Critical caveat (RESEARCH-verified):** `disableLatLong = true` controls EXIF *extraction* in templates (e.g., `.Exif.Lat`); it does NOT prevent EXIF preservation in processed output. The load-bearing privacy guarantee is the source-side `exiftool` scrub from Plan 06-01. This `[imaging.exif]` block satisfies ROADMAP success criterion 3 as documented belt-and-suspenders, NOT as a substitute for source-side stripping.

**Edit 3 — None.** The file currently ends at line 38 (`privacyEnhanced = true` of `[privacy.youtube]`). Do NOT add any other config.
  </action>
  <verify>
    <automated>[ "$(grep -c '\[\[menu.main\]\]' hugo.toml)" = "3" ] && [ "$(grep -c 'name = "Gallery"' hugo.toml)" = "1" ] && [ "$(grep -c 'url = "/gallery/"' hugo.toml)" = "1" ] && [ "$(grep -c '\[imaging.exif\]' hugo.toml)" = "1" ] && [ "$(grep -c 'disableLatLong = true' hugo.toml)" = "1" ] && [ "$(awk '/name = "About"/{f=1} f && /weight =/{print; exit}' hugo.toml)" = "    weight = 3" ] && [ "$(awk '/name = "Gallery"/{f=1} f && /weight =/{print; exit}' hugo.toml)" = "    weight = 2" ] && [ "$(awk '/name = "Blog"/{f=1} f && /weight =/{print; exit}' hugo.toml)" = "    weight = 1" ]</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '\[\[menu.main\]\]' hugo.toml` returns exactly `3`
    - `grep -c 'name = "Gallery"' hugo.toml` returns `1`
    - `grep -c 'url = "/gallery/"' hugo.toml` returns `1`
    - `grep -c 'name = "Blog"' hugo.toml` returns `1` (unchanged)
    - `grep -c 'name = "About"' hugo.toml` returns `1` (unchanged)
    - `awk '/name = "Blog"/{f=1} f && /weight =/{print; exit}' hugo.toml` outputs `    weight = 1`
    - `awk '/name = "Gallery"/{f=1} f && /weight =/{print; exit}' hugo.toml` outputs `    weight = 2`
    - `awk '/name = "About"/{f=1} f && /weight =/{print; exit}' hugo.toml` outputs `    weight = 3` (the bump)
    - `grep -c '^\[imaging\]$' hugo.toml` returns `1`
    - `grep -c '\[imaging.exif\]' hugo.toml` returns `1`
    - `grep -c 'disableLatLong = true' hugo.toml` returns `1`
    - `grep -c '^\[privacy\]$' hugo.toml` returns `1` (privacy block still present, unchanged)
    - `grep -c '^\[markup\]$' hugo.toml` returns `1` (markup block still present, unchanged)
    - `grep -c 'baseURL = ' hugo.toml` returns `1` (header line unchanged)
  </acceptance_criteria>
  <done>
`hugo.toml` contains exactly 3 `[[menu.main]]` entries (Blog/Gallery/About) at weights 1/2/3 in that order; an `[imaging]` table with `[imaging.exif] disableLatLong = true` is present between the menu block and the privacy block. All other config (baseURL, languageCode, title, theme, paginate, params, markup, privacy) is unchanged from the pre-edit state.
  </done>
</task>

<task type="auto">
  <name>Task 2: Edit baseof.html line 26 — add page-type body class hook</name>
  <read_first>
    - themes/minimal/layouts/_default/baseof.html (full file — 58 lines)
    - .planning/phases/06-gallery/06-CONTEXT.md § "CSS Grid & Wider Canvas" (D-10)
    - .planning/phases/06-gallery/06-PATTERNS.md § "themes/minimal/layouts/_default/baseof.html (MODIFY — line 26 only)" (verbatim diff with surrounding context)
    - .planning/phases/06-gallery/06-RESEARCH.md § "Type Quirk: Homepage" (homepage `.Type` defaults to `"page"`, so `body.page-page` is what the homepage emits — `default "default"` fallback never fires; documented surprising-but-correct behavior)
    - .planning/phases/06-gallery/06-UI-SPEC.md § "Body-Class Pattern Documentation" (per-page class output map)
  </read_first>
  <files>themes/minimal/layouts/_default/baseof.html</files>
  <action>
**One-line edit on line 26 of `themes/minimal/layouts/_default/baseof.html` — apply via Edit tool.**

Surrounding context (DO NOT modify lines 25 or 27 — verified via PATTERNS § baseof.html):

```
Line 25: </head>
Line 26: <body>                       ← edit target
Line 27:   <div class="site-wrapper">
```

Replace line 26 from:

```html
<body>
```

To:

```html
<body class="page-{{ .Type | default "default" }}">
```

**Why this exact form (D-10 / UI-SPEC § Body-Class Pattern Documentation):**
- `page-{{ .Type }}` produces self-documenting class names. The expected per-page outputs are:

| Page route | `.Type` | Emitted body class |
|------------|---------|--------------------|
| `/` (homepage) | `"page"` | `page-page` |
| `/blog/` (list) | `"blog"` | `page-blog` |
| `/blog/{slug}/` (post) | `"blog"` | `page-blog` |
| `/gallery/` (after Plan 06-03 leaf bundle) | `"gallery"` | `page-gallery` ← the hook for D-09 CSS |
| `/about/` (today, standalone) | `"page"` | `page-page` (will become `page-about` after Phase 7's leaf-bundle conversion) |

- `default "default"` — defensive fallback documented in CONTEXT D-10. Per RESEARCH § Type Quirk, the fallback never fires because Hugo's `.Type` ALWAYS has a value (defaults to `"page"` on standalone pages and home), but the explicit `default` clause signals defensive intent for future maintainers.

**DO NOT change ANY other line.** The pre-edit file has:
- Lines 1-10: `<!DOCTYPE html>` + `<html>` + `<head>` opening + `<meta>` tags + `<title>` + `{{ partial "favicon.html" . }}`
- Lines 11-23: theme bootstrap IIFE (Phase 4 D-09 — preserves no-FOUC theme application)
- Line 24: stylesheet link
- Line 25: `</head>`
- Line 26: `<body>` ← THE ONLY LINE THAT CHANGES
- Line 27: `<div class="site-wrapper">`
- Lines 28-32: header partial, main block, footer partial
- Lines 34-56: theme-toggle click-handler IIFE (Phase 4 D-13)
- Line 57-58: `</body></html>`

Verify NO other line has been modified by inspecting the file post-edit.

**Important quoting note:** The Edit tool will need to handle the embedded double-quotes correctly. The string to write is:

```
<body class="page-{{ .Type | default "default" }}">
```

Where `class="page-{{ .Type | default "default" }}"` has unescaped inner quotes around `default`. This is valid HTML+Go-template syntax (Go templates parse `"default"` as a string argument before HTML rendering; HTML parsers see only the outer `class="..."`). Verify by inspecting the file post-edit and confirming the literal text matches.
  </action>
  <verify>
    <automated>[ "$(grep -c 'class="page-{{ .Type' themes/minimal/layouts/_default/baseof.html)" = "1" ] && [ "$(grep -c '^<body>$' themes/minimal/layouts/_default/baseof.html)" = "0" ] && [ "$(grep -c '{{ block "main" . }}' themes/minimal/layouts/_default/baseof.html)" = "1" ] && [ "$(grep -c 'partial "favicon.html"' themes/minimal/layouts/_default/baseof.html)" = "1" ] && [ "$(grep -c 'document.documentElement.dataset.theme = theme' themes/minimal/layouts/_default/baseof.html)" = "1" ] && [ "$(wc -l < themes/minimal/layouts/_default/baseof.html | tr -d ' ')" = "58" ]</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'class="page-{{ .Type' themes/minimal/layouts/_default/baseof.html` returns `1` (the new body class hook is present)
    - `grep -c '^<body>$' themes/minimal/layouts/_default/baseof.html` returns `0` (the bare `<body>` line is gone)
    - `grep -c 'default "default"' themes/minimal/layouts/_default/baseof.html` returns `1` (the defensive fallback is present)
    - `grep -c '{{ block "main" . }}' themes/minimal/layouts/_default/baseof.html` returns `1` (slot untouched — Phase 5 contract preserved)
    - `grep -c 'partial "favicon.html"' themes/minimal/layouts/_default/baseof.html` returns `1` (Phase 5 D-13/D-14 contract preserved)
    - `grep -c 'document.documentElement.dataset.theme = theme' themes/minimal/layouts/_default/baseof.html` returns `1` (Phase 4 head IIFE preserved)
    - `grep -c 'document.querySelector(.\\.theme-toggle.)' themes/minimal/layouts/_default/baseof.html` returns `1` (Phase 4 click handler preserved)
    - `wc -l < themes/minimal/layouts/_default/baseof.html` returns `58` (no lines added or removed — only line 26 changed)
  </acceptance_criteria>
  <done>
Line 26 of `themes/minimal/layouts/_default/baseof.html` has been updated from `<body>` to `<body class="page-{{ .Type | default "default" }}">`. All other lines (head, IIFE, partials, slot, footer) are byte-identical to the pre-edit state. The file remains 58 lines total. The body-class hook is now ready for the `body.page-gallery` CSS rule from Task 3 to consume; cross-page render check (rendering `/about/` produces `page-page`, `/blog/` produces `page-blog`) is deferred to Plan 06-04's cold-build smoke test.
  </done>
</task>

<task type="auto">
  <name>Task 3: Append /* === Gallery === */ section to style.css with grid, focus-visible, max-width override</name>
  <read_first>
    - themes/minimal/static/css/style.css (full file — 320 lines; especially lines 117-122 for focus-visible analog and lines 169-274 for surrounding section context)
    - .planning/phases/06-gallery/06-CONTEXT.md § "CSS Grid & Wider Canvas" (D-08, D-09, D-10, D-11)
    - .planning/phases/06-gallery/06-UI-SPEC.md § "Element Specs" (verbatim CSS for .gallery-grid, .gallery-item, .gallery-item:focus-visible, .gallery-img)
    - .planning/phases/06-gallery/06-PATTERNS.md § "themes/minimal/static/css/style.css (MODIFY — append section)" (insertion point + verbatim CSS + analog excerpts)
    - .planning/phases/06-gallery/06-RESEARCH.md § "CSS Specificity Verification" (body.page-gallery .site-wrapper specificity (0,2,0) beats default .site-wrapper (0,1,0) — no !important needed)
    - .planning/phases/06-gallery/06-RESEARCH.md § "Responsive Collapse Math" (CONTEXT D-11 was empirically wrong about 600px collapse — actual 1-col breakpoint is ~488px, but auto-fill behavior is acceptable as-is; no mobile breakpoint changes needed)
  </read_first>
  <files>themes/minimal/static/css/style.css</files>
  <action>
**One append operation on `themes/minimal/static/css/style.css` — apply via Edit tool.**

**Insertion point (verified via Read):**
- Line 274: `}` (closing brace of `.page-content hr { border: none; border-top: 1px solid var(--border); margin: 2rem 0; }`)
- Line 275: blank line
- Line 276: `/* === Footer === */`

Insert the new `/* === Gallery === */` section between line 274 and line 276 — i.e., AFTER the existing `}` at line 274 and BEFORE the existing `/* === Footer === */` comment at line 276. Maintain a blank line on both sides of the new section comment per the existing CSS file convention (verified at lines 168/169, 275/276, 312/313).

**Exact text to insert** (verbatim from RESEARCH § Code Excerpts lines 717-749 + UI-SPEC § Element Specs + PATTERNS § style.css verbatim):

```css

/* === Gallery === */
body.page-gallery .site-wrapper {
  max-width: 1100px;
}

.gallery-grid {
  display: grid;
  grid-template-columns: repeat(auto-fill, minmax(220px, 1fr));
  gap: 1rem;
  margin: 0;
}

.gallery-item {
  display: block;
  line-height: 0;        /* kill baseline gap below <img> */
  border-radius: 4px;
}

.gallery-item:focus-visible {
  outline: 2px solid var(--accent);
  outline-offset: 2px;
}

.gallery-img {
  width: 100%;
  height: auto;
  display: block;
  border-radius: 4px;
}
```

**Per-rule provenance:**

| Rule | Source | Note |
|------|--------|------|
| `body.page-gallery .site-wrapper { max-width: 1100px; }` | D-09 / UI-SPEC § Layout | Specificity (0,2,0) beats default `.site-wrapper { max-width: var(--max-width); }` at line 60 (specificity (0,1,0)) — no `!important` needed (RESEARCH § CSS Specificity Verification) |
| `grid-template-columns: repeat(auto-fill, minmax(220px, 1fr))` | D-08 / ROADMAP locked verbatim | `auto-fill` (NOT `auto-fit`) is correct — RESEARCH § auto-fill vs auto-fit confirms with 18 items in 4-col rows the trailing 2 photos must stay at column natural width, not stretch the row |
| `gap: 1rem` | D-08 | Matches existing 16px md spacing token (UI-SPEC § Spacing Scale) |
| `margin: 0` on `.gallery-grid` | D-08 / UI-SPEC § Element Specs | Explicit zero override against any inherited `.page-content p`-style margins |
| `.gallery-item { line-height: 0 }` | D-08 / UI-SPEC § Element Specs | Eliminates inline-baseline whitespace gap below `<img>` |
| `.gallery-item:focus-visible { outline: 2px solid var(--accent); outline-offset: 2px; }` | D-discretion (Claude's, recommended yes per CONTEXT § Claude's Discretion) / UI-SPEC § Accessibility Contract | Mirrors existing `.site-nav a:focus-visible` pattern at style.css lines 117-122. NOTE: Omits `border-radius: 2px` from the focus rule because the wrapper already has `border-radius: 4px` (the outline traces a 4px-rounded rectangle aligned with photo edges) |
| `.gallery-img { width: 100%; height: auto; display: block; border-radius: 4px; }` | D-08 / UI-SPEC § Element Specs | `width: 100%` fills cell; `height: auto` preserves Hugo-Process-fixed 600x400 aspect; `border-radius: 4px` matches existing `.page-content img` at line 225 |

**What NOT to add (locked OUT — UI-SPEC § Color + § Out of Scope):**
- NO `.gallery-item:hover` rule. Locked: zero hover styling per UI-SPEC § Color "no opacity dimming, scale transforms, color overlays, or border highlights." The cursor-changes-to-pointer (browser default for `<a href>`) is the sufficient interaction signal.
- NO `transition` on `.gallery-item:focus-visible`. Match existing focus-visible patterns which have no transitions.
- NO mobile-specific `.gallery-grid` rules under `@media (max-width: 600px)`. The existing breakpoint at lines 314-320 stays UNCHANGED — auto-fill collapses naturally at narrow viewports (RESEARCH § Responsive Collapse Math: actual 1-col breakpoint is ~488px, accepted as-is).
- NO `[data-theme="dark"]` block in the Gallery section. Photos are theme-agnostic raster content; only the focus-outline and heading text inherit theme tokens via existing `var(--accent)` / `var(--text)` rules — no per-theme override needed.

**File length check:** The pre-edit file is 320 lines. After the insert (the new section is approximately 28 lines including the section comment and blank lines), the file will be ~348 lines. The exact line count depends on blank-line preservation around the section.
  </action>
  <verify>
    <automated>[ "$(grep -c '/\* === Gallery === \*/' themes/minimal/static/css/style.css)" = "1" ] && [ "$(grep -c 'body\.page-gallery \.site-wrapper' themes/minimal/static/css/style.css)" = "1" ] && [ "$(grep -c 'max-width: 1100px' themes/minimal/static/css/style.css)" = "1" ] && [ "$(grep -c '\.gallery-grid' themes/minimal/static/css/style.css)" = "1" ] && [ "$(grep -c '\.gallery-item' themes/minimal/static/css/style.css)" = "2" ] && [ "$(grep -c '\.gallery-img' themes/minimal/static/css/style.css)" = "1" ] && [ "$(grep -c '\.gallery-item:hover' themes/minimal/static/css/style.css)" = "0" ] && [ "$(grep -c 'repeat(auto-fill, minmax(220px, 1fr))' themes/minimal/static/css/style.css)" = "1" ] && [ "$(grep -c '@media (max-width: 600px)' themes/minimal/static/css/style.css)" = "1" ]</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '/\* === Gallery === \*/' themes/minimal/static/css/style.css` returns `1` (new section header present)
    - `grep -c 'body\.page-gallery \.site-wrapper' themes/minimal/static/css/style.css` returns `1` (max-width override scoped to gallery)
    - `grep -c 'max-width: 1100px' themes/minimal/static/css/style.css` returns `1`
    - `grep -c '\.gallery-grid' themes/minimal/static/css/style.css` returns `1`
    - `grep -c '\.gallery-item' themes/minimal/static/css/style.css` returns `2` (the rule + the `:focus-visible` rule)
    - `grep -c '\.gallery-img' themes/minimal/static/css/style.css` returns `1`
    - `grep -c '\.gallery-item:hover' themes/minimal/static/css/style.css` returns `0` (LOCKED: zero hover styling)
    - `grep -c 'repeat(auto-fill, minmax(220px, 1fr))' themes/minimal/static/css/style.css` returns `1` (verbatim ROADMAP-locked template — D-08)
    - `grep -c 'gap: 1rem' themes/minimal/static/css/style.css` returns at least `1` (the new gallery rule; could be more if other rules already use this)
    - `grep -c '@media (max-width: 600px)' themes/minimal/static/css/style.css` returns `1` (existing breakpoint UNCHANGED)
    - `grep -c 'outline: 2px solid var(--accent)' themes/minimal/static/css/style.css` returns at least `2` (existing nav-link focus rule + new gallery-item focus rule)
    - `grep -c '/\* === Single Post / Page === \*/' themes/minimal/static/css/style.css` returns `1` (existing section header preserved)
    - `grep -c '/\* === Footer === \*/' themes/minimal/static/css/style.css` returns `1` (existing section header preserved)
    - `awk '/=== Gallery ===/{found=1} found && /=== Footer ===/{print "ORDER OK"; exit}' themes/minimal/static/css/style.css` outputs `ORDER OK` (Gallery section appears before Footer section in file order)
    - The CSS validates as parse-clean: `awk '{open+=gsub(/\{/,""); close+=gsub(/\}/,"");} END{print (open==close ? "BALANCED" : "UNBALANCED open="open" close="close)}' themes/minimal/static/css/style.css` outputs `BALANCED`
  </acceptance_criteria>
  <done>
A new `/* === Gallery === */` section has been appended to `themes/minimal/static/css/style.css` between the `/* === Single Post / Page === */` section (ending at the `.page-content hr` rule) and the `/* === Footer === */` section. The new section contains exactly 5 rules: `body.page-gallery .site-wrapper`, `.gallery-grid`, `.gallery-item`, `.gallery-item:focus-visible`, `.gallery-img`. The CSS is parse-clean (matched braces). No `.gallery-item:hover` rule exists (locked). The existing `@media (max-width: 600px)` breakpoint at the bottom of the file is unchanged. CSS specificity for the max-width override (`(0,2,0)`) wins against the default `.site-wrapper` selector (`(0,1,0)`) without `!important`.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Site config (`hugo.toml`) → all rendered pages | A bad TOML edit breaks the entire build. Hugo's TOML parser will surface errors at build time. |
| Base template (`baseof.html`) → every rendered page | A syntax error in line 26 (mismatched braces in the Go-template expression) breaks every page. |
| Stylesheet → visual regression on every page | The new `/* === Gallery === */` section is appended in a non-overlapping location; cross-page CSS regression is highly unlikely but verifiable via `hugo --minify` from Plan 06-04. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06.2-01 | Tampering | TOML syntax error in `hugo.toml` after edit | mitigate | Acceptance criteria use grep on exact tokens (`name = "Gallery"`, `weight = 2`, etc.); Plan 06-04's `hugo --minify` is the integration gate that surfaces any TOML parse error. |
| T-06.2-02 | Tampering | Go-template syntax error on baseof.html line 26 (mismatched quotes) | mitigate | Acceptance criterion `grep -c 'class="page-{{ .Type'` confirms the literal text after edit; line-count check confirms no spurious newline insertion; Plan 06-04 `hugo --minify` surfaces template-parse errors. |
| T-06.2-03 | Tampering | CSS brace imbalance after section append | mitigate | Acceptance criterion `awk '...gsub(/\{/...)' BALANCED` confirms parse-clean; matched-pair check is straightforward and reliable for hand-authored CSS. |
| T-06.2-04 | Information Disclosure | `disableLatLong = false` (or absent) lets template extract GPS to user-visible HTML | mitigate | This plan SETS `disableLatLong = true`. Combined with Plan 06-03's gallery template emitting zero `.Exif.*` references, the lat/long path is double-blocked: extraction disabled AND no consumer. The source-side scrub from Plan 06-01 is the load-bearing layer for non-GPS EXIF (Make/Model/Serial). |
| T-06.2-05 | Denial of Service | About menu entry duplicated (weight 2 + weight 3) breaks nav rendering | mitigate | Edit 1 collapses the bump into the same edit as the Gallery insertion (single replace block). Acceptance criterion `grep -c 'name = "About"'` returns exactly `1`. |
| T-06.2-06 | Information Disclosure | Body class accidentally exposed to a `[data-attr]` selector that downstream JS reads | accept | The body class is purely visual (CSS scoping); no JS in the codebase reads `document.body.className`. Future risk low; pattern is widely-used Hugo idiom. |
| T-06.2-07 | Cross-page CSS regression | New CSS rule accidentally targets non-gallery pages | mitigate | All new selectors are scoped: `body.page-gallery .site-wrapper`, `.gallery-grid`, `.gallery-item`, `.gallery-img`. None of these class names appear elsewhere in the codebase (verifiable via `grep`). Plan 06-04 cold-build smoke test confirms `/about/`, `/blog/`, `/` render unchanged. |
</threat_model>

<verification>
After all three tasks complete:

1. **hugo.toml structural integrity:** 3 menu entries at weights 1/2/3 (Blog/Gallery/About); `[imaging.exif] disableLatLong = true` present; existing `[markup]` and `[privacy]` blocks unchanged.

2. **baseof.html surgical precision:** Only line 26 modified; file remains 58 lines; head IIFE, favicon partial, main slot, click-handler IIFE all preserved.

3. **style.css clean append:** `/* === Gallery === */` section sits between Single Post and Footer sections; 5 rules present (including focus-visible); zero hover rules; matched braces; existing breakpoint untouched.

4. **Cross-task interface contract:** The body-class hook on baseof.html line 26 must produce `page-gallery` when consumed by Plan 06-03's leaf bundle (`content/gallery/index.md` derives `Type=gallery`). The CSS rule `body.page-gallery .site-wrapper { max-width: 1100px }` will fire when that body class is emitted. This integration gate is verified in Plan 06-04 via `hugo --minify` and `grep 'class="page-gallery"' public/gallery/index.html`.

5. **No build verification in this plan:** `hugo --minify` is intentionally NOT run here. The full integration smoke is reserved for Plan 06-04 (cold build with content/gallery/photos/ from Plan 06-01 AND content/gallery/index.md + layouts/gallery/single.html from Plan 06-03 ALL present).
</verification>

<success_criteria>
This plan is complete when:
- `hugo.toml` has 3 `[[menu.main]]` entries at weights 1/2/3 (Blog → Gallery → About) AND an `[imaging.exif] disableLatLong = true` block (D-12, D-13 prong 2)
- `themes/minimal/layouts/_default/baseof.html` line 26 is `<body class="page-{{ .Type | default "default" }}">` (D-10)
- `themes/minimal/static/css/style.css` contains a `/* === Gallery === */` section with grid, focus-visible, and max-width override (D-08, D-09)
- All grep-based acceptance criteria pass (each task's `<acceptance_criteria>` block)
- No regressions to surrounding code: head IIFE, favicon partial, click-handler IIFE, existing CSS sections, existing menu entries — all preserved
- File-length sanity: baseof.html still 58 lines; hugo.toml grew by ~9 lines; style.css grew by ~28 lines
</success_criteria>

<output>
After completion, create `.planning/phases/06-gallery/06-02-SUMMARY.md` documenting:
- The three edit operations performed (hugo.toml menu+imaging, baseof.html line 26, style.css gallery section)
- Verification outputs from each task's grep gates (paste exit codes / grep counts)
- Confirmation that Phase 4's head IIFE and Phase 5's favicon partial are unchanged in baseof.html
- The integration interface for Plan 06-03: when its `content/gallery/index.md` produces `Type=gallery`, the body class becomes `page-gallery`, which triggers `max-width: 1100px` from the new CSS rule
- The integration interface for Plan 06-04: `hugo --minify` will be the cross-cutting build verification; this plan does NOT run it
- Cross-phase outgoing contract: the body-class pattern is now usable by Phase 7 (when `content/about.md` becomes `content/about/index.md`, body class becomes `page-about`, enabling Phase 7's "richer About layout" CSS scoping)
</output>
