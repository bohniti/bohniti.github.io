---
phase: 09-about-dynamic-rounded-redesign
plan: 03
type: execute
wave: 3
depends_on:
  - 09-01
  - 09-02
files_modified:
  - .planning/phases/09-about-dynamic-rounded-redesign/09-HUMAN-UAT.md
  - .planning/STATE.md
autonomous: true
requirements:
  - ABOUT-01
  - ABOUT-02
  - ABOUT-03
  - ABOUT-04
  - ABOUT-05
  - ABOUT-06
  - ABOUT-07
tags:
  - verification
  - human-uat
  - gates
  - phase-close

must_haves:
  truths:
    - "All 11 UI-SPEC verification gates have been executed: 6 fully automated (Gates 2, 3, 8, 9, 10, 11), 4 partially automated with HUMAN-UAT items captured (Gates 1, 4, 6, 7), and 1 fully HUMAN-UAT (Gate 5)"
    - "Hugo builds twice in succession produce byte-identical `public/about/index.html` (build determinism — Gate 11)"
    - "No JS file introduced in the About surface paths (Pitfall 21 — Gate 10)"
    - "No generic class names like `.card`, `.section`, `.feature`, `.role`, `.row`, `.split` exist as top-level rules in style.css (Pitfall 17 — Gate 9)"
    - "Every `<img>` in /about/ ships with the new title arm classes (`about-hero-img`, `about-grid-item`) and appropriate width/height attributes from Hugo `image.Process`"
    - "`.planning/phases/09-about-dynamic-rounded-redesign/09-HUMAN-UAT.md` exists with the 5 deferred-to-deploy gate checklists scaffolded; STATE.md Deferred Items table updated to track it (matches Phase 5/05.1/06/07 project precedent)"
  artifacts:
    - path: ".planning/phases/09-about-dynamic-rounded-redesign/09-HUMAN-UAT.md"
      provides: "Post-deploy browser walkthrough checklist covering Gates 1, 4c, 5, 6b, 7a (visual-judgment items)"
      contains: "Gate 5 — Content rebalance"
      min_lines: 60
    - path: ".planning/STATE.md"
      provides: "Updated Deferred Items table with row for 09-HUMAN-UAT.md"
      contains: "09-HUMAN-UAT.md"
  key_links:
    - from: ".planning/phases/09-about-dynamic-rounded-redesign/09-HUMAN-UAT.md"
      to: "Post-deploy `https://tbohnstedt.cloud/about/` browser walkthrough"
      via: "Manual checklist drained at next milestone close"
      pattern: "tbohnstedt.cloud/about"
    - from: ".planning/STATE.md ## Deferred Items"
      to: ".planning/phases/09-about-dynamic-rounded-redesign/09-HUMAN-UAT.md"
      via: "Filename reference in Deferred Items table"
      pattern: "09-HUMAN-UAT.md"
---

<objective>
Plan 09-03 — Verification + HUMAN-UAT scaffolding. No code changes. Run all 11 UI-SPEC verification gates, then author a HUMAN-UAT checklist for the 5 visual-judgment gates that can only be confirmed in a browser against a deployed build, then update STATE.md Deferred Items to track the deferral (matches Phase 5/05.1/06/07 project precedent — see STATE.md § Deferred Items).

Purpose: Phase 9 ships when (a) every automatable gate passes against the code on disk, and (b) the human-judgment gates have a concrete checklist queued for the next post-deploy browser walkthrough. This plan closes the verification surface and ends the phase.

Output: 1 new `09-HUMAN-UAT.md` checklist (~60-80 LOC of markdown), 1 STATE.md edit (1 row added to Deferred Items table). Total: ~80 LOC of markdown, 0 LOC of code.

**Gate distribution (per RESEARCH § "Gate-to-Plan map"):**
- **Fully automated in this plan:** Gates 2 (hook arms), 3 (radius applied), 8 (no hard-coded colors), 9 (no generic class names), 10 (no JS on About), 11 (build determinism), plus parts of 4 (a + b CSS contract), 6 (a + c + d CSS contract).
- **Authored as HUMAN-UAT items:** Gates 1 (visual asymmetric layout), 4c (no leak to /blog/ + /gallery/), 5 (content rebalance read-through), 6b (live DevTools dark-mode contrast inspector), 7a (DevTools mobile emulation at 320 px).

**Gate 2 adaptation note (carried from 09-01-SUMMARY):** UI-SPEC Gate 2 specifies `grep -c "else if eq \$title" ≥ 3`. The locked render-image template uses pre-bound boolean variables (`$isHero`, `$isGrid`, `$isSplit`, `$isFeature`) rather than inline `eq $title` re-checks. This plan adapts Gate 2 to grep for the boolean declarations: `grep -cE 'eq \$title "(hero|grid|split|feature)"' themes/minimal/layouts/_default/_markup/render-image.html` returns ≥ 3 (or 4 with feature shipping — Phase 9 ships feature, so expect 4).

**Mode:** Light plan. ~10-15% context. 2 tasks. The first task runs ~10 grep/find/build commands and records results; the second task authors the HUMAN-UAT markdown + edits STATE.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-about-dynamic-rounded-redesign/09-CONTEXT.md
@.planning/phases/09-about-dynamic-rounded-redesign/09-UI-SPEC.md
@.planning/phases/09-about-dynamic-rounded-redesign/09-PATTERNS.md
@.planning/phases/09-about-dynamic-rounded-redesign/09-01-SUMMARY.md
@.planning/phases/09-about-dynamic-rounded-redesign/09-02-SUMMARY.md

<interfaces>
<!-- Verbatim verification commands from UI-SPEC § "Verification Gates" + RESEARCH § "Concrete verification commands". -->

GATE 2 (adapted) — render-image hook arm count:
```bash
grep -cE 'eq \$title "(hero|grid|split|feature)"' themes/minimal/layouts/_default/_markup/render-image.html
# Expect: 4 (hero, grid, split, feature — feature ships per locked branch decision in 09-01)
```

GATE 3 — `--radius-soft` token applied:
```bash
grep -c 'var(--radius-soft)' themes/minimal/static/css/style.css
# Expect: ≥ 5; phase 9 actual ≥ 5 (about-hero photos, about-grid items, about-role-card, about-feature × 2)
```

GATE 4a — role-card visual contract present:
```bash
grep -A 5 'body.page-about .about-role-card {' themes/minimal/static/css/style.css | grep -cE 'background: var\(--bg-secondary\)|border: 1px solid var\(--border\)|border-radius: var\(--radius-soft\)'
# Expect: 3 (all three required properties present)
```

GATE 4b — role-card forbidden treatments absent:
```bash
grep -A 5 'body.page-about .about-role-card' themes/minimal/static/css/style.css | grep -cE 'box-shadow|linear-gradient|transform:|transition:'
# Expect: 0
```

GATE 6c — pullquote typography invariant preserved:
```bash
grep -A 4 'body.page-about .about-pullquote {' themes/minimal/static/css/style.css | grep -cE 'font-size: 1.4rem|font-weight: 500'
# Expect: 2

grep -A 2 'body.page-about .about-pullquote strong' themes/minimal/static/css/style.css | grep -c 'font-weight: 700'
# Expect: 1
```

GATE 6d — load-bearing contrast comment preserved:
```bash
grep -c 'Dark-mode #D14D41 on #1C1B1A measures 3.97:1' themes/minimal/static/css/style.css
# Expect: 1
```

GATE 8 — hard-coded color audit (inverted form per RESEARCH `[ASSUMED]`):
```bash
# Every #hex literal in style.css must be inside one of the two :root blocks (light + dark palette).
# We grep for any #hex outside :root blocks that touches About-related rules.
awk '/^:root/ {in_root=1} /^\}/ {if (in_root) {in_root=0; next}} !in_root {print NR": "$0}' themes/minimal/static/css/style.css | grep -E '#[0-9A-Fa-f]{3,8}' | grep -E 'about-|page-about'
# Expect: 0 matches
```

GATE 9 — generic class-name leak:
```bash
grep -nE '^\.(card|section|feature|role|row|split)\b' themes/minimal/static/css/style.css
# Expect: 0 matches
```

GATE 10 — no JS on About surface:
```bash
find content/about themes/minimal/layouts/about themes/minimal/layouts/shortcodes -name '*.js' 2>/dev/null
# Expect: empty output
```

GATE 11 — build determinism:
```bash
rm -rf public public.first
hugo --minify
mv public public.first
hugo --minify
diff -r public.first/about/ public/about/
rm -rf public public.first
# Expect: no output from diff (byte-identical)
```

PHASE 5/05.1/06/07 PRECEDENT — STATE.md Deferred Items table format (read STATE.md § Deferred Items lines ~96-107):
```
| Category | Phase | Item | Status | Resolution Path |
|----------|-------|------|--------|-----------------|
| uat | 05 | 05-HUMAN-UAT.md (4 scenarios) | partial | Post-deploy browser walkthrough |
| uat | 05.1 | 05.1-HUMAN-UAT.md (6 scenarios) | partial | Post-deploy browser walkthrough |
| uat | 06 | 06-HUMAN-UAT.md | unknown | Post-deploy browser walkthrough |
| uat | 07 | 07-HUMAN-UAT.md | unknown | Post-deploy browser walkthrough at https://tbohnstedt.cloud/about/ |
```
Phase 9 follows the same row format. Add ONE row for 09-HUMAN-UAT.md.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Run all automated verification gates (Gates 2, 3, 4a, 4b, 6c, 6d, 8, 9, 10, 11) and record results</name>
  <files>(no file modifications — verification only; results recorded in 09-03-SUMMARY.md by execute-plan flow)</files>
  <read_first>
    - themes/minimal/layouts/_default/_markup/render-image.html (post-09-01)
    - themes/minimal/static/css/style.css (post-09-02)
    - themes/minimal/layouts/about/single.html (post-09-02)
    - content/about/index.md (post-09-02)
    - .planning/phases/09-about-dynamic-rounded-redesign/09-UI-SPEC.md § "Verification Gates" (full 11-gate list — verbatim)
    - .planning/phases/09-about-dynamic-rounded-redesign/09-RESEARCH.md § "Concrete verification commands (copy/paste-ready)" (commands authored in canonical form)
    - .planning/phases/09-about-dynamic-rounded-redesign/09-01-SUMMARY.md (Gate 2 adaptation note)
    - .planning/phases/09-about-dynamic-rounded-redesign/09-02-SUMMARY.md (confirms feature shipped, build succeeded)
  </read_first>
  <action>
    Run each verification command below in sequence. Record the actual output (counts, line numbers, or empty) inline in the executor's working notes. Pass criteria are explicit per gate.

    **GATE 2 (REQ ABOUT-02 — render-image hook arms — adapted form):**
    ```bash
    grep -cE 'eq \$title "(hero|grid|split|feature)"' themes/minimal/layouts/_default/_markup/render-image.html
    ```
    PASS: returns 4 (Phase 9 ships all four arms — feature included per locked branch decision).
    FAIL: returns ≤ 3 (some arm missing — investigate via 09-01-SUMMARY).

    **GATE 3 (REQ ABOUT-03 — `--radius-soft` consumer count):**
    ```bash
    grep -c 'var(--radius-soft)' themes/minimal/static/css/style.css
    ```
    PASS: returns ≥ 5. Phase 9 expected actual: 5 to 7 depending on selector grouping (about-hero img/about-hero-img, about-grid img/about-grid-item, about-role-card, about-feature, about-feature-img — 5 minimum if comma-grouped selectors counted as 1, up to 7 if each selector counted independently).

    **GATE 4a (REQ ABOUT-04 — role-card visual contract present):**
    ```bash
    grep -A 5 'body.page-about .about-role-card {' themes/minimal/static/css/style.css | grep -cE 'background: var\(--bg-secondary\)|border: 1px solid var\(--border\)|border-radius: var\(--radius-soft\)'
    ```
    PASS: returns 3 (all three required properties present in `.about-role-card` rule body).

    **GATE 4b (Pitfall 16 — role-card forbidden treatments absent):**
    ```bash
    grep -A 5 'body.page-about .about-role-card' themes/minimal/static/css/style.css | grep -cE 'box-shadow|linear-gradient|transform:|transition:'
    ```
    PASS: returns 0.

    **GATE 6c (REQ ABOUT-06, Pitfall 15 — pullquote typography invariant):**
    ```bash
    grep -A 4 'body.page-about .about-pullquote {' themes/minimal/static/css/style.css | grep -cE 'font-size: 1.4rem|font-weight: 500'
    ```
    PASS: returns 2.
    ```bash
    grep -A 2 'body.page-about .about-pullquote strong' themes/minimal/static/css/style.css | grep -c 'font-weight: 700'
    ```
    PASS: returns 1.

    **GATE 6d (Pitfall 15 — load-bearing contrast comment preserved verbatim):**
    ```bash
    grep -c 'Dark-mode #D14D41 on #1C1B1A measures 3.97:1' themes/minimal/static/css/style.css
    ```
    PASS: returns 1.

    **GATE 6a — pullquote rendered HTML byte-identical (modulo whitespace):**

    Build the site; inspect rendered pullquote against the pre-Plan-09-02 baseline. The rendered `<aside class="about-pullquote">…</aside>` block (with `<strong>40% → 95%</strong>` inside) MUST appear in `public/about/index.html` after Plan 2's `roles[0].pullquote` migration. Whitespace deltas are acceptable; tag/attribute changes are not.
    ```bash
    hugo --minify
    grep -A 1 '<aside class="about-pullquote">' public/about/index.html | grep -c '<strong>40% → 95%</strong>'
    ```
    PASS: returns 1.

    **GATE 8 (Pitfall 3 — hard-coded color audit, inverted form):**

    Per RESEARCH `[ASSUMED]`: invert the gate so it works regardless of `:root` block line numbers. Use `awk` to mask out content INSIDE the two `:root` blocks; grep for hex literals OUTSIDE those blocks in About-scoped rules.
    ```bash
    awk 'BEGIN {in_root=0} /^:root/ {in_root=1} in_root && /^\}/ {in_root=0; next} !in_root {print}' themes/minimal/static/css/style.css | grep -E '#[0-9A-Fa-f]{3,8}' | grep -E 'about-|page-about'
    ```
    PASS: returns empty (zero hex literals outside `:root` blocks in About-related rules).

    **GATE 9 (Pitfall 17 — generic class-name leak):**
    ```bash
    grep -nE '^\.(card|section|feature|role|row|split)\b' themes/minimal/static/css/style.css
    ```
    PASS: returns empty (zero matches — no top-level generic class names introduced).

    Additional Pitfall 17 check (top-level `.about-` selectors must NOT exist outside `body.page-about` prefix):
    ```bash
    grep -nE '^\.about-' themes/minimal/static/css/style.css
    ```
    PASS: returns empty.

    **GATE 10 (Pitfall 21 — no JS on About surface):**
    ```bash
    find content/about themes/minimal/layouts/about themes/minimal/layouts/shortcodes -name '*.js' 2>/dev/null
    ```
    PASS: returns empty.

    **GATE 11 (build determinism — twice-built diff):**
    ```bash
    rm -rf public public.first
    hugo --minify
    mv public public.first
    hugo --minify
    diff -r public.first/about/ public/about/
    EXIT=$?
    rm -rf public public.first
    [ "$EXIT" = "0" ]
    ```
    PASS: `diff` returns no output AND exits 0.

    **Bonus check — render-image classes appear in compiled output (UI-SPEC Gate 11 / `/about/index.html` smoke):**
    ```bash
    hugo --minify
    grep -c 'class="about-hero-img"' public/about/index.html  # expect 1 — hero portrait
    grep -c 'class="about-grid-item"' public/about/index.html  # expect 4 — 4 hobby photos
    grep -c 'class="about-role-card"' public/about/index.html  # expect 3 — 3 role cards
    grep -c 'class="page-about"' public/about/index.html       # expect 1 — body class
    grep -c '<article class="about">' public/about/index.html  # expect 1 — outer wrapper from new layout
    ```

    **Recording protocol:** for each gate, record:
    - The command run (verbatim).
    - The actual output (count, line numbers, or empty).
    - PASS / FAIL.
    - For FAIL: which 09-01 or 09-02 task likely caused the regression (cross-reference 09-01-SUMMARY / 09-02-SUMMARY).

    If `hugo` is not available on PATH (per CLAUDE.md "Hugo is NOT installed locally"), fall back: skip Gates 6a, 11, and the bonus rendered-output checks; mark them as `BLOCKED: hugo CLI unavailable, deferred to post-deploy CI verification`. CI runs Hugo 0.157.0 in the GitHub Actions workflow at `.github/workflows/hugo.yml` (verified per CLAUDE.md), so the build correctness is enforced at deploy. Static greps (Gates 2, 3, 4a, 4b, 6c, 6d, 8, 9, 10) all run regardless.
  </action>
  <verify>
    <automated>[ "$(grep -cE 'eq \$title "(hero|grid|split|feature)"' themes/minimal/layouts/_default/_markup/render-image.html)" = "4" ] && [ "$(grep -c 'var(--radius-soft)' themes/minimal/static/css/style.css)" -ge 5 ] && [ "$(grep -A 5 'body.page-about .about-role-card {' themes/minimal/static/css/style.css | grep -cE 'background: var\(--bg-secondary\)|border: 1px solid var\(--border\)|border-radius: var\(--radius-soft\)')" = "3" ] && [ "$(grep -A 5 'body.page-about .about-role-card' themes/minimal/static/css/style.css | grep -cE 'box-shadow|linear-gradient|transform:|transition:')" = "0" ] && [ "$(grep -A 4 'body.page-about .about-pullquote {' themes/minimal/static/css/style.css | grep -cE 'font-size: 1.4rem|font-weight: 500')" = "2" ] && [ "$(grep -A 2 'body.page-about .about-pullquote strong' themes/minimal/static/css/style.css | grep -c 'font-weight: 700')" = "1" ] && [ "$(grep -c 'Dark-mode #D14D41 on #1C1B1A measures 3.97:1' themes/minimal/static/css/style.css)" = "1" ] && [ "$(awk 'BEGIN {in_root=0} /^:root/ {in_root=1} in_root && /^\}/ {in_root=0; next} !in_root {print}' themes/minimal/static/css/style.css | grep -E '#[0-9A-Fa-f]{3,8}' | grep -cE 'about-|page-about')" = "0" ] && [ "$(grep -nE '^\.(card|section|feature|role|row|split)\b' themes/minimal/static/css/style.css | wc -l | tr -d ' ')" = "0" ] && [ "$(grep -nE '^\.about-' themes/minimal/static/css/style.css | wc -l | tr -d ' ')" = "0" ] && [ "$(find content/about themes/minimal/layouts/about themes/minimal/layouts/shortcodes -name '*.js' 2>/dev/null | wc -l | tr -d ' ')" = "0" ]</automated>
  </verify>
  <acceptance_criteria>
    All automated gates that can run locally (greps + finds) PASS:
    - Gate 2: `grep -cE 'eq \$title "(hero|grid|split|feature)"' themes/minimal/layouts/_default/_markup/render-image.html` returns 4.
    - Gate 3: `grep -c 'var(--radius-soft)' themes/minimal/static/css/style.css` returns ≥ 5.
    - Gate 4a: `grep -A 5 'body.page-about .about-role-card {' themes/minimal/static/css/style.css | grep -cE 'background: var\(--bg-secondary\)|border: 1px solid var\(--border\)|border-radius: var\(--radius-soft\)'` returns 3.
    - Gate 4b: `grep -A 5 'body.page-about .about-role-card' themes/minimal/static/css/style.css | grep -cE 'box-shadow|linear-gradient|transform:|transition:'` returns 0.
    - Gate 6c (rule body): `grep -A 4 'body.page-about .about-pullquote {' themes/minimal/static/css/style.css | grep -cE 'font-size: 1.4rem|font-weight: 500'` returns 2.
    - Gate 6c (strong): `grep -A 2 'body.page-about .about-pullquote strong' themes/minimal/static/css/style.css | grep -c 'font-weight: 700'` returns 1.
    - Gate 6d: `grep -c 'Dark-mode #D14D41 on #1C1B1A measures 3.97:1' themes/minimal/static/css/style.css` returns 1.
    - Gate 8: hard-coded color audit (inverted form) returns 0 matches.
    - Gate 9 (generic names): `grep -nE '^\.(card|section|feature|role|row|split)\b' themes/minimal/static/css/style.css` returns 0.
    - Gate 9 (top-level about): `grep -nE '^\.about-' themes/minimal/static/css/style.css` returns 0.
    - Gate 10: `find content/about themes/minimal/layouts/about themes/minimal/layouts/shortcodes -name '*.js' 2>/dev/null` returns empty.

    Build-dependent gates (Gates 6a, 11) and bonus rendered-output checks: PASS if Hugo available; otherwise BLOCKED with explicit deferral to CI/post-deploy.

    All results recorded in 09-03-SUMMARY.md (created by the standard execute-plan summary flow at the end of the plan).

    <automated>[ "$(grep -cE 'eq \$title "(hero|grid|split|feature)"' themes/minimal/layouts/_default/_markup/render-image.html)" = "4" ] && [ "$(grep -c 'var(--radius-soft)' themes/minimal/static/css/style.css)" -ge 5 ] && [ "$(grep -A 5 'body.page-about .about-role-card {' themes/minimal/static/css/style.css | grep -cE 'background: var\(--bg-secondary\)|border: 1px solid var\(--border\)|border-radius: var\(--radius-soft\)')" = "3" ] && [ "$(grep -A 5 'body.page-about .about-role-card' themes/minimal/static/css/style.css | grep -cE 'box-shadow|linear-gradient|transform:|transition:')" = "0" ] && [ "$(grep -A 4 'body.page-about .about-pullquote {' themes/minimal/static/css/style.css | grep -cE 'font-size: 1.4rem|font-weight: 500')" = "2" ] && [ "$(grep -A 2 'body.page-about .about-pullquote strong' themes/minimal/static/css/style.css | grep -c 'font-weight: 700')" = "1" ] && [ "$(grep -c 'Dark-mode #D14D41 on #1C1B1A measures 3.97:1' themes/minimal/static/css/style.css)" = "1" ] && [ "$(awk 'BEGIN {in_root=0} /^:root/ {in_root=1} in_root && /^\}/ {in_root=0; next} !in_root {print}' themes/minimal/static/css/style.css | grep -E '#[0-9A-Fa-f]{3,8}' | grep -cE 'about-|page-about')" = "0" ] && [ "$(grep -nE '^\.(card|section|feature|role|row|split)\b' themes/minimal/static/css/style.css | wc -l | tr -d ' ')" = "0" ] && [ "$(grep -nE '^\.about-' themes/minimal/static/css/style.css | wc -l | tr -d ' ')" = "0" ] && [ "$(find content/about themes/minimal/layouts/about themes/minimal/layouts/shortcodes -name '*.js' 2>/dev/null | wc -l | tr -d ' ')" = "0" ]</automated>
  </acceptance_criteria>
  <done>
    All 10 statically-checkable gates PASS. Build-dependent gates (6a, 11, bonus rendered-output) PASS if Hugo available, BLOCKED with explicit deferral if not. All results recorded with verbatim commands + outputs in the plan summary so the human-judgment plan (Task 2) can reference them.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Author 09-HUMAN-UAT.md scaffolding for 5 visual-judgment gates and update STATE.md Deferred Items</name>
  <files>
    .planning/phases/09-about-dynamic-rounded-redesign/09-HUMAN-UAT.md,
    .planning/STATE.md
  </files>
  <read_first>
    - .planning/STATE.md § Deferred Items (lines ~96-107 — table format reference; row pattern for Phase 5/05.1/06/07)
    - .planning/phases/09-about-dynamic-rounded-redesign/09-UI-SPEC.md § "Verification Gates" Gates 1, 4 (part c), 5, 6 (part b), 7 (part a)
    - .planning/ROADMAP.md § "Phase 9: ABOUT — Dynamic Rounded Redesign" Success Criteria (6 deployed-site verifications)
    - .planning/phases/07-*-SUMMARY.md or .planning/phases/06-*/06-HUMAN-UAT.md if either exists (project precedent for HUMAN-UAT scaffolding)
  </read_first>
  <action>
    Two operations:

    **Operation A — Create `.planning/phases/09-about-dynamic-rounded-redesign/09-HUMAN-UAT.md`** with the contents below. This is the post-deploy browser walkthrough checklist; it queues 5 gates that require human visual judgment against the deployed `https://tbohnstedt.cloud/about/`.

    File contents (write verbatim):

    ```markdown
    # Phase 9 — HUMAN-UAT Checklist (post-deploy)

    **Phase:** 09-about-dynamic-rounded-redesign
    **Status:** queued for post-deploy browser walkthrough
    **Trigger:** push commits → GitHub Actions deploy → walk this checklist in a browser at https://tbohnstedt.cloud/about/
    **Project precedent:** Phase 5, 05.1, 06, 07 all deferred their HUMAN-UAT to a post-deploy walkthrough — see STATE.md § Deferred Items.

    These 5 gates are the visual-judgment items deferred from Plan 09-03. Plan 09-03 ran every automatable grep / find / build gate against the code on disk; these remaining gates require eyes on the deployed page (or a deployed-equivalent local preview at the level of fidelity that DevTools provides).

    ---

    ## Gate 1 — Asymmetric layout from new template (REQ ABOUT-01)

    **What to verify:** the `/about/` page is rendered by `themes/minimal/layouts/about/single.html`, not `_default/single.html`, and presents an asymmetric layout (alternating ratios per section, hero 2-col 2fr/1fr, role cards stacked, Outside Work tail section with 4-photo grid).

    **Steps:**
    1. Visit https://tbohnstedt.cloud/about/ in light mode.
    2. Open DevTools → Elements panel.
    3. Confirm `<body class="page-about">` is present at the root.
    4. Confirm `<article class="about">` is present (not `<div class="page-content">`) — this is the new layout's outer wrapper.
    5. Confirm the hero block: `<section class="about-hero">` with two children `<div class="about-hero-text">` (containing the greeting + intro paragraphs + CV link) and `<div class="about-hero-photo">` (containing the portrait `<img class="about-hero-img">`).
    6. Confirm the Experience block: `<section class="about-section" data-section="experience">` containing `<ol class="about-role-list">` with 3 `<li class="about-role-card">` children.
    7. Confirm the page does NOT have `<div class="page-header">` chrome (intentional divergence from gallery analog).

    **PASS criteria:** all 7 checks above. **FAIL action:** open a follow-up plan to investigate the layout-routing chain (likely cause: `type: "about"` in frontmatter not matching `layouts/about/single.html` discovery — verify file location).

    ---

    ## Gate 4c — No CSS leak to /blog/ or /gallery/ (REQ ABOUT-04, Pitfall 17)

    **What to verify:** Phase 9's About-scoped CSS rules do not affect non-About pages.

    **Steps:**
    1. Visit https://tbohnstedt.cloud/blog/ — confirm posts list renders identically to pre-Phase-9 deploy (same typography, no rounded-card surfaces, no `var(--bg-secondary)` tinting on post items).
    2. Visit any individual blog post (e.g. https://tbohnstedt.cloud/blog/2026-03-27-video-editing-journey/) — confirm prose renders identically, no role-card surfaces, no asymmetric grid.
    3. Visit https://tbohnstedt.cloud/gallery/ — confirm masonry-grid + lightbox-stub layout renders identically (Phase 10 will redesign this; Phase 9 must NOT have already perturbed it).
    4. Open DevTools → Elements on each of the three URLs above. Confirm `<body class="page-blog">`, `<body class="page-default">` (post page), and `<body class="page-gallery">` respectively. None should have `page-about`.

    **PASS criteria:** all 4 pages render identically to pre-deploy baseline; no rounded role-card or asymmetric grid surfaces appear outside `/about/`. **FAIL action:** likely cause is a Phase 9 CSS rule missing the `body.page-about` prefix; re-run Plan 09-03 Gate 9 grep against post-deploy `style.css`.

    ---

    ## Gate 5 — Content rebalance: professional ≥ personal (REQ ABOUT-05)

    **What to verify:** the rebalanced `/about/` reads professional-first, with climbing/cycling/running/cooking demoted to a single tail "Outside Work" section.

    **Steps:**
    1. Visit https://tbohnstedt.cloud/about/.
    2. Read the page top-to-bottom from the hero greeting through Outside Work.
    3. Visual-block count: count the rendered "blocks" each role/topic occupies.
       - Hero (1 prose block + 1 portrait): professional content (intro mentions Erste, Accenture, Siemens).
       - Experience section: 3 role cards + 1 pullquote. Pullquote stat is professional.
       - Education section: bullet list (3 items). Professional.
       - Certifications section: bullet list (3 items). Professional.
       - Outside Work section: 1 prose paragraph + 1 photo grid (4 photos).
    4. Confirm: professional blocks (hero + 3 cards + pullquote + Education + Certifications = 7 blocks) ≥ personal blocks (Outside Work prose + 4 photos = 1 narrative block + 4 photos).
    5. Confirm the 4 hobby photos appear ONLY in the Outside Work section, NOT in the hero or in any earlier section.
    6. Confirm the page reads first-person and evergreen-toned (no marketing language, no emoji, no decorative SVGs).

    **PASS criteria:** professional blocks > personal blocks; 4 hobby photos appear once in Outside Work; tone is plain narrative.
    **FAIL action:** if hobby photos appear early or duplicate, audit `content/about/index.md` for a stray `"grid"`-titled image outside the Outside Work block.

    ---

    ## Gate 6b — Live DevTools dark-mode contrast inspector (REQ ABOUT-06, Pitfall 15)

    **What to verify:** the dark-mode pullquote `<strong>` text passes WCAG AA-large-bold contrast (≥ 3.0:1; current 3.97:1).

    **Steps:**
    1. Visit https://tbohnstedt.cloud/about/ and switch to dark mode (click the sun/moon toggle, or set OS preference to dark).
    2. Open DevTools → Elements panel.
    3. Locate the `<aside class="about-pullquote">` rendered inside the Experience section (after the Erste Group role card).
    4. Inside that aside, locate the `<strong>40% → 95%</strong>` element.
    5. Open DevTools → Accessibility panel (or hover the contrast badge in Inspect mode).
    6. Read the contrast ratio reported.

    **PASS criteria:** ratio ≥ 3.0:1 (large-bold AA path). Current measured 3.97:1 — preserved.
    **FAIL action:** if ratio < 3.0:1, audit `style.css` `.about-pullquote strong` rule for any change in `color`, `font-size`, or `font-weight` since pre-Phase-9. The rule MUST be preserved verbatim per Pitfall 15.

    ---

    ## Gate 7a — DevTools mobile emulation at 320 px (REQ ABOUT-07)

    **What to verify:** at viewport widths < 600 px, every multi-column About section collapses to single column with no horizontal overflow, no broken image crops, and source-order = reading order.

    **Steps:**
    1. Visit https://tbohnstedt.cloud/about/.
    2. Open DevTools → Toggle device toolbar (Cmd+Shift+M / Ctrl+Shift+M).
    3. Set viewport to **320 px wide** (use the device dropdown or enter custom dimensions).
    4. Scroll the page top-to-bottom.
    5. Confirm:
       - No horizontal scroll bar appears (check by sliding the page horizontally — should not move).
       - Hero collapses: portrait stacks below the text (or above; layout source order determines which).
       - Each role card spans the full content column (no horizontal split).
       - Outside Work 4-photo grid collapses from 2×2 to 1×4 (single column of 4 photos stacked vertically).
       - All images render at full column width with their original aspect ratios preserved (no squashed/squished/clipped images).
       - The pullquote stat remains readable and properly indented (asymmetric one-sided radius preserved).
    6. Resize to 600 px → confirm at exactly 600 px the layout flips to mobile rules (test the breakpoint boundary).
    7. Resize to 900 px → confirm desktop rules return (hero 2-col, grid 2x2).

    **PASS criteria:** all 7 sub-checks above. **FAIL action:** likely cause is a mobile rule landed outside the existing `@media (max-width: 600px)` block (RESEARCH "Risk Hotspots" row 6); re-audit Plan 09-02 Task 3 Operation B.

    ---

    ## Resolution

    - When all 5 gates above PASS in a real browser walkthrough, mark Phase 9 fully shipped: update STATE.md → Deferred Items → row for `09-HUMAN-UAT.md` → Status: `complete`.
    - Any FAIL triggers a follow-up plan referencing the failing gate and the specific page/element that broke.
    ```

    **Operation B — Update `.planning/STATE.md` Deferred Items table.** Locate the table at lines ~96-107 (between `## Deferred Items` heading and the trigger paragraph). Add ONE row at the bottom of the table (after Phase 7 row, before the closing `All items gated on the same trigger…` paragraph):

    Find the existing table:
    ```
    | uat | 07 | 07-HUMAN-UAT.md | unknown | Post-deploy browser walkthrough at https://tbohnstedt.cloud/about/ |
    | verification | 05 | 05-VERIFICATION.md | human_needed | Confirm favicon in tab/bookmark/iOS home-screen after deploy |
    | verification | 05.1 | 05.1-VERIFICATION.md | human_needed | Confirm SVG favicon prefers-color-scheme swap in real browsers |
    ```

    Insert a new `uat` row between the existing `uat | 07 | …` row and the first `verification | 05 | …` row:
    ```
    | uat | 09 | 09-HUMAN-UAT.md (5 gates) | partial | Post-deploy browser walkthrough at https://tbohnstedt.cloud/about/ |
    ```

    Do NOT touch:
    - The trigger paragraph below the table.
    - Any other STATE.md section (Performance Metrics, Accumulated Context, Session Continuity).
    - The Phase 5 / 05.1 / 06 / 07 rows (those track existing deferrals — not Phase 9's).
  </action>
  <verify>
    <automated>test -f .planning/phases/09-about-dynamic-rounded-redesign/09-HUMAN-UAT.md && [ "$(wc -l < .planning/phases/09-about-dynamic-rounded-redesign/09-HUMAN-UAT.md | tr -d ' ')" -ge 60 ] && [ "$(grep -cE '^## Gate ' .planning/phases/09-about-dynamic-rounded-redesign/09-HUMAN-UAT.md)" = "5" ] && [ "$(grep -c 'PASS criteria:' .planning/phases/09-about-dynamic-rounded-redesign/09-HUMAN-UAT.md)" = "5" ] && [ "$(grep -c 'FAIL action:' .planning/phases/09-about-dynamic-rounded-redesign/09-HUMAN-UAT.md)" = "5" ] && grep -c '09-HUMAN-UAT.md' .planning/STATE.md | grep -q '^1$' && [ "$(grep -cE '\| uat \| 09 \| 09-HUMAN-UAT.md.*\| partial' .planning/STATE.md)" = "1" ] && grep -c 'All items gated on the same trigger' .planning/STATE.md | grep -q '^1$'</automated>
  </verify>
  <acceptance_criteria>
    **Operation A — 09-HUMAN-UAT.md:**
    - `test -f .planning/phases/09-about-dynamic-rounded-redesign/09-HUMAN-UAT.md` (file exists).
    - `wc -l .planning/phases/09-about-dynamic-rounded-redesign/09-HUMAN-UAT.md` returns ≥ 60.
    - File contains all 5 gate sections: `grep -c '^## Gate 1' .planning/phases/09-about-dynamic-rounded-redesign/09-HUMAN-UAT.md` returns 1; same for Gate 4c, Gate 5, Gate 6b, Gate 7a (5 sections total).
    - `grep -c 'tbohnstedt.cloud/about' .planning/phases/09-about-dynamic-rounded-redesign/09-HUMAN-UAT.md` returns ≥ 3 (multiple gate sections reference the deployed URL).
    - `grep -c 'PASS criteria:' .planning/phases/09-about-dynamic-rounded-redesign/09-HUMAN-UAT.md` returns 5 (one per gate).
    - `grep -c 'FAIL action:' .planning/phases/09-about-dynamic-rounded-redesign/09-HUMAN-UAT.md` returns 5 (one per gate).

    **Operation B — STATE.md Deferred Items update:**
    - `grep -c '09-HUMAN-UAT.md' .planning/STATE.md` returns 1.
    - The new row contains all required columns: `grep -E '\| uat \| 09 \| 09-HUMAN-UAT.md.*\| partial \| Post-deploy browser walkthrough' .planning/STATE.md` returns 1 match.
    - Existing Phase 5/05.1/06/07 deferred-items rows preserved: `grep -c '05-HUMAN-UAT.md\|05.1-HUMAN-UAT.md\|06-HUMAN-UAT.md\|07-HUMAN-UAT.md' .planning/STATE.md` returns 4.
    - The trigger paragraph below the table is unchanged: `grep -c 'All items gated on the same trigger' .planning/STATE.md` returns 1.

    <automated>test -f .planning/phases/09-about-dynamic-rounded-redesign/09-HUMAN-UAT.md && [ "$(wc -l < .planning/phases/09-about-dynamic-rounded-redesign/09-HUMAN-UAT.md | tr -d ' ')" -ge 60 ] && [ "$(grep -cE '^## Gate ' .planning/phases/09-about-dynamic-rounded-redesign/09-HUMAN-UAT.md)" = "5" ] && [ "$(grep -c 'PASS criteria:' .planning/phases/09-about-dynamic-rounded-redesign/09-HUMAN-UAT.md)" = "5" ] && [ "$(grep -c 'FAIL action:' .planning/phases/09-about-dynamic-rounded-redesign/09-HUMAN-UAT.md)" = "5" ] && grep -c '09-HUMAN-UAT.md' .planning/STATE.md | grep -q '^1$' && [ "$(grep -cE '\| uat \| 09 \| 09-HUMAN-UAT.md.*\| partial' .planning/STATE.md)" = "1" ] && grep -c 'All items gated on the same trigger' .planning/STATE.md | grep -q '^1$'</automated>
  </acceptance_criteria>
  <done>
    `.planning/phases/09-about-dynamic-rounded-redesign/09-HUMAN-UAT.md` exists and lists all 5 visual-judgment gates with concrete browser walkthrough steps, PASS criteria, and FAIL actions. `.planning/STATE.md` § Deferred Items has a new row tracking 09-HUMAN-UAT.md with `partial` status and post-deploy resolution path matching the Phase 5/05.1/06/07 precedent. All existing rows in STATE.md preserved.
  </done>
</task>

</tasks>

<verification>
**Plan 09-03 phase-close gates** (run after both tasks land):

1. **All automated UI-SPEC gates PASS** (Task 1 acceptance criteria — Gates 2, 3, 4a, 4b, 6c, 6d, 8, 9, 10 statically; Gates 6a, 11 build-dependent).
2. **HUMAN-UAT scaffolding complete** (Task 2 — file exists, 5 gates, STATE.md updated).
3. **All 7 ABOUT requirement IDs accounted for across the 3 plans:**
   - ABOUT-01: Plan 09-02 (layout file + asymmetric structure) + Plan 09-03 Gate 1 HUMAN-UAT.
   - ABOUT-02: Plan 09-01 (hook arms) + Plan 09-03 Gate 2 automated.
   - ABOUT-03: Plan 09-01 (token declared) + Plan 09-02 (token consumed in ≥ 5 selectors) + Plan 09-03 Gate 3 automated.
   - ABOUT-04: Plan 09-02 (role-card visual contract) + Plan 09-03 Gates 4a/4b automated, 4c HUMAN-UAT.
   - ABOUT-05: Plan 09-02 (content rewrite — Outside Work consolidation) + Plan 09-03 Gate 5 HUMAN-UAT.
   - ABOUT-06: Plan 09-02 (pullquote rule preserved verbatim) + Plan 09-03 Gates 6a/6c/6d automated, 6b HUMAN-UAT.
   - ABOUT-07: Plan 09-02 (mobile @media block extended) + Plan 09-03 Gate 7b automated, 7a HUMAN-UAT.

4. **Phase-close handoff:**
   - 09-03-SUMMARY.md created (by execute-plan flow) with full gate-result table.
   - STATE.md updated with Phase 9 verification status.
   - ROADMAP.md Phase 9 row stays at `Plans: 3 plans` and the 3 plan checkboxes are marked complete (handled by execute-plan flow on plan-by-plan basis, not in this plan's tasks).
   - Phase 9 ships as "verifying" pending HUMAN-UAT walkthrough — same shipped state as Phase 5/05.1/06/07 in the v2.0 close.
</verification>

<success_criteria>
- All 10 statically-checkable verification gates PASS (Gates 2, 3, 4a, 4b, 6c, 6d, 8, 9, 10, plus the redundant Pitfall 17 top-level `.about-` check).
- Build-dependent gates (6a, 11, plus bonus rendered-output checks) PASS if Hugo CLI available; if not, BLOCKED with explicit deferral to GitHub Actions CI (which runs Hugo Extended 0.157.0 per .github/workflows/hugo.yml).
- 09-HUMAN-UAT.md exists with 5 gate sections, each with steps + PASS/FAIL criteria.
- STATE.md Deferred Items table has a new row for 09-HUMAN-UAT.md matching the Phase 5/05.1/06/07 precedent format.
- Phase 9 enters the same "shipped pending post-deploy walkthrough" state as the v2.0-close phases.
</success_criteria>

<output>
After completion, create `.planning/phases/09-about-dynamic-rounded-redesign/09-03-SUMMARY.md` recording:
- Verbatim command + output for every automated gate run in Task 1 (a results table mapping Gate ID → command → output → PASS/FAIL/BLOCKED).
- Build-dependent gates: PASS / BLOCKED with reason (Hugo unavailable → CI deferral).
- Confirmation that 09-HUMAN-UAT.md exists with 5 sections.
- Confirmation that STATE.md Deferred Items has the new Phase 9 row.
- Phase-close summary: "Phase 9 — ABOUT — Dynamic Rounded Redesign — code complete, automated gates green, HUMAN-UAT queued for post-deploy. Total plan count: 3 (09-01 Foundation, 09-02 Application, 09-03 Verification)."
- Note for the next milestone close (post-Phase-10 ship of v3.0): walk all 4-5 deferred HUMAN-UATs (Phases 5, 05.1, 06, 07, 09) in a single browser session.
</output>
