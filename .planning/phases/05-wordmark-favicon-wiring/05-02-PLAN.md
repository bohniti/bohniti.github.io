---
phase: 05-wordmark-favicon-wiring
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - themes/minimal/layouts/partials/header.html
  - themes/minimal/static/css/style.css
autonomous: true
requirements:
  - HEAD-01
  - HEAD-02
  - HEAD-03
tags:
  - hugo
  - css
  - wordmark
  - theming
  - flexoki
  - cls
user_setup: []

must_haves:
  truths:
    - "Site header shows the script wordmark image in place of the plain text site title on every page (home, blog, single post, about)"
    - "Both wordmark <img> tags exist inside the existing <a href=\"{{ .Site.BaseURL }}\"> anchor — wordmark is still a clickable home link"
    - "When html[data-theme=\"light\"] is set, only the .wordmark-light <img> is visible; .wordmark-dark has display: none"
    - "When html[data-theme=\"dark\"] is set, only the .wordmark-dark <img> is visible; .wordmark-light has display: none"
    - "Hidden variant is removed from the accessibility tree (display:none) so screen readers announce alt='Timo Bohnstedt' exactly once"
    - "Both <img> tags carry alt=\"Timo Bohnstedt\" and explicit width=\"200\" height=\"117\" (CLS prevention — HEAD-03)"
    - "Per-variant background color is applied directly to each <img>: .wordmark-light has background #FEFEFE, .wordmark-dark has background #000000 (Phase 4 D-02 hard cross-phase contract — masks the seam against Flexoki --bg)"
    - "Under @media (max-width: 600px), .wordmark renders at 160x93 (mobile size override per D-03)"
    - "Theme toggle (Phase 4) swaps the wordmark variants instantly via the CSS [data-theme] selector with no FOUC and no JS swap"
  artifacts:
    - path: "themes/minimal/layouts/partials/header.html"
      provides: "Two <img class=\"wordmark wordmark-{light,dark}\"> tags inside the existing <a href=\"{{ .Site.BaseURL }}\"> anchor; replaces the plain {{ .Site.Title }} text"
      contains: "wordmark wordmark-light"
    - path: "themes/minimal/static/css/style.css"
      provides: ".wordmark base rule (block + 200x117), html[data-theme=\"light\"] / html[data-theme=\"dark\"] swap rules, .wordmark-light / .wordmark-dark seam-masking bg rules, mobile 160x93 override inside the existing @media (max-width: 600px) block"
      contains: "html[data-theme=\"light\"] .wordmark-dark"
  key_links:
    - from: "themes/minimal/layouts/partials/header.html"
      to: "themes/minimal/static/images/brand/logo-light.png + logo-dark.png"
      via: "Two <img src=\"{{ \"images/brand/logo-{light,dark}.png\" | absURL }}\"> tags inside the existing <a> anchor"
      pattern: "logo-light\\.png|logo-dark\\.png"
    - from: "html[data-theme=\"light\"|\"dark\"] (set on <html> by Phase 4 head IIFE)"
      to: ".wordmark-light / .wordmark-dark display: none toggle (D-01)"
      via: "CSS attribute selector — no JS, no FOUC"
      pattern: "html\\[data-theme=\"(light|dark)\"\\]\\s+\\.wordmark-(dark|light)"
    - from: ".wordmark-light / .wordmark-dark"
      to: "Phase 4 D-02 corner-pixel hex contract (#FEFEFE / #000000)"
      via: "Hardcoded background rules on each .wordmark-{variant} class — NOT theme tokens"
      pattern: "\\.wordmark-light\\s*\\{\\s*background:\\s*#FEFEFE"

threat_model:
  trust_boundaries:
    - "Static template + stylesheet only; no user input, no network IO at runtime."
  threats:
    - id: T-05-03
      category: Cross-Site-Scripting
      component: "<img alt=\"Timo Bohnstedt\"> in partials/header.html"
      disposition: accept
      rationale: "alt text is a hardcoded literal in the template — no Hugo data binding, no user input. No XSS surface."
  asvs_l1_blocking_issues: []
---

<objective>
Wire the script "time BOHNSTEDT" wordmark into the site header. Replace the plain `{{ .Site.Title }}` text in `themes/minimal/layouts/partials/header.html` with two `<img>` tags (light + dark variants) sitting inside the existing `<a href="{{ .Site.BaseURL }}">` anchor; add the `.wordmark` family of CSS rules to `themes/minimal/static/css/style.css` (base sizing, `[data-theme]`-keyed display swap, per-variant seam-masking background, mobile size override).

Purpose: Phase 5 success criteria 1 + 2 (HEAD-01, HEAD-02, HEAD-03). The wordmark replaces text on every page (it lives in the partial included by `baseof.html`); the swap is pure CSS keyed off Phase 4's `html[data-theme]` attribute (no FOUC, no JS swap); the hardcoded per-variant background colors honor Phase 4 D-02's hard cross-phase contract by matching the PNG corner pixels (`#FEFEFE` for `-light`, `#000000` for `-dark`) — masking the rectangular seam against Flexoki `--bg`. Explicit `width="200" height="117"` HTML attrs + matching CSS dims pre-reserve layout, structurally guaranteeing Lighthouse Mobile CLS < 0.1 on `/`.

Output: Modified `partials/header.html` (line 3 swap — two `<img>` tags replace `{{ .Site.Title }}`); modified `style.css` (additive — `.wordmark` family of rules added inside the existing `/* === Header === */` block + mobile override added inside the existing `@media (max-width: 600px)` block).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-wordmark-favicon-wiring/05-CONTEXT.md
@.planning/phases/05-wordmark-favicon-wiring/05-PATTERNS.md
@.planning/phases/03-brand-asset-slicing/03-CONTEXT.md
@.planning/phases/04-theming-foundation/04-CONTEXT.md
@themes/minimal/layouts/partials/header.html
@themes/minimal/static/css/style.css

<interfaces>
<!-- Phase 3 outputs this plan consumes (8 sliced PNGs at the canonical location) -->
themes/minimal/static/images/brand/logo-light.png   (~26 KB, ≤ 30 KB BRAND-03 gate)
themes/minimal/static/images/brand/logo-dark.png    (~21 KB, ≤ 30 KB BRAND-03 gate)
Aspect ratio: 800×467 source → 1.71:1 → 200×117 (desktop) / 160×93 (mobile) preserves ratio.

<!-- Phase 4 cross-phase contracts this plan honors -->
- [data-theme] is set on <html> by the head IIFE (baseof.html lines 12-21) BEFORE the stylesheet parses → no FOUC, no JS swap.
- Per-variant <img> bg MUST be #FEFEFE for -light PNG and #000000 for -dark PNG (Phase 4 D-02 — corner-pixel hex from Phase 3, NOT Flexoki --bg/--bg-dark tokens which are #FFFCF0/#100F0F).

<!-- Existing files this plan modifies (verified line numbers from 05-PATTERNS.md) -->

themes/minimal/layouts/partials/header.html — current full file (11 lines):
```html
<header class="site-header">
  <div class="site-title">
    <a href="{{ .Site.BaseURL }}">{{ .Site.Title }}</a>
  </div>
  <nav class="site-nav">
    {{ range .Site.Menus.main }}
      <a href="{{ .URL }}"{{ if $.IsMenuCurrent "main" . }} class="active"{{ end }}>{{ .Name }}</a>
    {{ end }}
    <button type="button" class="theme-toggle" aria-pressed="false">Dark</button>
  </nav>
</header>
```
Only line 3 changes — surrounding structure (.site-header, .site-title, <a>, <nav>, theme-toggle) stays.

themes/minimal/static/css/style.css — relevant existing context:
- /* === Header === */ block: lines 65-122
- .site-title a typography: lines 74-83 (KEEP — link hover behavior preserved)
- .theme-toggle rules: lines 101-122
- /* === Post List (Home) === */: line 124 (next section after Header)
- @media (max-width: 600px) block: lines 306-311

<!-- Hugo idioms (from baseof.html line 23) -->
{{ "<path>" | absURL }}  — produces fully-qualified URL in production, root-relative in dev
{{ partial "..." . }}    — already used for header/footer; precedent for partial inclusion

<!-- Wordmark sizing decisions (D-03) -->
Desktop (≥600px viewport): width="200" height="117"
Mobile (max-width: 600px): width 160px, height 93px (CSS override)
Aspect 200/117 ≈ 1.7094, 160/93 ≈ 1.7204 — both within 0.011 of source 800/467 ≈ 1.7131; no visible distortion.

<!-- Per Claude's discretion (CONTEXT line 88-95) -->
- Class names .wordmark / .wordmark-light / .wordmark-dark are the chosen names (match existing .site-* family closely enough; CONTEXT explicitly suggests these).
- Keep the existing .site-title a typography rules at lines 74-83 — they color the anchor (link hover preserved); font-size/font-weight/color become irrelevant for image content but harmless. Adding new .wordmark rules is purely additive.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Replace {{ .Site.Title }} text with two wordmark <img> tags inside the existing anchor in partials/header.html</name>

  <files>themes/minimal/layouts/partials/header.html</files>

  <read_first>
    - themes/minimal/layouts/partials/header.html (current 11-line file — verify line 3 still matches the pattern in 05-PATTERNS.md)
    - .planning/phases/05-wordmark-favicon-wiring/05-CONTEXT.md (D-01 swap technique; D-02 alt text; D-03 dimensions; D-04 anchor wrapper stays)
    - .planning/phases/05-wordmark-favicon-wiring/05-PATTERNS.md (§ "themes/minimal/layouts/partials/header.html" — current full content + pattern to apply, including absURL precedent)
  </read_first>

  <action>
1. Replace the entire contents of line 3 (currently `    <a href="{{ .Site.BaseURL }}">{{ .Site.Title }}</a>`) with a multi-line anchor that contains exactly two `<img>` tags inside it. Final shape of header.html lines 1-12:

   ```html
   <header class="site-header">
     <div class="site-title">
       <a href="{{ .Site.BaseURL }}">
         <img class="wordmark wordmark-light" src="{{ "images/brand/logo-light.png" | absURL }}" alt="Timo Bohnstedt" width="200" height="117">
         <img class="wordmark wordmark-dark"  src="{{ "images/brand/logo-dark.png"  | absURL }}" alt="Timo Bohnstedt" width="200" height="117">
       </a>
     </div>
     <nav class="site-nav">
       {{ range .Site.Menus.main }}
         <a href="{{ .URL }}"{{ if $.IsMenuCurrent "main" . }} class="active"{{ end }}>{{ .Name }}</a>
       {{ end }}
       <button type="button" class="theme-toggle" aria-pressed="false">Dark</button>
     </nav>
   </header>
   ```

   Do NOT change: the `<header class="site-header">` outer wrapper, the `<div class="site-title">` element, the `<a href="{{ .Site.BaseURL }}">` anchor opening (it stays — per D-04 the wordmark remains a clickable home link), the `<nav class="site-nav">` block, the `<button type="button" class="theme-toggle" aria-pressed="false">Dark</button>` Phase 4 toggle, the closing tags.

2. Concrete attribute requirements (do NOT vary):
   - Both `<img>` tags: `alt="Timo Bohnstedt"` literal (D-02). NOT empty alt; NOT a Hugo template binding.
   - Both `<img>` tags: `width="200" height="117"` literals (D-03 desktop dims; CLS prevention HEAD-03).
   - Light variant class list: `class="wordmark wordmark-light"` (two classes, space-separated).
   - Dark variant class list: `class="wordmark wordmark-dark"` (two classes, space-separated).
   - Both `src` attrs use the `absURL` pipe per the project precedent at `baseof.html` line 23: `src="{{ "images/brand/logo-light.png" | absURL }}"` and `src="{{ "images/brand/logo-dark.png" | absURL }}"`.
   - Order: light variant FIRST, dark variant SECOND (alphabetical by suffix; matches the order used throughout 05-CONTEXT.md examples and 05-PATTERNS.md).
   - The `{{ .Site.Title }}` template expression is removed entirely from this partial. (`<title>` in baseof.html still uses `{{ .Site.Title }}` — that's not affected.)

3. Do NOT add `loading="lazy"` to either variant — both are above-the-fold (header). Do NOT add `loading="eager"`; default is fine. Do NOT add `decoding="async"` — out of scope, no measured benefit.

4. Do NOT add a `<picture>` element or any `<source media="(prefers-color-scheme: ...)">` — REQUIREMENTS.md § "Out of Scope" explicitly rejects `<picture>` + `prefers-color-scheme` for the wordmark (silently overrides the manual toggle). Two `<img>` + CSS `display: none` is the locked technique (D-01).
  </action>

  <verify>
    <automated>grep -c '&lt;img class="wordmark wordmark-light"' themes/minimal/layouts/partials/header.html | grep -q '^1$' &amp;&amp; grep -c '&lt;img class="wordmark wordmark-dark"' themes/minimal/layouts/partials/header.html | grep -q '^1$' &amp;&amp; ! grep -q '{{ .Site.Title }}' themes/minimal/layouts/partials/header.html &amp;&amp; grep -q 'href="{{ .Site.BaseURL }}"' themes/minimal/layouts/partials/header.html &amp;&amp; echo OK</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c '<img class="wordmark wordmark-light"' themes/minimal/layouts/partials/header.html` returns `1`
    - `grep -c '<img class="wordmark wordmark-dark"' themes/minimal/layouts/partials/header.html` returns `1`
    - `grep -c 'alt="Timo Bohnstedt"' themes/minimal/layouts/partials/header.html` returns `2`
    - `grep -c 'width="200" height="117"' themes/minimal/layouts/partials/header.html` returns `2`
    - `grep -c '"images/brand/logo-light.png" | absURL' themes/minimal/layouts/partials/header.html` returns `1`
    - `grep -c '"images/brand/logo-dark.png"  | absURL' themes/minimal/layouts/partials/header.html` returns `1`
    - `grep -c '{{ .Site.Title }}' themes/minimal/layouts/partials/header.html` returns `0` (the text title is fully removed from this partial)
    - The existing anchor wrapper survives unchanged: `grep -c 'href="{{ .Site.BaseURL }}"' themes/minimal/layouts/partials/header.html` returns `1`
    - The Phase 4 theme-toggle button is unchanged: `grep -c '<button type="button" class="theme-toggle" aria-pressed="false">Dark</button>' themes/minimal/layouts/partials/header.html` returns `1`
    - `<picture>` is NOT introduced: `grep -c '<picture>' themes/minimal/layouts/partials/header.html` returns `0`
  </acceptance_criteria>

  <done>
    `partials/header.html` now contains two `<img class="wordmark wordmark-{light,dark}">` tags inside the existing `<a href="{{ .Site.BaseURL }}">` anchor; the plain `{{ .Site.Title }}` text is removed from this partial; the surrounding `<header>`, `<div class="site-title">`, `<nav class="site-nav">`, and Phase 4 theme-toggle button are untouched.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add .wordmark CSS rules to style.css — base sizing, [data-theme] swap, per-variant seam-masking bg, mobile size override</name>

  <files>themes/minimal/static/css/style.css</files>

  <read_first>
    - themes/minimal/static/css/style.css (current 312 lines — verify line numbers from 05-PATTERNS.md still accurate)
    - .planning/phases/05-wordmark-favicon-wiring/05-CONTEXT.md (D-01 display:none swap; D-03 sizing; D-05 per-variant bg; D-06 single source)
    - .planning/phases/05-wordmark-favicon-wiring/05-PATTERNS.md (§ "themes/minimal/static/css/style.css" — patterns and verbatim CSS blocks)
    - .planning/phases/04-theming-foundation/04-CONTEXT.md (D-02 — Phase 4 hard cross-phase contract: `#FEFEFE` for `-light` PNG / `#000000` for `-dark` PNG)
  </read_first>

  <action>
1. Insert a new `.wordmark` family CSS block at the END of the existing `/* === Header === */` block, immediately AFTER the existing `:focus-visible` rule that closes at line 122 and BEFORE the `/* === Post List (Home) === */` comment at line 124. Insert verbatim (CSS values are locked by D-01, D-03, D-05; comments document the cross-phase contracts):

   ```css

   /* Wordmark — two <img> tags swapped by [data-theme] (D-01); display:none removes hidden variant from layout flow + a11y tree (D-02). */
   .wordmark {
     display: block;
     width: 200px;
     height: 117px;
   }

   html[data-theme="light"] .wordmark-dark { display: none; }
   html[data-theme="dark"]  .wordmark-light { display: none; }

   /* Per-variant seam-masking bg matching the PNG corner pixel (Phase 4 D-02 hard cross-phase contract).
      Hardcoded — intentionally NOT theme tokens. -light PNG corner = #FEFEFE; -dark PNG corner = #000000. */
   .wordmark-light { background: #FEFEFE; }
   .wordmark-dark  { background: #000000; }
   ```

   The leading blank line separates the new block from the existing `:focus-visible` rule. Section comment header `/* === Header === */` (line 65) still covers this code — it stays inside the same section.

2. Add a single mobile-size override line INSIDE the existing `@media (max-width: 600px)` block at lines 306-311. Insert as a new line immediately AFTER `.footer-links { gap: 1rem; }` (line 310) and BEFORE the closing `}` (line 311). Final shape of the @media block:

   ```css
   @media (max-width: 600px) {
     html { font-size: 16px; }
     .site-header { flex-direction: column; gap: 0.5rem; }
     .site-wrapper { padding: 2rem 0 4rem; }
     .footer-links { gap: 1rem; }
     .wordmark { width: 160px; height: 93px; }
   }
   ```

   The `.wordmark { width: 160px; height: 93px; }` override per D-03 — single source per theme, no separate srcset (D-06).

3. Do NOT modify: any existing `:root` or `:root[data-theme="dark"]` palette tokens (Phase 4 owns those); any existing `.site-title a` typography (lines 74-83 — link hover preserved per Claude's Discretion); any `.site-nav`, `.theme-toggle`, `.post-list`, `.page-content`, `.site-footer`, `.footer-links`, or `.social-icons` rules; the existing `body` transition `@media (prefers-reduced-motion: no-preference)` block.

4. Do NOT add transitions on `.wordmark`. Wordmark inherits whatever transition behavior Phase 4's existing `body` transition provides (which doesn't apply to `display` swaps anyway — `display: none` is non-animatable). REQUIREMENTS § Future Requirements lists "view-transition cross-fade on theme toggle" as deferred; wordmark inherits the deferral.

5. Do NOT use `var(--bg)` or `var(--bg-dark)` for the per-variant `.wordmark-{variant}` background. The hardcoded hex values (`#FEFEFE` for light, `#000000` for dark) are LOCKED by D-05 + Phase 4 D-02 because they MUST match the PNG corner pixel exactly, NOT the Flexoki theme tokens (`--bg = #FFFCF0`, `--bg-dark = #100F0F`). If you use `var(--bg)`, you reintroduce the ~5–7% lightness band the cross-phase contract is designed to mask.
  </action>

  <verify>
    <automated>grep -c '\.wordmark {' themes/minimal/static/css/style.css | grep -q '^1$' &amp;&amp; grep -c 'html\[data-theme="light"\] \.wordmark-dark' themes/minimal/static/css/style.css | grep -q '^1$' &amp;&amp; grep -c 'html\[data-theme="dark"\]  \.wordmark-light' themes/minimal/static/css/style.css | grep -q '^1$' &amp;&amp; grep -q '\.wordmark-light { background: #FEFEFE; }' themes/minimal/static/css/style.css &amp;&amp; grep -q '\.wordmark-dark  { background: #000000; }' themes/minimal/static/css/style.css &amp;&amp; grep -q '\.wordmark { width: 160px; height: 93px; }' themes/minimal/static/css/style.css &amp;&amp; echo OK</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c '^\.wordmark {$' themes/minimal/static/css/style.css` returns `1` (base rule)
    - `grep -nE 'width:\s*200px' themes/minimal/static/css/style.css` matches inside the `.wordmark` rule
    - `grep -nE 'height:\s*117px' themes/minimal/static/css/style.css` matches inside the `.wordmark` rule
    - `grep -c 'html\[data-theme="light"\] \.wordmark-dark { display: none; }' themes/minimal/static/css/style.css` returns `1`
    - `grep -c 'html\[data-theme="dark"\]  \.wordmark-light { display: none; }' themes/minimal/static/css/style.css` returns `1`
    - `grep -c '\.wordmark-light { background: #FEFEFE; }' themes/minimal/static/css/style.css` returns `1`
    - `grep -c '\.wordmark-dark  { background: #000000; }' themes/minimal/static/css/style.css` returns `1`
    - `grep -c '\.wordmark { width: 160px; height: 93px; }' themes/minimal/static/css/style.css` returns `1` (mobile override)
    - The mobile override appears INSIDE the existing `@media (max-width: 600px)` block: `awk '/^@media \(max-width: 600px\) \{/,/^\}/' themes/minimal/static/css/style.css | grep -c 'wordmark'` returns `1`
    - The Phase 4 dark palette block is untouched: `grep -c ':root\[data-theme="dark"\]' themes/minimal/static/css/style.css` returns `1`
    - Existing `.site-title a` typography survives: `grep -c '\.site-title a {' themes/minimal/static/css/style.css` returns `1`
    - No `var(--bg)` is used inside the `.wordmark-light` / `.wordmark-dark` background rules: `grep -E '\.wordmark-(light|dark)\s*\{[^}]*var\(--bg' themes/minimal/static/css/style.css` returns NO matches
  </acceptance_criteria>

  <done>
    `style.css` contains a `.wordmark` family of additive rules inside the existing `/* === Header === */` block (base sizing, `html[data-theme]`-keyed display swap, `#FEFEFE`/`#000000` per-variant seam-masking bg) and a single mobile size override inside the existing `@media (max-width: 600px)` block. No existing rules are modified. The Phase 4 D-02 cross-phase contract is honored — bg colors are hardcoded hex matching the PNG corner pixels.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Static template + stylesheet | `partials/header.html` and `style.css` are static project-controlled files. No user input, no Hugo data binding for the alt text or src paths (all literals + `absURL` over hardcoded relative paths). No network IO at runtime. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03 | Cross-Site-Scripting | `<img alt="Timo Bohnstedt">` and `src="{{ "images/brand/logo-{light,dark}.png" | absURL }}"` in `partials/header.html` | accept | alt text is a hardcoded literal in the template — no Hugo data binding, no user input. The `absURL` pipe operates over a hardcoded relative path, not user-controlled. No XSS surface. |

ASVS L1 blocking issues: none.
</threat_model>

<verification>
- Run `hugo --minify` and confirm the build succeeds (no template errors, no missing-file errors for `images/brand/logo-{light,dark}.png`).
- `grep -r '{{ .Site.Title }}' themes/minimal/layouts/partials/header.html` returns nothing (text title fully replaced).
- Open `/` in a browser. With `html[data-theme="light"]` set, only the light wordmark should render; with `html[data-theme="dark"]` set, only the dark wordmark should render. The Phase 4 toggle button should swap them with no flicker.
- Run Lighthouse Mobile against `/` after deploy — CLS < 0.1 (structurally guaranteed by explicit `width`/`height` on `<img>` matching CSS dims).
- Visual eyeball check: the wordmark rectangle should sit flush against `--bg` in BOTH themes — no visible ~5–7% lightness band framing the PNG. If a band is visible, the seam-masking bg rule (`#FEFEFE` / `#000000`) is wrong or missing.
</verification>

<success_criteria>
- HEAD-01 satisfied: site header renders the script wordmark image instead of plain text title on every page (the partial is included by `baseof.html` for all routes).
- HEAD-02 satisfied: wordmark variants swap automatically with the active theme via the two-image CSS toggle keyed off `html[data-theme]` — no FOUC, no JS swap.
- HEAD-03 satisfied: both `<img>` tags carry `alt="Timo Bohnstedt"` and explicit `width="200" height="117"`.
- ROADMAP success criterion 2 satisfied: theme toggle (Phase 4) flips wordmark variants instantly with no logo flicker; CLS < 0.1 structurally guaranteed.
- Phase 4 D-02 cross-phase contract honored — per-variant seam-masking bg uses the PNG corner-pixel hex (`#FEFEFE` / `#000000`), not Flexoki theme tokens.
</success_criteria>

<output>
After completion, create `.planning/phases/05-wordmark-favicon-wiring/05-02-SUMMARY.md` summarising: header.html line-3 swap, .wordmark CSS family added (base + display swap + seam-mask bg + mobile override), Phase 4 D-02 contract verification, eyeball-check result for the seam.
</output>
