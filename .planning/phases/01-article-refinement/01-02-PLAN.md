---
phase: 01-article-refinement
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - content/blog/2026-03-27-video-editing-journey/index.md
  - content/blog/2026-03-27-video-editing-journey/images/Node tree.png
autonomous: true
requirements:
  - ART-01
  - ART-02
  - ART-03
  - ART-04
  - ART-05
tags:
  - content
  - blog
  - mermaid

must_haves:
  truths:
    - "The article shows a Mermaid diagram (rendered as `{{< mermaid >}}` shortcode invocation) of the 5-node color grading chain in left-to-right flow"
    - "The ASCII tree fenced code block (former lines 79-103) is gone"
    - "The shared 6-row parameter table (former lines 105-114) is gone"
    - "The `Node tree.png` image and its Markdown reference are gone"
    - "Each of the 5 nodes is its own H4 subsection with a one-line intro plus a `Parameter | Range | Tip` table"
    - "Every per-node table includes value ranges that already existed (Lift -0.02 to 0.00, Gamma 0.98-1.02, Gain 1.00-1.10, Saturation 45-55, LUT Intensity 0.5-0.7, Contrast 1.00-1.15) plus the supporting parameters from the existing ASCII tree"
    - "Every Tip cell is a practical do/don't sentence (what should look right, what to avoid)"
    - "The intro and outro prose around the node section is tightened to a more technical voice"
  artifacts:
    - path: "content/blog/2026-03-27-video-editing-journey/index.md"
      provides: "Refined article with Mermaid diagram + per-node tables"
      contains: "{{< mermaid >}}"
      min_lines: 140
    - path: "content/blog/2026-03-27-video-editing-journey/images/Node tree.png"
      provides: "DELETED - replaced by Mermaid diagram"
      deleted: true
  key_links:
    - from: "content/blog/2026-03-27-video-editing-journey/index.md"
      to: "themes/minimal/layouts/shortcodes/mermaid.html"
      via: "{{< mermaid >}} shortcode invocation"
      pattern: "\\{\\{<\\s*mermaid\\s*>\\}\\}"
    - from: "Mermaid diagram"
      to: "5 H4 node subsections below it"
      via: "Document structure"
      pattern: "#### Node 1.*#### Node 2.*#### Node 3.*#### Node 4.*#### Node 5"
---

<objective>
Rewrite the "My DaVinci Resolve Node Template" section of the video editing post (current lines 77-123) to:

1. Replace the ASCII tree fenced block (D-11) and the `Node tree.png` image (D-13) with a single `{{< mermaid >}}` shortcode invocation showing all 5 nodes in left-to-right flow (D-01, D-02).
2. Replace the shared parameter table (D-12) with 5 per-node H4 subsections, each containing a one-line intro and a `Parameter | Range | Tip` table (D-05, D-06, D-07).
3. Tighten the intro paragraph and the closing takeaway paragraph to match the existing technical voice (D-14).
4. Delete the `Node tree.png` asset from the page bundle (D-13).

Purpose: Convert a screenshot-based section into a precise technical reference. Every node entry tells the reader the actual numeric range to start with AND a practical visual cue to verify they got it right.

Output: Modified `content/blog/2026-03-27-video-editing-journey/index.md` and deleted `images/Node tree.png`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-article-refinement/01-CONTEXT.md
@.planning/phases/01-article-refinement/01-PATTERNS.md
@.planning/phases/01-article-refinement/01-01-SUMMARY.md
@CLAUDE.md
@content/blog/2026-03-27-video-editing-journey/index.md
@content/blog/2026-03-15-intervals-copilot/index.md
@themes/minimal/layouts/shortcodes/mermaid.html

<interfaces>
<!-- The shortcode contract that this plan consumes. -->

The `mermaid` shortcode (created by Plan 01) accepts diagram source as `.Inner`:

```
{{< mermaid >}}
graph LR
  A["Node 1<br/>Lift / Gamma / Gain"] --> B["Node 2<br/>Input CST"]
{{< /mermaid >}}
```

It emits `<div class="mermaid">...</div>` plus a once-per-page `<script src="cdn.jsdelivr.net/npm/mermaid/dist/mermaid.min.js">` and a `mermaid.initialize` call configured with Flexoki theme variables. No noscript fallback inside the shortcode.

Existing in-repo Mermaid usage to mirror (from `content/blog/2026-03-15-intervals-copilot/index.md` lines 45-54): direction is `graph LR`; node labels use double-quoted strings; arrows are `-->`.

Existing parameter table style in this same post (camera settings, lines 131-138): `Parameter | Value | Why` with no leading/trailing pipes shaving, plain English in rightmost column, en-dashes for numeric ranges (e.g. `0.98-1.02` rendered with the en-dash character).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite node section with Mermaid diagram, add per-node tables, tighten surrounding prose, delete Node tree.png</name>
  <files>
    content/blog/2026-03-27-video-editing-journey/index.md
    content/blog/2026-03-27-video-editing-journey/images/Node tree.png
  </files>
  <read_first>
    - content/blog/2026-03-27-video-editing-journey/index.md (the file being modified - read entirely; the section to be replaced is lines 67-123)
    - themes/minimal/layouts/shortcodes/mermaid.html (the shortcode this article will invoke - confirm it exists and uses `.Inner | safeHTML`)
    - content/blog/2026-03-15-intervals-copilot/index.md lines 42-64 (existing Mermaid usage in this repo - confirms `graph LR` direction and node label syntax conventions)
    - .planning/phases/01-article-refinement/01-CONTEXT.md (decisions D-01 through D-14 - every concrete value below traces back to a decision)
  </read_first>
  <action>
This task makes three coordinated edits to `content/blog/2026-03-27-video-editing-journey/index.md` and one file deletion. Apply them in this order. Use the en-dash character (the actual Unicode `EN DASH` U+2013) wherever a numeric range appears - matching the existing parameter-table style in the same post.

<!-- section break -->

### Edit A: Tighten the Step 5 intro (lines 67-75)

Locate this exact block (current lines 67-75):

```
## Step 5: Color Grading in DaVinci Resolve

This turned into its own rabbit hole. I started with a basic color correction tutorial, then went deeper into Resolve's node-based grading system. Here are the resources that helped me most:

- [Basic Color Correction in DaVinci Resolve](https://youtu.be/QB_GKc6LqIM?si=gWgKliSivFnGr8NS) - solid starting point for beginners
- [DaVinci Resolve Color Grading Playlist](https://www.youtube.com/watch?v=J1aU-zZ6ACs&list=PLanQgfsci7riIwjUdJ2UxqDBZAsaHT18d) - in-depth series on nodes, scopes, and grading workflows
- [Color Transform & LUT Workflow](https://youtu.be/g8cdK8pci40?si=M5RpnE6oN8k42aII) - helped me understand how to properly chain transforms

After a lot of trial and error, I landed on a node structure that consistently gave me the best results. Here's what worked:
```

(The bullets above use em-dash in the source; preserve whatever dash character is actually in the file.)

Replace with (D-14: tighter, more technical voice):

```
## Step 5: Color Grading in DaVinci Resolve

Color grading became its own rabbit hole. After working through the resources below and a lot of trial and error, I converged on a fixed five-node chain that I now reuse for every project. The structure stays the same shot to shot - only the values change.

Resources that mattered most:

- [Basic Color Correction in DaVinci Resolve](https://youtu.be/QB_GKc6LqIM?si=gWgKliSivFnGr8NS) - solid starting point for beginners
- [DaVinci Resolve Color Grading Playlist](https://www.youtube.com/watch?v=J1aU-zZ6ACs&list=PLanQgfsci7riIwjUdJ2UxqDBZAsaHT18d) - in-depth series on nodes, scopes, and grading workflows
- [Color Transform & LUT Workflow](https://youtu.be/g8cdK8pci40?si=M5RpnE6oN8k42aII) - how to chain transforms correctly
```

When writing this replacement, use the same em-dash character that appears in the original bullets (look at the original file before deciding). The first paragraph should use the same em-dash style as elsewhere in the post (e.g. line 142 already uses em-dashes).

<!-- section break -->

### Edit B: Replace the entire node-template body (current lines 77-114) with Mermaid diagram + 5 per-node H4 subsections

Delete current lines 77-114 (everything from `### My DaVinci Resolve Node Template` through and including the entire shared parameter table). Specifically:

- Line 77: `### My DaVinci Resolve Node Template`
- Lines 79-103: the ASCII tree fenced code block (D-11)
- Lines 105-114: the shared parameter table headed by `**Settings to start experimenting with:**` (D-12)

KEEP unchanged for now (Edit C handles them):
- Lines 116-119: before/after image references
- Line 121: closing takeaway paragraph
- Line 123: `Node tree.png` reference (deleted in Edit C)

Replace lines 77-114 with this exact content. The triple-backtick markdown fence below is a code-block wrapper for clarity - the actual content goes into the index.md WITHOUT the outer triple backticks. The Mermaid block uses `{{< mermaid >}}` not backtick-fenced code:

---BEGIN REPLACEMENT CONTENT (do not include this delimiter)---

`### My DaVinci Resolve Node Template`

(blank line)

`The chain runs left to right. Each node has one job. The order matters - input transforms before creative grading, creative grading before output transforms.`

(blank line)

`{{< mermaid >}}`
`graph LR`
`  N1["Node 1<br/>Primary Correction<br/>Lift / Gamma / Gain"] --> N2["Node 2<br/>Input CST<br/>Rec.709 input"]`
`  N2 --> N3["Node 3<br/>Creative LUT<br/>Fuji emulation"]`
`  N3 --> N4["Node 4<br/>Output CST<br/>Rec.709 output"]`
`  N4 --> N5["Node 5<br/>Final Adjustments<br/>Saturation / Contrast / Vignette"]`
`{{< /mermaid >}}`

(blank line)

`#### Node 1: Primary Correction`

(blank line)

`Fix exposure and white balance before any creative move. This is where the footage becomes neutral.`

(blank line)

The first per-node table (use en-dash U+2013 in the numeric ranges; pipe-separated columns, no leading/trailing pipe shaving):

| Parameter | Range | Tip |
|-----------|-------|-----|
| Lift | -0.02 to 0.00 | Slightly crush blacks for a cinematic feel - don't push past -0.05 or shadows go muddy. |
| Gamma | 0.98-1.02 | Small moves only; >1.05 flattens midtones into milky grey. |
| Gain | 1.00-1.10 | Lift highlights gently; check the waveform stays under 100 to avoid clipping skies. |
| White Balance | Match scene temp | Neutral whites should read close to (1.00, 1.00, 1.00) on the parade scope; a warm cast on snow means you've gone too far. |

(In the table above, replace the hyphen between `0.98` and `1.02` with the en-dash character U+2013, same for `1.00-1.10`. The Lift row keeps the regular hyphen-minus because `-0.02` is a negative number, not a range separator. Match the existing convention from the post's camera-settings table.)

(blank line)

`#### Node 2: Color Space Transform (Input)`

(blank line)

`Normalizes the camera's color space to a known starting point. Skip this and your LUT will look different on every clip.`

(blank line)

| Parameter | Range | Tip |
|-----------|-------|-----|
| Input Color Space | Match camera profile (Rec.709 for X4 Standard, LOG for Flat) | If the picture goes washed-out and grey-flat after this node, you set Input to Rec.709 on LOG footage - switch to the LOG profile. |
| Input Gamma | Rec.709 (or matching LOG curve) | Should pair with the Color Space above; mismatched gamma produces unrealistic contrast. |

(blank line)

`#### Node 3: Creative LUT`

(blank line)

`The look. Apply your LUT here - never on the raw footage and never after the output transform.`

(blank line)

| Parameter | Range | Tip |
|-----------|-------|-----|
| LUT | Fuji film emulation (or matching mood) | Pick a LUT that matches the scene mood, not just one you saw on YouTube - a warm LUT on overcast climbing footage looks fake. |
| LUT Intensity | 0.5-0.7 | Full strength almost always looks overdone; if you can name the LUT just by looking, you went too far. |

(Use en-dash in `0.5-0.7`.)

(blank line)

`#### Node 4: Color Space Transform (Output)`

(blank line)

`Converts back to the delivery color space. Required for correct color on the final export.`

(blank line)

| Parameter | Range | Tip |
|-----------|-------|-----|
| Output Color Space | Rec.709 (for web/Instagram delivery) | If skies look magenta or skin looks green in the export but fine in Resolve, this node is missing or misconfigured. |
| Output Gamma | Rec.709 | Matches the output color space; avoid mixing 2.2 / 2.4 unless you know your delivery target needs it. |

(blank line)

`#### Node 5: Final Adjustments`

(blank line)

`Last-mile polish. Use this for shot-specific tweaks without disturbing the chain above.`

(blank line)

| Parameter | Range | Tip |
|-----------|-------|-----|
| Saturation | 45-55 | Less is more, especially over a creative LUT - saturated reds in skin tones are the first sign you've over-pushed. |
| Contrast | 1.00-1.15 | Add punch without clipping; check the waveform isn't slamming the top or bottom rails. |
| Vignette | Subtle if at all | A heavy vignette on 9:16 reels reads as filter, not cinema - keep it under 0.3 strength or skip it. |

(Use en-dash in `45-55` and `1.00-1.15`.)

(blank line - this is the boundary; the next existing line is the "Here's what the difference looks like..." paragraph that Edit C will rewrite)

---END REPLACEMENT CONTENT---

Notes on the Mermaid diagram:
- Direction `graph LR` per D-01 (matches DaVinci Resolve's horizontal node layout AND the existing intervals-copilot Mermaid pattern).
- Node labels use double-quoted strings with `<br/>` line breaks per D-02 (name + key parameters per node, not just names).
- Two-space indent inside the diagram for readability (mirrors intervals-copilot post line 47).
- No edge labels - the chain is linear; arrows alone communicate flow.

Notes on the per-node tables:
- All H4 subsections use `####` (the post's existing structure tops out at H3; H4 is the natural next level).
- The Tip column follows D-07: each tip names a visual cue (what should look right) AND/OR a failure mode (what to avoid). Voice matches line 142 of the same post (`*protect the highlights, you can always brighten shadows in post, but you can't recover clipped whites.*`).
- Parameter values come from D-06 (existing shared table is accurate) plus the parameter names already in the ASCII tree (lines 79-103). Specifically: `Lift`, `Gamma`, `Gain`, `Saturation`, `LUT Intensity`, `Contrast` come from the existing shared table; `White Balance`, `Input Color Space`, `Input Gamma`, `LUT`, `Output Color Space`, `Output Gamma`, `Vignette` come from the existing ASCII tree.

<!-- section break -->

### Edit C: Tighten the closing prose and DELETE the Node tree.png reference

Locate this exact block (current lines 116-123):

```
Here's what the difference looks like in practice - before and after applying the node chain:

![Before color grading](images/before-color.png)
![After color grading](images/after-color.png)

The key takeaway: you'll need to adjust these values for every single shot, but the *structure* of the node tree should stay the same. That consistency is what keeps your edit looking cohesive and your workflow efficient.

![DaVinci Resolve node tree with 5-node structure](images/Node%20tree.png)
```

Replace with (D-13 removes the screenshot; D-14 tightens the takeaway):

```
Here's the chain applied - before, then after:

![Before color grading](images/before-color.png)
![After color grading](images/after-color.png)

The takeaway: values change every shot, structure never does. The fixed chain is what keeps an edit looking cohesive without re-deriving a workflow each time.
```

(The em-dashes in the prose should match the existing em-dash style elsewhere in the post.)

The `Node tree.png` markdown reference must NOT exist anywhere in the file after this edit.

<!-- section break -->

### Edit D: Delete the Node tree.png asset

Run from the project root:

```
rm "content/blog/2026-03-27-video-editing-journey/images/Node tree.png"
```

(Note the space in the filename - the path must be quoted.)

Per D-13, the Mermaid diagram fully replaces the screenshot, so the asset is removed from the page bundle.

<!-- section break -->

### Final structure check

After all four edits, the section between `## Step 5: Color Grading in DaVinci Resolve` and `## Step 6: Getting the Camera Settings Right (for Next Time)` should contain, in order:

1. The tightened Step 5 intro paragraph (Edit A)
2. The "Resources that mattered most:" lead-in
3. Three resource bullets
4. `### My DaVinci Resolve Node Template`
5. One-line intro paragraph ("The chain runs left to right...")
6. `{{< mermaid >}}` block with the 5-node `graph LR` diagram (Edit B)
7. Five `#### Node N: ...` subsections, each with one-line intro + 3-column table (Edit B)
8. Two-line transition ("Here's the chain applied - before, then after:")
9. Two image references: `before-color.png` and `after-color.png`
10. The tightened closing takeaway (Edit C)

The `Node tree.png` reference and asset are both gone (D-13).
  </action>
  <verify>
    <automated>bash -c 'set -e; F="content/blog/2026-03-27-video-editing-journey/index.md"; grep -q "{{< mermaid >}}" "$F"; grep -q "{{< /mermaid >}}" "$F"; grep -q "graph LR" "$F"; grep -q "#### Node 1: Primary Correction" "$F"; grep -q "#### Node 2: Color Space Transform (Input)" "$F"; grep -q "#### Node 3: Creative LUT" "$F"; grep -q "#### Node 4: Color Space Transform (Output)" "$F"; grep -q "#### Node 5: Final Adjustments" "$F"; grep -q "| Parameter | Range | Tip |" "$F"; ! grep -q "Settings to start experimenting with" "$F"; ! grep -q "images/Node%20tree.png" "$F"; ! grep -q "images/Node tree.png" "$F"; ! grep -qE "^Node 1: Primary Correction$" "$F"; [ ! -f "content/blog/2026-03-27-video-editing-journey/images/Node tree.png" ]; echo OK'</automated>
  </verify>
  <acceptance_criteria>
    - File `content/blog/2026-03-27-video-editing-journey/index.md` contains the literal string `{{< mermaid >}}` (single occurrence): `[ "$(grep -c '{{< mermaid >}}' content/blog/2026-03-27-video-editing-journey/index.md)" = "1" ]`
    - File contains the matching close: `grep -q '{{< /mermaid >}}' content/blog/2026-03-27-video-editing-journey/index.md`
    - File contains `graph LR` (Mermaid direction per D-01): `grep -q 'graph LR' content/blog/2026-03-27-video-editing-journey/index.md`
    - File contains all 5 node subsections (in this order): `grep -n '#### Node [1-5]:' content/blog/2026-03-27-video-editing-journey/index.md` returns exactly 5 lines, in numerical order
    - Each H4 subsection title:
      - `grep -q '#### Node 1: Primary Correction' content/blog/2026-03-27-video-editing-journey/index.md`
      - `grep -q '#### Node 2: Color Space Transform (Input)' content/blog/2026-03-27-video-editing-journey/index.md`
      - `grep -q '#### Node 3: Creative LUT' content/blog/2026-03-27-video-editing-journey/index.md`
      - `grep -q '#### Node 4: Color Space Transform (Output)' content/blog/2026-03-27-video-editing-journey/index.md`
      - `grep -q '#### Node 5: Final Adjustments' content/blog/2026-03-27-video-editing-journey/index.md`
    - File contains the per-node table header at least 5 times (one per node): `[ "$(grep -c '| Parameter | Range | Tip |' content/blog/2026-03-27-video-editing-journey/index.md)" -ge 5 ]`
    - All required value ranges are present (existing values preserved per D-06):
      - `grep -q '\\-0.02 to 0.00' content/blog/2026-03-27-video-editing-journey/index.md` (Lift)
      - `grep -q '0.98' content/blog/2026-03-27-video-editing-journey/index.md` AND `grep -q '1.02' content/blog/2026-03-27-video-editing-journey/index.md` (Gamma range)
      - `grep -q '1.00' content/blog/2026-03-27-video-editing-journey/index.md` AND `grep -q '1.10' content/blog/2026-03-27-video-editing-journey/index.md` (Gain range)
      - `grep -q '45' content/blog/2026-03-27-video-editing-journey/index.md` AND `grep -q '55' content/blog/2026-03-27-video-editing-journey/index.md` (Saturation)
      - `grep -q '0.5' content/blog/2026-03-27-video-editing-journey/index.md` AND `grep -q '0.7' content/blog/2026-03-27-video-editing-journey/index.md` (LUT Intensity)
      - `grep -q '1.15' content/blog/2026-03-27-video-editing-journey/index.md` (Contrast upper bound)
    - The shared parameter table is gone: `! grep -q 'Settings to start experimenting with' content/blog/2026-03-27-video-editing-journey/index.md`
    - The original column headers `Starting Value | Notes` are gone: `! grep -q 'Starting Value' content/blog/2026-03-27-video-editing-journey/index.md`
    - The ASCII tree is gone (no node names appearing as bare lines starting with `Node N:`): `! grep -qE '^Node 1: Primary Correction$' content/blog/2026-03-27-video-editing-journey/index.md` AND `! grep -qE '^Node 5: Final Adjustments$' content/blog/2026-03-27-video-editing-journey/index.md`
    - Tree-drawing characters are gone: `! grep -q 'Lift/Gamma/Gain adjustments' content/blog/2026-03-27-video-editing-journey/index.md` (this string only appeared inside the ASCII tree)
    - Specifically the box-drawing branch markers are gone: `! grep -F 'business' content/blog/2026-03-27-video-editing-journey/index.md` is irrelevant; instead: `! grep -q '└──' content/blog/2026-03-27-video-editing-journey/index.md` AND `! grep -q '├──' content/blog/2026-03-27-video-editing-journey/index.md`
    - The Node tree.png Markdown reference is gone (both URL-encoded and raw forms):
      - `! grep -q 'images/Node%20tree.png' content/blog/2026-03-27-video-editing-journey/index.md`
      - `! grep -q 'images/Node tree.png' content/blog/2026-03-27-video-editing-journey/index.md`
    - The Node tree.png asset file is deleted: `[ ! -f "content/blog/2026-03-27-video-editing-journey/images/Node tree.png" ]`
    - The before/after color grading images are still referenced: `grep -q 'images/before-color.png' content/blog/2026-03-27-video-editing-journey/index.md` AND `grep -q 'images/after-color.png' content/blog/2026-03-27-video-editing-journey/index.md`
    - The article still has its frontmatter: `head -1 content/blog/2026-03-27-video-editing-journey/index.md` returns exactly `---`
    - The article's H3 heading is preserved: `grep -q '### My DaVinci Resolve Node Template' content/blog/2026-03-27-video-editing-journey/index.md`
    - File line count is reasonable (was 167 lines, expect 140-200 after edits): `[ "$(wc -l < content/blog/2026-03-27-video-editing-journey/index.md)" -ge 140 ] && [ "$(wc -l < content/blog/2026-03-27-video-editing-journey/index.md)" -le 220 ]`
  </acceptance_criteria>
  <done>
    The article displays a Mermaid diagram of the 5-node chain instead of the ASCII tree and the screenshot, has 5 per-node H4 subsections each with a `Parameter | Range | Tip` table containing concrete value ranges and practical do/don't tips, the surrounding intro and outro prose has been tightened, and the `Node tree.png` asset is removed from the page bundle. All five phase requirements (ART-01 through ART-05) are satisfied.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser -> cdn.jsdelivr.net | Inherited from Plan 01 - the article's `{{< mermaid >}}` invocation triggers the shortcode's CDN script load. No new boundary introduced by this plan. |
| Markdown author -> Hugo | The article author (the user) writes diagram source as inner content of the shortcode. Goldmark with `unsafe = true` (already enabled site-wide) renders the resulting `<div class="mermaid">` and any embedded `<br/>` tags. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-06 | Tampering | Mermaid CDN script load (inherited) | accept | Inherited from Plan 01's threat T-01-01 - this plan is the first caller of the shortcode but introduces no new supply-chain surface. The article being modified is already a producer of CDN-loaded JS via the Instagram embed (line 17). |
| T-01-07 | Information Disclosure | Same as T-01-02 (CDN load reveals reader IP) | accept | Inherited; no change. |
| T-01-08 | Injection / XSS via diagram source | Article markdown content | accept | The diagram source is authored by the site owner and committed to git. There is no user-submitted content. The `<br/>` tags inside Mermaid node labels are static literals authored by the writer. ASVS L1 input-validation does not apply (no untrusted input). |
| T-01-09 | Denial of Service via malformed Mermaid syntax | Article render | accept | A syntax error in the diagram source produces a visible Mermaid error message in the rendered page. The author verifies during local preview before commit. The Hugo build itself does not validate Mermaid syntax (it just emits the text inside `<div class="mermaid">`), so a syntax error fails at render time, not build time. |
| T-01-10 | Spoofing / Repudiation / Elevation | N/A | n/a | No authentication or privileged operations - article is static content. |
| T-01-11 | Stale image reference | `Node tree.png` asset | mitigate | The Edit D step deletes both the markdown reference AND the asset file. The acceptance criteria verify both the absence of the markdown reference (URL-encoded and raw forms) AND the absence of the asset file. This prevents an orphaned asset shipping in the page bundle. |

No high-severity threats. The only mitigated threat (T-01-11) is fully covered by the action and acceptance criteria.
</threat_model>

<verification>
- The verify automation in Task 1 covers the structural truths (mermaid block present, 5 H4 subsections present, 5 per-node tables present, screenshot reference and asset both gone).
- Hugo build verification (manual or in a downstream phase): running `hugo --minify` should produce no errors and the rendered page at `/blog/2026-03-27-video-editing-journey/` should contain a `<div class="mermaid">` element with a `<script>` loading mermaid from jsdelivr.
- Visual verification (manual): the diagram renders in Flexoki theme colors (cream background `#FFFCF0`, dark text `#100F0F`, grey edges `#6F6E69`) and the per-node tables align with the existing camera-settings table style.
</verification>

<success_criteria>
- ART-01: SATISFIED - Mermaid diagram replaces the Node tree screenshot in the article.
- ART-02: SATISFIED - Each node description includes specific parameter value ranges (Lift -0.02 to 0.00, Gamma 0.98-1.02, Gain 1.00-1.10, etc., distributed per node per D-06).
- ART-03: SATISFIED - Each node table includes practical do/don't tips in every Tip cell.
- ART-04: SATISFIED - Node descriptions are concise and technical: each node is a one-liner intro plus a focused 2-4 row table, replacing 25+ lines of ASCII tree and a 6-row shared table.
- ART-05: SATISFIED - The Mermaid diagram uses the `{{< mermaid >}}` shortcode created by Plan 01, which renders correctly in the Hugo site.
- All five Phase 1 requirement IDs are now closed.
</success_criteria>

<output>
After completion, create `.planning/phases/01-article-refinement/01-02-SUMMARY.md` documenting:
- Files modified: `content/blog/2026-03-27-video-editing-journey/index.md` (rewritten node section)
- Files deleted: `content/blog/2026-03-27-video-editing-journey/images/Node tree.png`
- New shortcode invocation: first use of `{{< mermaid >}}` in the content directory
- Decisions implemented: D-01, D-02, D-05, D-06, D-07, D-11, D-12, D-13, D-14
- Phase 1 (ART-01..ART-05) requirements all satisfied
</output>
