---
phase: 07-about-enrichment
plan: 03
type: execute
wave: 2
depends_on:
  - 07-01
files_modified:
  - content/about/images/portrait.jpg
  - content/about/images/climbing.jpg
  - content/about/images/cycling.jpg
  - content/about/images/running.jpg
  - content/about/images/cooking.jpg
  - content/about/images/.gitkeep
autonomous: false
requirements:
  - ABOUT-02
  - ABOUT-03
tags:
  - assets
  - exif
  - human-uat
user_setup:
  - service: "Photo curation"
    why: "5 personal photos must be hand-picked by Timo before the About page can render correctly"
    env_vars: []
    dashboard_config:
      - task: "Provide 5 source photos at content/about/images/"
        location: "Local filesystem — copy from existing photo library"
must_haves:
  truths:
    - "5 user-provided photos exist at content/about/images/{portrait,climbing,cycling,running,cooking}.{jpg|jpeg|png}"
    - "The portrait source is at least 480px wide and 600px tall (Hugo's Fill action upscales smaller sources per RESEARCH amendment 3)"
    - "All 5 photos have GPS / Make / Model / Serial EXIF fields stripped via exiftool source-side scrub (matching Phase 6 D-13 procedure)"
    - "The .gitkeep placeholder from Plan 07-01 is removed (real photos make it redundant)"
  artifacts:
    - path: "content/about/images/portrait.jpg"
      provides: "Hero portrait, ≥ 480x600 px source, EXIF-scrubbed"
    - path: "content/about/images/climbing.jpg"
      provides: "Grid candid (bouldering), EXIF-scrubbed"
    - path: "content/about/images/cycling.jpg"
      provides: "Grid candid (cycling), EXIF-scrubbed"
    - path: "content/about/images/running.jpg"
      provides: "Grid candid (running), EXIF-scrubbed"
    - path: "content/about/images/cooking.jpg"
      provides: "Grid candid (cooking), EXIF-scrubbed"
  key_links:
    - from: "content/about/images/portrait.jpg"
      to: "content/about/index.md image ref ![Portrait of Timo](images/portrait.jpg \"hero\")"
      via: "Hugo .Page.Resources.GetMatch resolution"
      pattern: "images/portrait\\.jpg"
    - from: "content/about/images/{climbing,cycling,running,cooking}.jpg"
      to: "content/about/index.md grid wrapper image refs"
      via: "Hugo .Page.Resources.GetMatch resolution + render-image hook 'grid' arm"
      pattern: "\"grid\""
---

<objective>
Photo curation + EXIF scrub. The user must provide 5 source photos (1 portrait + 4 candids matching the activities listed in the existing Interests copy) and the executor verifies dimensions (RESEARCH amendment 3) and runs the EXIF scrub recipe (D-17, mirrors Phase 6 D-13). This plan is `autonomous: false` — if the photos are not yet selected when execution starts, the executor pauses and surfaces a HUMAN-UAT prompt to the user.

Purpose: Land the page-bundle resource files so Plan 07-04's cold build produces a `/about/` page with real images. Without these files, the render-image hook from Plan 07-01 falls through to its passthrough fallback and emits `<img>` tags pointing at non-existent files.

Output: 5 photos at `content/about/images/`, all EXIF-scrubbed, dimensions verified for the hero.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-about-enrichment/07-CONTEXT.md
@.planning/phases/07-about-enrichment/07-RESEARCH.md
@.planning/phases/06-gallery/06-CONTEXT.md

<interfaces>
<!-- Filename contract — content/about/index.md (created in Plan 07-01) references these exact paths -->
The leaf-bundle markdown contains the following image references (from Plan 07-01):
- `![Portrait of Timo](images/portrait.jpg "hero")` → expects `content/about/images/portrait.jpg`
- `![Bouldering at the climbing gym](images/climbing.jpg "grid")` → expects `content/about/images/climbing.jpg`
- `![Cycling in the Alps](images/cycling.jpg "grid")` → expects `content/about/images/cycling.jpg`
- `![Trail running](images/running.jpg "grid")` → expects `content/about/images/running.jpg`
- `![Cooking](images/cooking.jpg "grid")` → expects `content/about/images/cooking.jpg`

The five filenames are LOCKED. If the user provides photos with different extensions (`.jpeg` or `.png`), the markdown image references in `content/about/index.md` would also need to update — to keep this plan's blast radius minimal, prefer renaming the source files to `.jpg` even if the original extension is `.jpeg` (a rename is enough; the file format inside is unchanged).

<!-- EXIF scrub recipe (verbatim from Phase 6 D-13 / Phase 7 D-17) -->
```bash
exiftool -GPS:All= -Make= -Model= -SerialNumber= -InternalSerialNumber= -overwrite_original content/about/images/*.{jpg,jpeg,png}
```

<!-- Hugo upscaling caveat (RESEARCH item 2) -->
Hugo's `Fill` action upscales sub-target sources by default, producing blurry output. The hero is processed at `fill 480x600 Smart webp q80`, so the source portrait MUST be at least 480 px wide AND 600 px tall. Pre-flight check via Pillow:

```python
from PIL import Image
im = Image.open('content/about/images/portrait.jpg')
assert im.size[0] >= 480 and im.size[1] >= 600, f'Portrait too small: {im.size}'
```

The 4 grid candids are processed at `fill 400x300`, so the dimension floor is much lower (≥ 400 px wide AND ≥ 300 px tall) — any modern phone shot easily clears this; no explicit Pillow check is required for grid photos.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: HUMAN-UAT — Confirm 5 source photos are present at content/about/images/</name>
  <read_first>
    - .planning/phases/07-about-enrichment/07-CONTEXT.md § D-16 (HUMAN-UAT photo dependency, 5-photo set)
    - .planning/phases/07-about-enrichment/07-RESEARCH.md § Research Finding 2 (upscaling risk)
    - content/about/index.md (created by Plan 07-01 — confirms the 5 filenames the photos must match)
  </read_first>
  <what-built>The Plan 07-01 leaf bundle expects 5 photos at exact filenames in `content/about/images/`. This task is a HUMAN-UAT pause to confirm the user has supplied those photos. The executor MUST NOT proceed past this checkpoint without confirmation.</what-built>
  <how-to-verify>
1. Run this directory listing:
   ```bash
   ls -la content/about/images/
   ```
   Expected output: 5 photo files named exactly `portrait.jpg`, `climbing.jpg`, `cycling.jpg`, `running.jpg`, `cooking.jpg` (all `.jpg` extension; if user supplied `.jpeg` or `.png`, ask user to rename — the markdown references in `content/about/index.md` are locked to `.jpg`).

2. If any of the 5 are missing, surface this prompt to the user verbatim and STOP execution:

   > **HUMAN-UAT BLOCKER (Plan 07-03):** Phase 7 needs 5 personal photos before it can complete. Please copy them into `content/about/images/` with these exact filenames:
   >
   > 1. `portrait.jpg` — a portrait-orientation photo of you (face-forward or 3/4-front). MUST be at least 480 px wide AND 600 px tall — Hugo will upscale (and blur) sub-480×600 sources.
   > 2. `climbing.jpg` — a candid of you bouldering / climbing
   > 3. `cycling.jpg` — a candid of you cycling
   > 4. `running.jpg` — a candid of you trail running
   > 5. `cooking.jpg` — a candid of you cooking
   >
   > These can come from your existing photo library (including the 18-photo gallery set). Copy, don't symlink — each leaf bundle owns its own resource directory.
   >
   > After copying, reply "photos ready" and I'll resume.

3. If all 5 are present, proceed to Task 2 (dimension precheck) without prompting.
  </how-to-verify>
  <acceptance_criteria>
    - `test -f content/about/images/portrait.jpg` returns 0
    - `test -f content/about/images/climbing.jpg` returns 0
    - `test -f content/about/images/cycling.jpg` returns 0
    - `test -f content/about/images/running.jpg` returns 0
    - `test -f content/about/images/cooking.jpg` returns 0
  </acceptance_criteria>
  <resume-signal>User reply "photos ready" OR all 5 files already present at task start</resume-signal>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Dimension precheck on the hero portrait + EXIF source-side scrub on all 5 photos + remove .gitkeep</name>
  <files>content/about/images/portrait.jpg, content/about/images/climbing.jpg, content/about/images/cycling.jpg, content/about/images/running.jpg, content/about/images/cooking.jpg, content/about/images/.gitkeep</files>
  <read_first>
    - content/about/images/ (verify directory contents — Task 1 confirmed all 5 photos arrived)
    - .planning/phases/07-about-enrichment/07-CONTEXT.md § D-17 (EXIF scrub recipe)
    - .planning/phases/07-about-enrichment/07-RESEARCH.md § Research Finding 2 (upscaling — dimension precheck on portrait)
    - .planning/phases/06-gallery/06-CONTEXT.md § D-13 (Phase 6 EXIF scrub for reference — same recipe)
  </read_first>
  <action>
**Step 1 — Dimension precheck on the hero portrait** (RESEARCH amendment 3). Run this Python one-liner; it MUST exit 0 (success). If it fails, this is a HUMAN-UAT blocker — surface the prompt below and STOP execution; do not proceed to Step 2.

```bash
python3 -c "from PIL import Image; im=Image.open('content/about/images/portrait.jpg'); assert im.size[0]>=480 and im.size[1]>=600, f'Portrait too small: {im.size}'; print('OK', im.size)"
```

If the assertion fails, the user provided a too-small source (Hugo's `Fill` would upscale it, producing a blurry hero). Surface this prompt verbatim:

> **HUMAN-UAT BLOCKER (Plan 07-03 dimension gate):** `content/about/images/portrait.jpg` is smaller than 480×600 px (actual dimensions: <SIZE>). Hugo's `image.Process "fill 480x600"` would upscale this and produce a blurry hero photo. Please replace `portrait.jpg` with a larger source (any modern phone shot easily clears this — even a 1080p selfie is 1920×1080 which crops correctly). Reply "portrait replaced" when ready.

The 4 grid candids do NOT need an explicit dimension check — Hugo processes them at `fill 400x300` which is well below typical phone shot dimensions; modern phones produce 4032×3024 or larger.

**Step 2 — Verify exiftool is installed**:

```bash
which exiftool
```

If exiftool is not installed, surface a HUMAN-UAT prompt:

> **HUMAN-UAT BLOCKER:** `exiftool` is not installed. On macOS: `brew install exiftool`. On Debian/Ubuntu: `sudo apt-get install libimage-exiftool-perl`. Reply "exiftool installed" when ready.

**Step 3 — EXIF source-side scrub** (D-17, mirrors Phase 6 D-13). Run this command:

```bash
exiftool -GPS:All= -Make= -Model= -SerialNumber= -InternalSerialNumber= -overwrite_original content/about/images/portrait.jpg content/about/images/climbing.jpg content/about/images/cycling.jpg content/about/images/running.jpg content/about/images/cooking.jpg
```

(Listing files explicitly avoids shell-glob quirks across `.jpg` / `.jpeg` / `.png`. The plan locks all 5 to `.jpg` extension per the leaf-bundle markdown references; if the user supplied a different extension, the photos should have been renamed in Task 1's HUMAN-UAT prompt.)

Expected exiftool output: `5 image files updated`. The `-overwrite_original` flag prevents `.jpg_original` backup files from being created.

**Step 4 — Verify the scrub took effect** by spot-checking the source files:

```bash
exiftool -GPSLatitude -GPSLongitude -Make -Model -SerialNumber -InternalSerialNumber content/about/images/portrait.jpg content/about/images/climbing.jpg content/about/images/cycling.jpg content/about/images/running.jpg content/about/images/cooking.jpg 2>/dev/null
```

Expected output: ONLY filename header lines (e.g., `======== content/about/images/portrait.jpg`). NO field-value rows like `GPS Latitude : 48.2082`. If any field-value row appears, exiftool's scrub failed — surface the offending field to the user and stop.

**Step 5 — Remove the `.gitkeep` placeholder** from Plan 07-01 (real photos make it redundant):

```bash
rm content/about/images/.gitkeep
```

This is a regular `rm`, not `git rm` — the `.gitkeep` was committed in Plan 07-01 as part of the page-bundle directory tracker; removing it now leaves git aware of the 5 real photos in the same directory.

**Note:** This task is the only one in Plan 07-03 that runs commands; it does NOT modify the leaf-bundle markdown, the render-image hook, the CSS file, or any other text artifact. The 5 photos and the absence of `.gitkeep` are the only filesystem deltas.

**Note on `public/about/images/*.webp`:** the rendered WebP outputs do NOT exist yet — they are produced during Plan 07-04's cold build. The verification gate `exiftool -GPSLatitude -GPSLongitude … public/about/images/*.webp` runs in Plan 07-04 after `hugo --minify`. This plan only verifies the **source-side** scrub.
  </action>
  <verify>
    <automated>test -f content/about/images/portrait.jpg && test -f content/about/images/climbing.jpg && test -f content/about/images/cycling.jpg && test -f content/about/images/running.jpg && test -f content/about/images/cooking.jpg && test ! -f content/about/images/.gitkeep && python3 -c "from PIL import Image; im=Image.open('content/about/images/portrait.jpg'); assert im.size[0]>=480 and im.size[1]>=600, f'Portrait too small: {im.size}'; print('OK', im.size)" && OUT=$(exiftool -GPSLatitude -GPSLongitude -Make -Model -SerialNumber -InternalSerialNumber content/about/images/portrait.jpg content/about/images/climbing.jpg content/about/images/cycling.jpg content/about/images/running.jpg content/about/images/cooking.jpg 2>/dev/null | grep -v '^=' | grep -v '^$' | wc -l) && test "$OUT" -eq 0 && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - All 5 photos exist at exact paths: `content/about/images/{portrait,climbing,cycling,running,cooking}.jpg`
    - The `.gitkeep` placeholder is removed: `test ! -f content/about/images/.gitkeep` returns 0
    - Portrait source is at least 480×600: Pillow assertion passes (RESEARCH amendment 3)
    - EXIF source-side scrub is clean: `exiftool -GPSLatitude -GPSLongitude -Make -Model -SerialNumber -InternalSerialNumber content/about/images/*.jpg` returns ONLY filename headers, NO field-value rows. The pipeline `... | grep -v '^=' | grep -v '^$' | wc -l` returns 0.
    - exiftool is installed (verified by Step 2 prompt — `which exiftool` returns a path)
  </acceptance_criteria>
  <done>All 5 photos exist at the expected paths, the portrait clears the 480×600 dimension floor, source-side EXIF is scrubbed (zero GPS/Make/Model/Serial fields), and the `.gitkeep` placeholder is removed.</done>
</task>

</tasks>

<verification>
This plan delivers ABOUT-02 (multiple photos rendered → photos exist on disk) and ABOUT-03 (page-bundle resources, EXIF clean). The full-resolution WebP gate (`public/about/images/*.webp` with zero GPS/Make/Model/Serial) runs in Plan 07-04 after `hugo --minify` produces the WebP outputs.

Why two-task structure (HUMAN-UAT then auto):
- Task 1 is a pure asset-arrival check that may pause. Splitting it from the scrub means the executor can resume cleanly when the user replies — Task 2 picks up from "all 5 files present" and runs deterministically.
- The dimension precheck and EXIF scrub are tightly coupled (both touch the same files) and don't pause unless something is wrong, so they live in one auto task.
</verification>

<success_criteria>
- 5 photos at `content/about/images/{portrait,climbing,cycling,running,cooking}.jpg`
- Portrait dimensions ≥ 480×600 (Hugo upscaling guard per RESEARCH item 2)
- Source-side EXIF clean (zero GPS/Make/Model/Serial fields)
- `.gitkeep` placeholder removed (real photos take its place)
- Plan 07-04 can now run a cold build and produce real WebP outputs
</success_criteria>

<output>
After completion, create `.planning/phases/07-about-enrichment/07-03-SUMMARY.md` with:
- The 5 source files (paths, source dimensions before scrub, file sizes)
- The exiftool scrub command and "5 image files updated" confirmation
- The Pillow precheck output for the hero portrait
- Note: WebP-output EXIF verification deferred to Plan 07-04 (post-build)
</output>
