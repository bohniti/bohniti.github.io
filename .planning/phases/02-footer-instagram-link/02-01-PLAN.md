---
phase: 02-footer-instagram-link
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - themes/minimal/layouts/partials/footer.html
  - themes/minimal/static/css/style.css
autonomous: true
requirements:
  - SOC-01
  - SOC-02
  - SOC-03

must_haves:
  truths:
    - "An Instagram icon is visible in the site footer on every page"
    - "Clicking the Instagram icon opens https://instagram.com/bohniti in a new tab"
    - "A GitHub icon (replacing the previous text link) is visible in the same footer"
    - "Both icons inherit the existing footer link colors (rest = --text-secondary, hover = --accent)"
    - "Screen readers announce 'GitHub' and 'Instagram' as the icon link destinations"
    - "The two social icons are visually clustered tighter than the gap between them and the text nav (About | Blog)"
  artifacts:
    - path: "themes/minimal/layouts/partials/footer.html"
      provides: "Footer markup with text nav (About, Blog) + clustered icon links (GitHub, Instagram)"
      contains: 'aria-label="Instagram"'
    - path: "themes/minimal/layouts/partials/footer.html"
      provides: "Instagram external link"
      contains: 'href="https://instagram.com/bohniti"'
    - path: "themes/minimal/layouts/partials/footer.html"
      provides: "External link safety on both social links"
      contains: 'rel="noopener noreferrer"'
    - path: "themes/minimal/static/css/style.css"
      provides: "Social cluster wrapper styling with tight inner gap"
      contains: ".social-icons"
  key_links:
    - from: "themes/minimal/layouts/partials/footer.html"
      to: "themes/minimal/static/css/style.css"
      via: ".social-icons class"
      pattern: "\\.social-icons"
    - from: "themes/minimal/layouts/partials/footer.html"
      to: "Instagram"
      via: "href on icon link"
      pattern: "instagram\\.com/bohniti"
    - from: "inline SVG icons"
      to: ".site-footer a color rules"
      via: "stroke=\"currentColor\""
      pattern: "stroke=\"currentColor\""
---

<objective>
Add an Instagram icon link to the site footer (visible on every page via the shared `footer.html` partial), convert the existing GitHub text link to an icon for visual consistency, and add the small CSS rule needed for the tight social-icon cluster. Both icons use inline SVG with `currentColor` so they inherit the existing footer hover/rest colors with no new color rules. Both external links get `rel="noopener noreferrer"` and `aria-label` per D-09 and accessibility requirements.

Purpose: Deliver SOC-01 (icon visible on every page), SOC-02 (links to https://instagram.com/bohniti), SOC-03 (style matches Flexoki minimal aesthetic).

Output: Modified `themes/minimal/layouts/partials/footer.html` and `themes/minimal/static/css/style.css`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-footer-instagram-link/02-CONTEXT.md
@.planning/phases/02-footer-instagram-link/02-PATTERNS.md
@themes/minimal/layouts/partials/footer.html
@themes/minimal/static/css/style.css

<interfaces>
<!-- Existing CSS variables and footer rules the SVG icons inherit from. -->
<!-- Executor must NOT redeclare these — icons inherit via currentColor. -->

From themes/minimal/static/css/style.css `:root` block (lines 4-18):
```css
--text-secondary: #6F6E69;  /* footer link rest color */
--accent:         #AF3029;  /* footer link hover color */
--border:         #E6E4D9;  /* footer top border */
```

From themes/minimal/static/css/style.css `/* === Footer === */` section (lines 231-245):
```css
.site-footer a {
  color: var(--text-secondary);
  text-decoration: none;
}

.site-footer a:hover {
  color: var(--accent);
}

.footer-links {
  display: flex;
  justify-content: center;
  gap: 1.5rem;
  margin-bottom: 0.5rem;
}
```

Current state of themes/minimal/layouts/partials/footer.html (7 lines):
```html
<footer class="site-footer">
  <div class="footer-links">
    <a href="/about/">About</a>
    <a href="/blog/">Blog</a>
    <a href="https://github.com/bohniti" target="_blank">GitHub</a>
  </div>
</footer>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace footer.html contents with text-nav + clustered social icons</name>
  <files>themes/minimal/layouts/partials/footer.html</files>
  <read_first>
    - themes/minimal/layouts/partials/footer.html (current 7-line state)
    - .planning/phases/02-footer-instagram-link/02-CONTEXT.md (decisions D-01 through D-09, plus accessibility section)
    - .planning/phases/02-footer-instagram-link/02-PATTERNS.md (External Link Safety, Color Inheritance, Accessibility for Icon-Only Links sections)
  </read_first>
  <action>
Overwrite the entire file `themes/minimal/layouts/partials/footer.html` with EXACTLY this content (preserving 2-space indentation, double-quoted attributes, and trailing newline):

```html
<footer class="site-footer">
  <div class="footer-links">
    <a href="/about/">About</a>
    <a href="/blog/">Blog</a>
    <span class="social-icons">
      <a href="https://github.com/bohniti" target="_blank" rel="noopener noreferrer" aria-label="GitHub">
        <svg xmlns="http://www.w3.org/2000/svg" width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
          <path d="M15 22v-4a4.8 4.8 0 0 0-1-3.5c3 0 6-2 6-5.5.08-1.25-.27-2.48-1-3.5.28-1.15.28-2.35 0-3.5 0 0-1 0-3 1.5-2.64-.5-5.36-.5-8 0C6 2 5 2 5 2c-.3 1.15-.3 2.35 0 3.5A5.403 5.403 0 0 0 4 9c0 3.5 3 5.5 6 5.5-.39.49-.68 1.05-.85 1.65-.17.6-.22 1.23-.15 1.85v4"></path>
          <path d="M9 18c-4.51 2-5-2-7-2"></path>
        </svg>
      </a>
      <a href="https://instagram.com/bohniti" target="_blank" rel="noopener noreferrer" aria-label="Instagram">
        <svg xmlns="http://www.w3.org/2000/svg" width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
          <rect width="20" height="20" x="2" y="2" rx="5" ry="5"></rect>
          <path d="M16 11.37A4 4 0 1 1 12.63 8 4 4 0 0 1 16 11.37z"></path>
          <line x1="17.5" x2="17.51" y1="6.5" y2="6.5"></line>
        </svg>
      </a>
    </span>
  </div>
</footer>
```

Notes on the markup choices (do NOT change them):
- Per D-02, the existing GitHub text link is converted to an icon (not deleted, not preserved as text).
- Per D-06, ordering inside `.footer-links` is About, Blog, GitHub-icon, Instagram-icon (text nav first, social cluster last).
- Per D-07, the two social anchors are wrapped in `<span class="social-icons">` so the inner gap is controlled by a dedicated CSS rule (not by per-element margins). The wrapper choice is intentional and Task 2 depends on this exact class name `social-icons`.
- Per D-03, SVGs are inline; no external file or CDN.
- Per D-04, glyphs are Lucide-style monochrome outlines (camera-square for Instagram, octocat-outline for GitHub) — explicitly NOT Instagram brand gradient.
- Per D-05, both SVGs use `stroke="currentColor"` and `fill="none"` so they inherit the footer link color (rest `--text-secondary`, hover `--accent`) from the existing `.site-footer a` rules — NO new color CSS for the icons themselves.
- Per D-09, both external `<a>`s use `target="_blank" rel="noopener noreferrer"`. The previous GitHub link lacked `rel`; this fixes it while we are editing.
- Per accessibility decisions in CONTEXT.md, each `<a>` has `aria-label` (icon-only links must announce destination) and each `<svg>` has `aria-hidden="true"` (prevents duplicate screen reader output).
- Per D-04 Claude's Discretion, icon dimensions are 18x18 (matches the 0.82rem footer text size visually).
- Per the file's existing convention (PATTERNS.md), no Hugo template directives are used in `footer.html` — the markup stays plain HTML.
  </action>
  <verify>
    <automated>bash -c 'set -e; F="themes/minimal/layouts/partials/footer.html"; [ "$(grep -c "<svg" "$F")" = "2" ]; grep -q '"'"'aria-label="GitHub"'"'"' "$F"; grep -q '"'"'aria-label="Instagram"'"'"' "$F"; grep -q '"'"'href="https://instagram.com/bohniti"'"'"' "$F"; grep -q '"'"'href="https://github.com/bohniti"'"'"' "$F"; [ "$(grep -c '"'"'rel="noopener noreferrer"'"'"' "$F")" = "2" ]; [ "$(grep -c '"'"'target="_blank"'"'"' "$F")" = "2" ]; grep -q '"'"'class="social-icons"'"'"' "$F"; [ "$(grep -c '"'"'stroke="currentColor"'"'"' "$F")" = "2" ]; [ "$(grep -c '"'"'aria-hidden="true"'"'"' "$F")" = "2" ]; [ "$(grep -c '"'"'fill="none"'"'"' "$F")" = "2" ]; ! grep -q ">GitHub</a>" "$F"; grep -q '"'"'<a href="/about/">About</a>'"'"' "$F"; grep -q '"'"'<a href="/blog/">Blog</a>'"'"' "$F"; echo OK'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '<svg' themes/minimal/layouts/partials/footer.html` returns exactly `2` (one GitHub, one Instagram)
    - `grep -q 'aria-label="GitHub"' themes/minimal/layouts/partials/footer.html` succeeds
    - `grep -q 'aria-label="Instagram"' themes/minimal/layouts/partials/footer.html` succeeds
    - `grep -q 'href="https://instagram.com/bohniti"' themes/minimal/layouts/partials/footer.html` succeeds
    - `grep -q 'href="https://github.com/bohniti"' themes/minimal/layouts/partials/footer.html` succeeds
    - `grep -c 'rel="noopener noreferrer"' themes/minimal/layouts/partials/footer.html` returns exactly `2`
    - `grep -c 'target="_blank"' themes/minimal/layouts/partials/footer.html` returns exactly `2`
    - `grep -q 'class="social-icons"' themes/minimal/layouts/partials/footer.html` succeeds
    - `grep -c 'stroke="currentColor"' themes/minimal/layouts/partials/footer.html` returns exactly `2`
    - `grep -c 'aria-hidden="true"' themes/minimal/layouts/partials/footer.html` returns exactly `2`
    - `grep -c 'fill="none"' themes/minimal/layouts/partials/footer.html` returns exactly `2`
    - The literal text `>GitHub</a>` no longer appears in the file (text link is gone): `! grep -q '>GitHub</a>' themes/minimal/layouts/partials/footer.html`
    - The internal `<a href="/about/">About</a>` and `<a href="/blog/">Blog</a>` lines are still present unchanged (text nav preserved)
    - No external file or CDN reference is introduced (no `<link>`, no `<script>`, no `src=` attribute pointing to icons)
  </acceptance_criteria>
  <done>
    `footer.html` contains text nav (About, Blog) followed by a `<span class="social-icons">` wrapper containing two icon-only `<a>` elements (GitHub, Instagram), each with full external-link safety (`target="_blank" rel="noopener noreferrer"`), accessibility (`aria-label` on `<a>`, `aria-hidden="true"` on inner `<svg>`), and `stroke="currentColor" fill="none"` so the icons inherit existing footer colors. The previous plain-text GitHub link is gone.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add `.social-icons` cluster CSS rule and mobile spacing tweak</name>
  <files>themes/minimal/static/css/style.css</files>
  <read_first>
    - themes/minimal/static/css/style.css (specifically the `/* === Footer === */` section starting at line 221, and `/* === Responsive === */` block at line 247)
    - themes/minimal/layouts/partials/footer.html (after Task 1 — confirms the wrapper class is `social-icons`)
    - .planning/phases/02-footer-instagram-link/02-CONTEXT.md (D-07 for the 0.6rem inner gap, and the mobile responsive note in Claude's Discretion)
    - .planning/phases/02-footer-instagram-link/02-PATTERNS.md (CSS organization conventions: section comments, kebab-case class names, two-space indentation, no vendor prefixes)
  </read_first>
  <action>
Edit `themes/minimal/static/css/style.css` to add the `.social-icons` cluster rule inside the existing `/* === Footer === */` section, and add a mobile spacing safeguard inside the existing `@media (max-width: 600px)` block.

**Edit 1 — Append to the Footer section.** Locate the existing block ending at line 245:

```css
.footer-links {
  display: flex;
  justify-content: center;
  gap: 1.5rem;
  margin-bottom: 0.5rem;
}
```

Insert a blank line after the closing `}`, then insert this new rule block immediately before the `/* === Responsive === */` comment line:

```css
.social-icons {
  display: flex;
  align-items: center;
  gap: 0.6rem;
}

.social-icons a {
  display: inline-flex;
  align-items: center;
}
```

Notes on these CSS choices (do NOT change them):
- Per D-07, the `.social-icons` wrapper has `gap: 0.6rem` (tight inner cluster) versus the parent `.footer-links` `gap: 1.5rem` (separation from text nav). The 0.6rem value is locked by D-07.
- `align-items: center` keeps the SVGs vertically centered against the text-link baseline.
- The `.social-icons a { display: inline-flex; align-items: center; }` rule prevents the default inline anchor baseline-alignment quirk that adds extra vertical whitespace below SVGs.
- Per D-08, NO new color or hover rules are added — icons inherit `var(--text-secondary)` and `var(--accent)` via the existing `.site-footer a` and `.site-footer a:hover` rules using `currentColor` on the SVGs.
- Per CSS organization conventions (PATTERNS.md), this rule goes inside the existing `/* === Footer === */` section, NOT as a new section. Use 2-space indentation, kebab-case class name, double-quoted strings if any, no vendor prefixes, one blank line between rule blocks.

**Edit 2 — Extend the mobile media query.** The existing block (lines 247-252) is:

```css
@media (max-width: 600px) {
  html { font-size: 16px; }
  .site-header { flex-direction: column; gap: 0.5rem; }
  .site-wrapper { padding: 2rem 0 4rem; }
}
```

Add ONE new rule line inside the same `@media` block (do NOT create a second media query). The block becomes:

```css
@media (max-width: 600px) {
  html { font-size: 16px; }
  .site-header { flex-direction: column; gap: 0.5rem; }
  .site-wrapper { padding: 2rem 0 4rem; }
  .footer-links { gap: 1rem; }
}
```

Notes:
- Per the existing responsive block convention, single-property rules are written on one line (e.g., `.site-wrapper { padding: 2rem 0 4rem; }`). Match that style for the new rule.
- The 1rem mobile gap (down from 1.5rem desktop) prevents the social cluster from wrapping awkwardly on narrow screens. The inner `.social-icons` 0.6rem stays as-is on mobile (still readable at small sizes).
- Do NOT introduce a new `:root` variable. Use the literal `1rem` value, matching the codebase convention of inline rem values inside media-query overrides.
  </action>
  <verify>
    <automated>bash -c 'set -e; F="themes/minimal/static/css/style.css"; grep -q "\.social-icons {" "$F"; grep -q "gap: 0.6rem" "$F"; grep -q "\.social-icons a {" "$F"; grep -q "display: inline-flex" "$F"; grep -q "\.footer-links { gap: 1rem; }" "$F"; [ "$(grep -c "@media (max-width: 600px)" "$F")" = "1" ]; ! grep -E "social-icons[^{]*\b(color|stroke|fill)\s*:" "$F"; awk '"'"'/\/\* === Responsive === \*\//{exit} /\.social-icons \{/{found=1} END{exit !found}'"'"' "$F"; echo OK'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q '\.social-icons {' themes/minimal/static/css/style.css` succeeds (the wrapper rule exists)
    - `grep -q 'gap: 0.6rem' themes/minimal/static/css/style.css` succeeds (D-07 inner-cluster gap value present verbatim)
    - `grep -q '\.social-icons a {' themes/minimal/static/css/style.css` succeeds (the inner anchor rule exists)
    - `grep -q 'display: inline-flex' themes/minimal/static/css/style.css` succeeds (anchor display rule present)
    - `grep -q '\.footer-links { gap: 1rem; }' themes/minimal/static/css/style.css` succeeds (mobile override present)
    - `grep -c '@media (max-width: 600px)' themes/minimal/static/css/style.css` returns exactly `1` (no duplicate media query introduced)
    - The new `.social-icons` rule appears BEFORE the line `/* === Responsive === */` (i.e., still inside the Footer section): verified by `awk '/\/\* === Responsive === \*\//{exit} /\.social-icons \{/{found=1} END{exit !found}' themes/minimal/static/css/style.css`
    - No new color rule is introduced for the icons: `! grep -E 'social-icons.*\b(color|stroke|fill)\s*:' themes/minimal/static/css/style.css`
    - No new CSS custom property is introduced (`:root` block size unchanged): the file should still contain only the existing variable declarations
    - No vendor prefixes added: `! grep -E '^\s*(-webkit-|-moz-|-ms-|-o-)' themes/minimal/static/css/style.css | grep -v 'box-sizing\|appearance' || true` (treat any *new* prefix as a failure)
  </acceptance_criteria>
  <done>
    `style.css` contains a new `.social-icons { display: flex; align-items: center; gap: 0.6rem; }` rule (plus a small `.social-icons a` inline-flex helper) inside the existing `/* === Footer === */` section, and a single new `.footer-links { gap: 1rem; }` rule inside the existing `@media (max-width: 600px)` block. No color, stroke, fill, or hover rules are added — icons rely on inheritance through `currentColor` plus the existing `.site-footer a` rules.
  </done>
</task>

</tasks>

<verification>

## Phase Verification

**SOC-01 (icon visible on every page):**
- `themes/minimal/layouts/partials/footer.html` is included on every page via `{{ partial "footer.html" . }}` in `_default/baseof.html` (existing wiring, unchanged).
- After Task 1, that partial contains an Instagram `<svg>` inside an `<a>` with `aria-label="Instagram"`.
- Verification: load the home page (`/`), `/about/`, and any blog post (e.g., `/blog/2026-03-27-video-editing-journey/`) in a browser. The Instagram icon must be visible in the footer of all three.
- Automated check: `grep -q 'aria-label="Instagram"' themes/minimal/layouts/partials/footer.html` AND `grep -q '{{ partial "footer.html" . }}' themes/minimal/layouts/_default/baseof.html` (existing).

**SOC-02 (icon links to https://instagram.com/bohniti):**
- After Task 1: `grep -q 'href="https://instagram.com/bohniti"' themes/minimal/layouts/partials/footer.html`.
- Manual verification (recommended): clicking the Instagram icon in a rendered page navigates to `https://instagram.com/bohniti` in a new tab.

**SOC-03 (style matches the minimal Flexoki aesthetic):**
- Icons use `stroke="currentColor"` so they render in `var(--text-secondary)` (#6F6E69) at rest, matching the muted footer text color.
- On hover they shift to `var(--accent)` (#AF3029), matching the existing footer link hover.
- Glyphs are monochrome outlines (no gradient, no fill), consistent with the "minimal, simplistic icon" project constraint from CLAUDE.md and D-04.
- No new color tokens, fonts, or external assets introduced.
- Manual verification: in a rendered page, the icons should be visually quiet (not loud), the same color family as the "About | Blog" text links, and turn red on hover identically to those text links.

**Build sanity:**
- After both tasks complete, run `hugo --minify` (or check the `.github/workflows/deploy.yml` build will succeed) — no template syntax errors should be introduced because `footer.html` remains plain HTML (no new Hugo directives).

</verification>

<success_criteria>

The phase is complete when ALL of these are true:

1. `footer.html` contains exactly two `<svg>` elements, one inside an `<a>` with `aria-label="GitHub"` and `href="https://github.com/bohniti"`, the other inside an `<a>` with `aria-label="Instagram"` and `href="https://instagram.com/bohniti"`.
2. Both social `<a>` elements have `target="_blank"` AND `rel="noopener noreferrer"` (verified by `grep -c` returning `2` for each).
3. Both `<svg>` elements have `stroke="currentColor"`, `fill="none"`, and `aria-hidden="true"`.
4. The two social anchors are wrapped in `<span class="social-icons">` inside `.footer-links`, after the `/about/` and `/blog/` text links.
5. `style.css` defines a `.social-icons` rule with `display: flex; align-items: center; gap: 0.6rem;` inside the existing `/* === Footer === */` section.
6. `style.css` adds `.footer-links { gap: 1rem; }` inside the existing single `@media (max-width: 600px)` block (no second media query).
7. No new color rule, no new CSS variable, no external icon asset, and no Hugo template directive is introduced.
8. The previous plain-text "GitHub" footer link is gone (replaced by an icon).
9. Hugo build (`hugo --minify`) completes without errors.

</success_criteria>

<output>

After completion, create `.planning/phases/02-footer-instagram-link/02-01-SUMMARY.md` recording:
- What was changed in `footer.html` (markup before -> after summary, and which decisions D-XX it implements).
- What CSS rules were added in `style.css`.
- Manual verification steps performed (which pages were viewed, hover behavior tested).
- Any deviations from the plan (should be none — every value is locked above).

</output>
