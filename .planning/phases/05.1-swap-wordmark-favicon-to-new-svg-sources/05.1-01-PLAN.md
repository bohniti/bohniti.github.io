---
phase: "05.1"
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - themes/minimal/layouts/partials/header.html
  - themes/minimal/static/css/style.css
  - themes/minimal/static/images/brand/logo.svg
autonomous: true
requirements:
  - HEAD-01
  - HEAD-02
  - HEAD-03

must_haves:
  truths:
    - "Site header renders the script wordmark instead of plain text site-title on every page"
    - "Wordmark recolors automatically when the user toggles between light and dark theme (no JS swap, no FOUC)"
    - "Wordmark carries an accessible label equivalent to the prior alt text (Timo Bohnstedt)"
    - "Wordmark dimensions are explicit on the SVG element (CLS guarantee preserved)"
    - "Mobile viewport (<= 600px) renders the wordmark at 160x98 px"
  artifacts:
    - path: "themes/minimal/static/images/brand/logo.svg"
      provides: "Native SVG wordmark with currentColor fills, explicit dimensions, a11y attributes"
      contains: 'fill="currentColor"'
    - path: "themes/minimal/layouts/partials/header.html"
      provides: "Single inlined SVG via readFile + safeHTML in place of the two-image swap"
      contains: 'readFile "themes/minimal/static/images/brand/logo.svg"'
    - path: "themes/minimal/static/css/style.css"
      provides: ".site-title svg rule driving wordmark color via var(--text); deleted .wordmark* family; updated mobile override"
      contains: ".site-title svg"
  key_links:
    - from: "themes/minimal/layouts/partials/header.html"
      to: "themes/minimal/static/images/brand/logo.svg"
      via: "Hugo readFile project-root-relative path piped through safeHTML"
      pattern: 'readFile.*logo\.svg.*safeHTML'
    - from: "themes/minimal/static/css/style.css (.site-title svg)"
      to: ":root[data-theme] var(--text) token"
      via: "color: var(--text) cascading into SVG paths via currentColor"
      pattern: "\\.site-title svg.*color: var\\(--text\\)"
---

<objective>
Swap the Phase 5 two-image PNG wordmark to a single inlined SVG that recolors via `currentColor` + Phase 4's `var(--text)` token. Replaces the two `<img>` tags in `partials/header.html` with one `{{ readFile ... | safeHTML }}` call, deletes the entire `.wordmark*` CSS family, and adds a single `.site-title svg` rule that pipes the Flexoki text token into the SVG's `currentColor` paths.

Purpose: Eliminate the two-image-swap CSS, the seam-masking hex backgrounds, the per-theme display-toggle, the duplicate HTTP fetches, and the `[data-theme]`-keyed display logic — all replaced by a single inline SVG that inherits `var(--text)` transitively. Honors HEAD-01/02/03 (wordmark in header, theme-aware, accessible) via inline SVG idiom. Honors UI-SPEC § Color hardcoded-hex justification — this plan introduces zero off-token colors because the wordmark inherits `--text`. Honors UI-SPEC § Verification Gates — explicit `width="200" height="122"` on the inlined `<svg>` element preserves the CLS-clean guarantee from Phase 5.

Output:
- `themes/minimal/static/images/brand/logo.svg` (NEW, ~25 KB; 11 fills rewritten to `currentColor`; explicit `width`/`height`/`role`/`aria-label`/`<title>` on root `<svg>`)
- `themes/minimal/layouts/partials/header.html` (MODIFIED; two `<img>` tags → one inline SVG)
- `themes/minimal/static/css/style.css` (MODIFIED; `.wordmark*` family deleted; `.site-title svg` rule added; mobile override rewritten)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05.1-swap-wordmark-favicon-to-new-svg-sources/05.1-CONTEXT.md
@.planning/phases/05.1-swap-wordmark-favicon-to-new-svg-sources/05.1-UI-SPEC.md
@.planning/phases/05.1-swap-wordmark-favicon-to-new-svg-sources/05.1-PATTERNS.md
@.planning/codebase/CONVENTIONS.md
@themes/minimal/layouts/partials/header.html
@themes/minimal/static/css/style.css
@images/brand-source/logo.svg

<interfaces>
<!-- Existing template idiom (preserved verbatim around the edit point in header.html) -->
<!-- Lines 1-3, 6-13 of partials/header.html stay identical; only lines 4-5 are replaced. -->

From themes/minimal/layouts/_default/baseof.html (Hugo template idiom precedent):
```go-html
{{ partial "favicon.html" . }}
{{ "favicon.svg" | absURL }}
```
The `{{ X | safeHTML }}` filter is the same idiom family as `{{ X | absURL }}` — chained filter, no special config needed.

From themes/minimal/static/css/style.css lines 74-83 (existing token-hookup precedent for the new rule):
```css
.site-title a {
  font-size: 1.1rem;
  font-weight: 600;
  color: var(--text);
  text-decoration: none;
}
.site-title a:hover { color: var(--accent); }
```
The new `.site-title svg { color: var(--text); }` rule mirrors line 77 — same surface, same token, same Flexoki contract.

From themes/minimal/static/css/style.css lines 4-33 (Flexoki token system the new rule plugs into):
```css
:root { --bg: #FFFCF0; --text: #100F0F; ... }
:root[data-theme="dark"] { --bg: #100F0F; --text: #CECDC3; ... }
```
The wordmark recolor on theme toggle is transitive through `var(--text)` — no `[data-theme]` selector needed in the new rule.

From themes/minimal/static/css/style.css lines 49-56 (inherited transition the wordmark uses for free):
```css
@media (prefers-reduced-motion: no-preference) {
  body { transition: background-color 150ms ease, color 150ms ease, border-color 150ms ease; }
}
```
The inline SVG inherits this via CSS cascade — no per-SVG transition rule needed.

From images/brand-source/logo.svg (source — to be transformed):
- Root: `<svg version="1.1" id="Layer_1" xmlns="..." xmlns:xlink="..." x="0px" y="0px" width="100%" viewBox="0 0 1024 624" enable-background="..." xml:space="preserve">`
- 11 `<path fill="#000000" opacity="1.000000" stroke="none" d="...">` elements
- File size ~25 KB, 395 lines (preserve all path `d=` data verbatim)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create themes/minimal/static/images/brand/logo.svg from source with currentColor + a11y attributes</name>
  <read_first>
    - images/brand-source/logo.svg (full file — preserve all 11 path `d=` attribute payloads verbatim; do NOT reformat or minify path data)
    - .planning/phases/05.1-swap-wordmark-favicon-to-new-svg-sources/05.1-CONTEXT.md § Wordmark Embedding (D-02, D-03, D-04)
    - .planning/phases/05.1-swap-wordmark-favicon-to-new-svg-sources/05.1-UI-SPEC.md § Color (hardcoded-hex justification — `currentColor` is the mechanism), § Copywriting Contract (a11y-equivalent of `alt="Timo Bohnstedt"` is `<title>` element + `aria-label`)
    - .planning/phases/05.1-swap-wordmark-favicon-to-new-svg-sources/05.1-PATTERNS.md § themes/minimal/static/images/brand/logo.svg (NEW) — full required transformation
  </read_first>
  <files>themes/minimal/static/images/brand/logo.svg (NEW)</files>
  <action>
Create the file `themes/minimal/static/images/brand/logo.svg` by copying the source `images/brand-source/logo.svg` and applying these transformations:

1. **Root `<svg>` element rewrite** — Replace the existing two-line root tag (currently `<svg version="1.1" id="Layer_1" xmlns="http://www.w3.org/2000/svg" xmlns:xlink="http://www.w3.org/1999/xlink" x="0px" y="0px"\n\t width="100%" viewBox="0 0 1024 624" enable-background="new 0 0 1024 624" xml:space="preserve">`) with this single-line root tag exactly:

   ```svg
   <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 1024 624" width="200" height="122" role="img" aria-label="Timo Bohnstedt">
     <title>Timo Bohnstedt</title>
   ```

   Drop these attributes from the root: `version="1.1"`, `id="Layer_1"`, `xmlns:xlink="..."`, `x="0px"`, `y="0px"`, `width="100%"`, `enable-background="new 0 0 1024 624"`, `xml:space="preserve"`. Keep `xmlns` (required) and `viewBox` (required for scaling). Add `width="200"` and `height="122"` (CLS gate per UI-SPEC § Verification Gates). Add `role="img"` and `aria-label="Timo Bohnstedt"`. Insert child `<title>Timo Bohnstedt</title>` as the first child of `<svg>` (before the first `<path>`).

2. **Path fill rewrite** — Replace every occurrence of `fill="#000000"` with `fill="currentColor"`. There are exactly 11 such occurrences in the source (verified by `grep -c 'fill="#000000"' images/brand-source/logo.svg` → `11`). Do NOT remove `opacity="1.000000"` or `stroke="none"` — leave those attributes in place; only the `fill` attribute changes.

3. **Path data preservation** — Every `d="..."` attribute payload across all 11 `<path>` elements MUST be preserved byte-for-byte. Do NOT reformat whitespace inside `d=`, do NOT normalize coordinates, do NOT minify. The path data is hand-authored vector geometry — any byte change risks visual regression.

4. **Closing tag** — Preserve the `</svg>` closing tag at the end of the file.

Use a precise approach: read the source file, perform a `s/fill="#000000"/fill="currentColor"/g` substitution on the file body, and replace the root `<svg ...>` line block (lines 1-2 in source) with the new single-line root + `<title>` child. Write the result to `themes/minimal/static/images/brand/logo.svg`. Per D-03, this is the canonical SVG location (co-located with theme assets).

Do NOT yet delete `images/brand-source/logo.svg` — Plan 4 handles cleanup of working/source files in coordination with the other deletions. Per D-03, `images/brand-source/` retention is Claude's discretion, but defer the decision to Plan 4 to keep this task atomic.

Per D-04: do NOT alter the `viewBox`. The 1.64:1 aspect ratio (1024:624) is preserved; the explicit `width="200" height="122"` on the root attributes provides the CLS-clean rendering size.
  </action>
  <verify>
    <automated>grep -c 'fill="currentColor"' themes/minimal/static/images/brand/logo.svg | grep -q '^11$' && grep -c 'fill="#000000"' themes/minimal/static/images/brand/logo.svg | grep -q '^0$' && grep -q 'width="200" height="122"' themes/minimal/static/images/brand/logo.svg && grep -q 'role="img"' themes/minimal/static/images/brand/logo.svg && grep -q 'aria-label="Timo Bohnstedt"' themes/minimal/static/images/brand/logo.svg && grep -q '<title>Timo Bohnstedt</title>' themes/minimal/static/images/brand/logo.svg && grep -q 'viewBox="0 0 1024 624"' themes/minimal/static/images/brand/logo.svg</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'fill="currentColor"' themes/minimal/static/images/brand/logo.svg` returns `11`
    - `grep -c 'fill="#000000"' themes/minimal/static/images/brand/logo.svg` returns `0`
    - `grep -c 'width="200" height="122"' themes/minimal/static/images/brand/logo.svg` returns `1`
    - `grep -c 'role="img"' themes/minimal/static/images/brand/logo.svg` returns `1`
    - `grep -c 'aria-label="Timo Bohnstedt"' themes/minimal/static/images/brand/logo.svg` returns `1`
    - `grep -c '<title>Timo Bohnstedt</title>' themes/minimal/static/images/brand/logo.svg` returns `1`
    - `grep -c 'viewBox="0 0 1024 624"' themes/minimal/static/images/brand/logo.svg` returns `1`
    - `grep -c 'xmlns:xlink' themes/minimal/static/images/brand/logo.svg` returns `0`
    - `grep -c 'enable-background' themes/minimal/static/images/brand/logo.svg` returns `0`
    - `grep -c 'xml:space' themes/minimal/static/images/brand/logo.svg` returns `0`
    - `grep -c '^.*<path' themes/minimal/static/images/brand/logo.svg` returns `11` (eleven path elements preserved)
    - File ends with `</svg>` (verifiable via `tail -c 10 themes/minimal/static/images/brand/logo.svg | grep -q '</svg>'`)
    - File parses as valid XML: `python3 -c "import xml.etree.ElementTree as ET; ET.parse('themes/minimal/static/images/brand/logo.svg')"` exits 0
  </acceptance_criteria>
  <done>
themes/minimal/static/images/brand/logo.svg exists, contains exactly 11 `fill="currentColor"` declarations, zero `fill="#000000"` declarations, explicit `width="200" height="122"` and `role="img"` and `aria-label="Timo Bohnstedt"` and child `<title>Timo Bohnstedt</title>` on the root `<svg>` element, viewBox preserved at 0 0 1024 624, all 11 `<path d="...">` payloads preserved byte-for-byte from source, parses as valid XML.
  </done>
</task>

<task type="auto">
  <name>Task 2: Inline the SVG into partials/header.html via readFile + safeHTML; update style.css to delete .wordmark* and add .site-title svg rule</name>
  <read_first>
    - themes/minimal/layouts/partials/header.html (full file — 14 lines; preserve lines 1-3, 6-14 verbatim; replace lines 4-5)
    - themes/minimal/static/css/style.css (lines 124-137 for `.wordmark*` block to delete; lines 74-83 for `.site-title a` precedent; lines 320-327 for mobile media query block; line 326 specifically for the `.wordmark { width: 160px; height: 93px; }` rule to replace)
    - .planning/phases/05.1-swap-wordmark-favicon-to-new-svg-sources/05.1-CONTEXT.md § Wordmark Embedding (D-01) and § Wordmark CSS Cleanup (D-05, D-06, D-07)
    - .planning/phases/05.1-swap-wordmark-favicon-to-new-svg-sources/05.1-UI-SPEC.md § Files Touched table (header.html and style.css rows)
    - .planning/phases/05.1-swap-wordmark-favicon-to-new-svg-sources/05.1-PATTERNS.md § themes/minimal/layouts/partials/header.html (transformation pattern) and § themes/minimal/static/css/style.css (current `.wordmark*` block, mobile override, ADD pattern)
    - themes/minimal/static/images/brand/logo.svg (created in Task 1 — confirms the file the readFile call will resolve)
  </read_first>
  <files>themes/minimal/layouts/partials/header.html, themes/minimal/static/css/style.css</files>
  <action>
**Part A — themes/minimal/layouts/partials/header.html:**

Replace lines 4-5 (the two `<img class="wordmark wordmark-light" ...>` and `<img class="wordmark wordmark-dark" ...>` tags) with this single line, exactly:

```go-html
      {{ readFile "themes/minimal/static/images/brand/logo.svg" | safeHTML }}
```

Use 6-space indentation (matching the indent of the surrounding `<a href="...">` block body). Per D-01, `readFile` is preferred over `resources.Get` because `resources.Get` requires Hugo Pipes static-dir configuration not currently set; `readFile` resolves project-root-relative paths and works out of the box.

After the replacement, the file should be 13 lines instead of 14. Lines 1-3 (`<header class="site-header">`, `<div class="site-title">`, `<a href="{{ .Site.BaseURL }}">`) remain unchanged. Lines 6-14 (the closing `</a>`, `</div>`, the entire `<nav class="site-nav">…</nav>`, the `<button>`, the closing `</header>`) remain unchanged. The result:

```go-html
<header class="site-header">
  <div class="site-title">
    <a href="{{ .Site.BaseURL }}">
      {{ readFile "themes/minimal/static/images/brand/logo.svg" | safeHTML }}
    </a>
  </div>
  <nav class="site-nav">
    {{ range .Site.Menus.main }}
      <a href="{{ .URL }}"{{ if $.IsMenuCurrent "main" . }} class="active"{{ end }}>{{ .Name }}</a>
    {{ end }}
    <button type="button" class="theme-toggle" aria-pressed="false">Dark</button>
  </nav>
</header>
```

**Part B — themes/minimal/static/css/style.css (deletions, lines 124-137):**

Delete the entire `.wordmark*` block (lines 124-137 inclusive, which contains the comment `/* Wordmark — two <img> tags ... */`, the `.wordmark` rule, the two `html[data-theme]` display-toggle rules, the second comment about seam-masking bg, and the `.wordmark-light`/`.wordmark-dark` background rules). The block to delete starts with `/* Wordmark — two <img> tags swapped by [data-theme]` and ends with `.wordmark-dark  { background: #000000; }`. After deletion, the file should jump directly from line 123 (the closing `}` of `.theme-toggle:focus-visible`) to what was previously line 139 (the `/* === Post List (Home) === */` comment) — keep one blank line between them as the existing convention.

**Part C — themes/minimal/static/css/style.css (addition):**

In place of the deleted `.wordmark*` block, insert this new rule (so it stays grouped with header CSS, matching the existing organization):

```css
/* Wordmark — single inlined SVG, currentColor recolors via Phase 4 var(--text) (05.1 D-01, D-02). */
.site-title svg {
  display: block;
  color: var(--text);
  width: 200px;
  height: 122px;
}
```

This rule mirrors `.site-title a { color: var(--text); }` (lines 74-83 precedent) — same surface, same Flexoki token. Per D-04, `width: 200px; height: 122px` matches the SVG's intrinsic 1.64:1 aspect (1024:624 viewBox).

**Part D — themes/minimal/static/css/style.css (mobile override, line 326):**

Inside the existing `@media (max-width: 600px)` block (lines 321-327), replace line 326 (`.wordmark { width: 160px; height: 93px; }`) with exactly:

```css
  .site-title svg { width: 160px; height: 98px; }
```

Use 2-space indentation (matching the existing rules inside the media block). Per D-07, the height changes from 93 to 98 to honor the new SVG's true 1.64:1 aspect at the 160 px mobile width. Do NOT touch lines 322-325 inside the media block (`html { font-size: 16px; }`, `.site-header { flex-direction: column; gap: 0.5rem; }`, `.site-wrapper { padding: 2rem 0 4rem; }`, `.footer-links { gap: 1rem; }`) — those are unrelated to 05.1.

**No new CSS variable, no new color, no new transition rule:** The wordmark inherits the body's `transition: color 150ms ease` (style.css lines 49-56) for free via cascade — UI-SPEC § Interaction Contract row 4 ("Reduced-motion respect") is satisfied without adding any new transition declaration.
  </action>
  <verify>
    <automated>grep -c 'readFile "themes/minimal/static/images/brand/logo.svg" | safeHTML' themes/minimal/layouts/partials/header.html | grep -q '^1$' && grep -c '<img.*wordmark' themes/minimal/layouts/partials/header.html | grep -q '^0$' && grep -c '\.wordmark-light\|\.wordmark-dark\|\.wordmark[^-]' themes/minimal/static/css/style.css | grep -q '^0$' && grep -c '\.site-title svg' themes/minimal/static/css/style.css | grep -q '^2$' && grep -q 'color: var(--text);' themes/minimal/static/css/style.css && grep -q '\.site-title svg { width: 160px; height: 98px; }' themes/minimal/static/css/style.css</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'readFile "themes/minimal/static/images/brand/logo.svg" | safeHTML' themes/minimal/layouts/partials/header.html` returns `1`
    - `grep -c '<img' themes/minimal/layouts/partials/header.html` returns `0` (no remaining img tags)
    - `grep -c 'wordmark-light\|wordmark-dark\|class="wordmark"' themes/minimal/layouts/partials/header.html` returns `0`
    - `grep -c '\.wordmark[^-a-zA-Z]\|\.wordmark-light\|\.wordmark-dark' themes/minimal/static/css/style.css` returns `0` (the entire `.wordmark*` family is deleted)
    - `grep -c 'html\[data-theme="light"\] \.wordmark\|html\[data-theme="dark"\] \.wordmark' themes/minimal/static/css/style.css` returns `0`
    - `grep -c '\.site-title svg' themes/minimal/static/css/style.css` returns `2` (one desktop rule, one mobile override)
    - `grep -c '\.site-title svg { width: 160px; height: 98px; }' themes/minimal/static/css/style.css` returns `1`
    - `grep -A 5 '\.site-title svg {' themes/minimal/static/css/style.css | grep -q 'color: var(--text);'`
    - `grep -A 5 '\.site-title svg {' themes/minimal/static/css/style.css | grep -q 'width: 200px;'`
    - `grep -A 5 '\.site-title svg {' themes/minimal/static/css/style.css | grep -q 'height: 122px;'`
    - `wc -l themes/minimal/layouts/partials/header.html` reports between 12 and 14 lines (was 14; new file is 13 — small range tolerates trailing newline variance)
    - File header.html still contains `<header class="site-header">` and `</header>` (preserved scaffolding)
    - File header.html still contains `<a href="{{ .Site.BaseURL }}">` (anchor preserved per UI-SPEC § Interaction Contract)
    - File style.css still contains the existing `.site-title a` rules at the original location (`grep -c '\.site-title a {' themes/minimal/static/css/style.css` returns at least 1)
  </acceptance_criteria>
  <done>
header.html replaces the two-image swap with a single `{{ readFile ... | safeHTML }}` call, preserves the surrounding `<a>` anchor, `<header>` wrapper, and entire `<nav>` block. style.css deletes the entire `.wordmark*` family (rule + display-toggles + seam-mask backgrounds), adds a `.site-title svg` rule with `color: var(--text)` and explicit 200×122 dimensions, and replaces the mobile `.wordmark` override with `.site-title svg { width: 160px; height: 98px; }`. No new transition rules added (inherited from body). No off-token colors introduced.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Build-time → page render | Hugo `readFile` injects `themes/minimal/static/images/brand/logo.svg` into HTML at build time via `safeHTML` filter. Untrusted content here would inject directly into rendered HTML. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05.1-01 | Tampering | `themes/minimal/static/images/brand/logo.svg` body content injected via `safeHTML` | mitigate | Source is repo-committed, hand-authored by Timo (see UI-SPEC § Registry Safety — "Brand asset provenance"). Task 1 produces the file from `images/brand-source/logo.svg` with a documented byte-for-byte path-data preservation rule; reviewer must confirm zero `<script>`, `<foreignObject>`, or `on*=` event-handler attributes in the produced SVG before commit. Add a sanity grep to the verify step: `grep -ciE '<script|<foreignObject|onload=|onclick=|onerror=' themes/minimal/static/images/brand/logo.svg` MUST return `0`. |
| T-05.1-02 | Information Disclosure | SVG path data | accept | Path data is the wordmark vector geometry; it is intentionally public (rendered as the site header). No PII, no secrets. |
| T-05.1-03 | Denial of Service | Hugo `readFile` failure if SVG is missing or malformed | accept | Hugo errors loudly at build time if `readFile` cannot resolve the path or `safeHTML` receives invalid content. CI fails, deployment doesn't ship. Single contributor, low operational risk. |
</threat_model>

<verification>
After both tasks complete, verify the integrated wordmark behavior:

1. **SVG file integrity:**
   - `grep -c 'fill="currentColor"' themes/minimal/static/images/brand/logo.svg` returns `11`
   - `python3 -c "import xml.etree.ElementTree as ET; ET.parse('themes/minimal/static/images/brand/logo.svg')"` exits 0
   - `grep -ciE '<script|<foreignObject|onload=|onclick=|onerror=' themes/minimal/static/images/brand/logo.svg` returns `0` (T-05.1-01 mitigation gate)

2. **Hugo template rendering (smoke test, optional — Hugo is not installed locally per CLAUDE.md):**
   - If Hugo is available locally: `hugo --minify --quiet 2>&1 | grep -i error` returns nothing
   - If Hugo is not available: skip — the github-actions deploy will surface any template errors

3. **CSS integrity:**
   - `grep -c '\.wordmark' themes/minimal/static/css/style.css` returns `0`
   - `grep -c '\.site-title svg' themes/minimal/static/css/style.css` returns `2`

4. **header.html integrity:**
   - `grep -c '<img' themes/minimal/layouts/partials/header.html` returns `0`
   - `grep -c 'readFile' themes/minimal/layouts/partials/header.html` returns `1`
   - File still contains the wrapping `<a href="{{ .Site.BaseURL }}">` anchor (interaction contract preserved per UI-SPEC § Interaction Contract)

5. **CLS guarantee surface:** The inline `<svg>` element carries `width="200"` and `height="122"`, and the CSS rule `.site-title svg { width: 200px; height: 122px; }` reinforces this — both layers ensure the browser allocates the correct space before paint.
</verification>

<success_criteria>
This plan is complete when:
- HEAD-01 (wordmark in header) is satisfied via inline SVG.
- HEAD-02 (theme-aware swap, no FOUC, no JS) is satisfied via `currentColor` + `var(--text)` cascade — the SVG recolors atomically with the body color when `[data-theme]` flips, no DOM mutation needed.
- HEAD-03 (a11y attributes, explicit dimensions) is satisfied via `role="img"` + `aria-label="Timo Bohnstedt"` + child `<title>` + explicit `width="200" height="122"` on the inlined `<svg>`.
- All `.wordmark*` CSS rules are deleted (lines 124-137 of the prior style.css).
- The mobile `.wordmark { width: 160px; height: 93px; }` rule is replaced with `.site-title svg { width: 160px; height: 98px; }` inside the existing `@media (max-width: 600px)` block.
- No new transition rules added; the inherited body transition handles smooth recolor.
- The wrapping `<a href="{{ .Site.BaseURL }}">` anchor preserved (interaction contract).
- The 8 sliced PNGs at `themes/minimal/static/images/brand/` are NOT touched by this plan — Plan 4 deletes them after the wiring is verified end-to-end.
</success_criteria>

<output>
After completion, create `.planning/phases/05.1-swap-wordmark-favicon-to-new-svg-sources/05.1-01-SUMMARY.md` documenting:
- The exact lines deleted from style.css and the exact lines added (with the new rule body)
- The exact replacement of header.html lines 4-5
- A confirmation grep snippet showing the 11 `currentColor` fills in the new logo.svg
- Any notes about Hugo path resolution if there were any surprises with `readFile`
- Cross-reference: this plan's edits and Plan 2's favicon SVG edit are independent — they share no files
</output>
