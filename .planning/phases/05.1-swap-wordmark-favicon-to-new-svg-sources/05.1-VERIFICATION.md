---
phase: 05.1-swap-wordmark-favicon-to-new-svg-sources
verified: 2026-04-29T19:45:00Z
status: human_needed
score: 13/13 must-haves verified
overrides_applied: 0
gaps_resolved:
  - truth: "images/logos.png no longer exists in the repo"
    status: resolved
    resolution: "Orchestrator removed the untracked images/logos.png (398,977 bytes) inline after the verifier flagged it. Plan 4 SUMMARY's claim of pre-existing absence was incorrect; the file was untracked in git but present in the working tree. Verified absent post-fix; zero references in themes/, scripts/, content/, hugo.toml."
human_verification:
  - test: "Theme toggle wordmark recolor smoothness"
    expected: "Click the theme toggle in the deployed site header. The script wordmark recolors smoothly between Flexoki ink (#100F0F) and Flexoki cream (#FFFCF0) with no flicker, no FOUC, no layout shift. The transition follows body's color 150ms ease (inherited via cascade)."
    why_human: "Visual recolor smoothness, FOUC presence, and transition behavior require browser rendering against a live deploy — cannot be inferred from CSS rules alone. The mechanism (currentColor + var(--text)) is structurally proven; perceptual quality requires a screen."
  - test: "OS dark-mode favicon swap"
    expected: "Open tbohnstedt.cloud in a browser with OS in light mode — tab favicon shows TB monogram in Flexoki ink (#100F0F). Switch OS theme to dark and hard-refresh the tab — favicon swaps to Flexoki cream (#FFFCF0). Verify in Chrome, Firefox, and Safari."
    why_human: "Browser tab chrome runs SVG <style> @media (prefers-color-scheme: dark) in an isolated CSSOM whose responsiveness is driver/browser-specific. Cannot be tested without a live deploy + OS theme toggle."
  - test: "iOS home-screen apple-touch-icon render"
    expected: "On iOS Safari, navigate to tbohnstedt.cloud and tap Share → Add to Home Screen. The home-screen icon renders the 180×180 TB monogram (Flexoki ink on transparent background, with iOS-applied white frame), not a generic web preview or screenshot."
    why_human: "iOS home-screen icon rendering uses apple-touch-icon.png at the native /apple-touch-icon.png URL; only a real iOS device can confirm the icon is selected and rendered correctly."
  - test: "Lighthouse Mobile CLS < 0.1 on /"
    expected: "Run Lighthouse Mobile against the deployed root URL. Cumulative Layout Shift score < 0.1. Structurally guaranteed by explicit width=\"200\" height=\"122\" on the inline SVG and matching CSS dimensions, but must be measured."
    why_human: "Lighthouse requires a live URL and Google's tooling — cannot be run on local files."
  - test: "Header layout regression at desktop and 600px viewport"
    expected: "Compare the deployed header at >=601px (desktop) and <=600px (mobile) against the Phase 5 baseline. Site-nav baseline alignment, theme-toggle position, and mobile column stack are unchanged. Wordmark renders at 200×122 desktop / 160×98 mobile with no overflow."
    why_human: "Layout regression requires DevTools responsive mode + visual diff against pre-05.1 baseline."
  - test: "Hugo build smoke test"
    expected: "On next push to main, GitHub Actions runs hugo --minify --baseURL \"$base_url/\" with Hugo Extended 0.157.0. Build emits no errors related to readFile, missing assets, or favicon link references. Generated public/index.html contains the inlined logo.svg markup (11 currentColor paths)."
    why_human: "Hugo is not installed locally per CLAUDE.md. The readFile + safeHTML idiom and asset deletions are validated by CI, not by this verification pass."
---

# Phase 05.1: Swap Wordmark + Favicon to New SVG Sources — Verification Report

**Phase Goal:** Retire the PNG-driven wordmark + favicon implementation from Phase 5 and migrate to native SVG sources at `images/brand-source/{logo.svg, fav-icon.svg}`. Wordmark inlined via Hugo readFile + safeHTML with currentColor recolor. Favicon rewritten as native path-based SVG with embedded prefers-color-scheme @media. Build script rewritten to rasterize via cairosvg into 16/32/48 favicon.ico + 180 apple-touch-icon.png.
**Verified:** 2026-04-29T19:45:00Z
**Status:** gaps_found
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths (Plans 1-4 consolidated)

| #  | Truth | Status | Evidence |
|----|-------|--------|----------|
| 1  | (Plan 1) Site header renders the script wordmark instead of plain text site-title on every page via inline SVG, no `<img>` tags | VERIFIED | `header.html`: 1 `readFile` + `safeHTML`, 0 `<img>` tags; wrapping `<a href="{{ .Site.BaseURL }}">` preserved |
| 2  | (Plan 1) Wordmark recolors automatically when user toggles theme — no JS swap, no FOUC | VERIFIED (mechanism) | `logo.svg` carries 11 `fill="currentColor"`; `style.css` `.site-title svg { color: var(--text); }`; body inherits `transition: color 150ms ease`; visual smoothness deferred to UAT |
| 3  | (Plan 1) Wordmark carries an accessible label equivalent to former alt text | VERIFIED | `logo.svg` contains `role="img"`, `aria-label="Timo Bohnstedt"`, child `<title>Timo Bohnstedt</title>` |
| 4  | (Plan 1) Wordmark dimensions are explicit on the SVG element (CLS guarantee) | VERIFIED | `logo.svg` root has `width="200" height="122"` and `viewBox="0 0 1024 624"`; CSS reinforces with `.site-title svg { width: 200px; height: 122px; }` |
| 5  | (Plan 1) Mobile viewport renders the wordmark at 160×98 px | VERIFIED | `style.css` `@media (max-width: 600px)`: `.site-title svg { width: 160px; height: 98px; }` |
| 6  | (Plan 2) Browser tab favicon renders TB monogram in Flexoki ink (light) / cream (dark) via embedded `@media` | VERIFIED (mechanism) | `favicon.svg` contains `path { fill: #100F0F; }` (light) and `@media (prefers-color-scheme: dark) { path { fill: #FFFCF0; } }` — visual swap deferred to UAT |
| 7  | (Plan 2) Favicon SVG is < 6 KB (was ~77 KB PNG-wrapped) | VERIFIED | `wc -c` reports 4823 bytes; ~16× shrink from Phase 5's 77001 bytes |
| 8  | (Plan 2) Favicon SVG contains zero `<image>` tags and at least 3 native `<path>` elements | VERIFIED | 0 `<image>` tags, 0 `data:image/png` payloads, 0 `base64`; 3 `<path>` elements present |
| 9  | (Plan 3) Build script rasterizes fav-icon.svg into multi-frame favicon.ico (16/32/48) and 180×180 apple-touch-icon.png | VERIFIED | `file favicon.ico` reports "MS Windows icon resource - 3 icons"; PIL `img.ico.sizes()` returns `[(16, 16), (32, 32), (48, 48)]`; PIL reports apple-touch-icon as `(180, 180)` |
| 10 | (Plan 3) Build script contains zero slicing logic | VERIFIED | 0 occurrences of `crop`, `slice`, `flatten_alpha`, `trim_to_content`, `pad_to_aspect`, `resize_max`, `build_favicon_svg`, `variant_bg`, `rgb_hex`, `import base64`, `ImageChops` |
| 11 | (Plan 3) Build script preserves Phase 3 Python conventions (PEP 8, snake_case, pathlib, no type hints, single-line docstring, `__main__` guard, shebang) | VERIFIED | Line 1 `#!/usr/bin/env python3`; line 2 single-line docstring; 0 type hints; 5 `def` (run_pngquant, fmt_kb, build_favicon_ico, build_apple_touch, main); `if __name__ == "__main__":` present; 69 lines (was 205) |
| 12 | (Plan 4) `themes/minimal/static/images/brand/` contains zero `.png` files (only `logo.svg`) | VERIFIED | `ls *.png` returns 0; directory contains only `logo.svg` (24,992 bytes) |
| 13 | (Plan 4) `images/logos.png` no longer exists in the repo | **FAILED** | File **still present** in working tree at `images/logos.png` (398,977 bytes, untracked). Plan 4 SUMMARY's claim of "already absent at base commit" contradicts current `ls -la images/logos.png` output. |
| 14 | (Plan 4) No remaining grep references to deleted PNG filenames in `themes/`, `scripts/`, `content/`, `hugo.toml` | VERIFIED | All 5 grep patterns (logo-, icon-, minimum-, favicon-, images/logos.png) return 0 matches across scoped directories |

**Score:** 13/14 truths verified (one of these maps to Plan 4 truth list of 4; consolidated total = 14 across 4 plans, but Plan 4 frontmatter lists 4 truths so consolidated count = 13 plus 1 failed = 14). Reconciled to **12/13 must-haves verified** by deduplicating the "no orphaned references" truth (already covered structurally) — see Gap 1 below.

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `themes/minimal/static/images/brand/logo.svg` | NEW; 11 `currentColor` fills, explicit dims, a11y | VERIFIED | 24,992 bytes; 11 `fill="currentColor"`, 0 `fill="#000000"`; `role="img"`, `aria-label="Timo Bohnstedt"`, `<title>` child; `width="200" height="122"`; `viewBox="0 0 1024 624"`; XML parses; 0 script-injection vectors |
| `themes/minimal/layouts/partials/header.html` | MODIFIED; single `readFile|safeHTML` call, no `<img>` tags | VERIFIED | 13 lines; 1 `readFile "themes/minimal/static/images/brand/logo.svg" | safeHTML`; 0 `<img>` tags; wrapping `<a href="{{ .Site.BaseURL }}">` preserved; nav block preserved |
| `themes/minimal/static/css/style.css` | MODIFIED; `.wordmark*` deleted, `.site-title svg` added | VERIFIED | 0 `.wordmark` matches; 2 `.site-title svg` rules (desktop + mobile); rule body confirms `display: block`, `color: var(--text)`, `width: 200px`, `height: 122px`; mobile `.site-title svg { width: 160px; height: 98px; }` present |
| `images/brand-source/fav-icon.svg` | MODIFIED; `<style>` block, native paths | VERIFIED | 4823 bytes; embedded `<style>` with `path { fill: #100F0F; }` + `@media (prefers-color-scheme: dark) { path { fill: #FFFCF0; } }`; 3 `<path>` elements; XML parses |
| `themes/minimal/static/favicon.svg` | REPLACED; byte-identical to brand-source | VERIFIED | 4823 bytes; `diff -q images/brand-source/fav-icon.svg themes/minimal/static/favicon.svg` exits 0 (identical); 0 `<image>` tags; 3 `<path>` elements; 0 base64; 0 script-injection vectors |
| `themes/minimal/static/favicon.ico` | REGENERATED; multi-frame 16/32/48 | VERIFIED | 5,416 bytes; `file` reports "MS Windows icon resource - 3 icons"; PIL confirms `[(16, 16), (32, 32), (48, 48)]` |
| `themes/minimal/static/apple-touch-icon.png` | REGENERATED; 180×180 | VERIFIED | 3,136 bytes; PIL reports `(180, 180)` mode `P` (paletted, pngquant-optimized); 38% smaller than Phase 5 baseline (5026 bytes) |
| `scripts/build_brand_assets.py` | REWRITTEN; cairosvg + Pillow ICO writer | VERIFIED | 69 lines (was 205, 66% reduction); valid Python; `from cairosvg import svg2png`; `from PIL import Image` (no ImageChops); 5 functions; preserves all Phase 3 conventions |
| `themes/minimal/static/images/brand/` directory | Contains only `logo.svg` | VERIFIED | Single file: `logo.svg`; 0 PNG files |
| `images/brand-source/` directory | Retained with both source SVGs | VERIFIED | `logo.svg` (25,000 bytes) and `fav-icon.svg` (4823 bytes) both present |
| `images/logos.png` | DELETED | **FAILED** | File present at 398,977 bytes (untracked in git, but present in working tree) |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `partials/header.html` | `themes/minimal/static/images/brand/logo.svg` | `readFile` + `safeHTML` filter chain | WIRED | grep matches `readFile "themes/minimal/static/images/brand/logo.svg" | safeHTML` (1 match); resolves via Hugo project-root-relative path |
| `style.css .site-title svg` | `:root[data-theme]` `--text` token | `color: var(--text)` cascade through inline SVG `currentColor` | WIRED | Rule body confirms `color: var(--text)`; logo.svg paths use `fill="currentColor"`; transitive Phase 4 token hookup intact |
| `images/brand-source/fav-icon.svg` | `themes/minimal/static/favicon.svg` | byte-identical copy | WIRED | `diff -q` exits 0 (4823 bytes each, identical) |
| `themes/minimal/static/favicon.svg` `<style>` | browser tab chrome render | `@media (prefers-color-scheme: dark)` selector inside SVG CSSOM | WIRED (mechanism) | `<style>` block contains both light (`#100F0F`) and dark (`#FFFCF0`) rules; visual rendering deferred to UAT |
| `scripts/build_brand_assets.py` | `images/brand-source/fav-icon.svg` | `cairosvg.svg2png(url=str(svg_path), output_width=N, output_height=N)` | WIRED | 2 calls to `svg2png(url=str(svg_path), ...)` — one in `build_favicon_ico` (48px), one in `build_apple_touch` (180px); `FAV_ICON_SVG = SOURCE_DIR / "fav-icon.svg"` constant resolves to brand-source path |
| `build_apple_touch` output | pngquant subprocess | `run_pngquant(out_path)` | WIRED | `run_pngquant(out_path)` invoked inside `build_apple_touch()` after `out_path.write_bytes(png_bytes)`; `PNGQUANT_CMD` argv pattern preserved |
| `baseof.html` `<head>` | `partials/favicon.html` | `{{ partial "favicon.html" . }}` | WIRED (preserved from Phase 5) | 1 match in baseof.html; partial body unchanged from Phase 5 D-13 (3 link tags: SVG, ICO, apple-touch) |

### Data-Flow Trace (Level 4)

Not applicable in the dynamic-data sense — this phase produces static templates, CSS rules, and pre-built binary/SVG assets. There is no runtime fetch, store, or API call. However, build-time data flow is verifiable:

| Artifact | Build-Time Source | Source Produces Real Output | Status |
|----------|------------------|----------------------------|--------|
| Inlined wordmark in rendered HTML | Hugo `readFile` reads `themes/minimal/static/images/brand/logo.svg` at build time | logo.svg exists (24,992 bytes), parses as XML, contains 11 paths with `currentColor` | FLOWING (build-time) |
| favicon.ico raster frames | cairosvg renders fav-icon.svg → PIL writes ICO multi-frame | fav-icon.svg exists post-Plan-2; cairosvg honored the `<style>` block; ICO output has 3 frames at 16/32/48 | FLOWING |
| apple-touch-icon.png raster | cairosvg renders fav-icon.svg → write bytes → pngquant in-place | fav-icon.svg exists; output is exactly 180×180; 38% smaller than Phase 5 baseline (pngquant palette optimization) | FLOWING |
| Browser tab favicon dark-mode swap | SVG `<style>` `@media (prefers-color-scheme: dark)` evaluated by browser CSSOM at runtime | Both rules present in deployed favicon.svg; swap deferred to UAT for visual confirmation | FLOWING (mechanism) — visual deferred |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| `logo.svg` parses as valid XML | `python3 -c "import xml.etree.ElementTree as ET; ET.parse('themes/minimal/static/images/brand/logo.svg')"` | exit 0 | PASS |
| `favicon.svg` parses as valid XML | `python3 -c "import xml.etree.ElementTree as ET; ET.parse('themes/minimal/static/favicon.svg')"` | exit 0 | PASS |
| `build_brand_assets.py` parses as valid Python | `python3 -c "import ast; ast.parse(open('scripts/build_brand_assets.py').read())"` | exit 0 | PASS |
| `favicon.ico` is a valid multi-frame ICO | `file themes/minimal/static/favicon.ico` | "MS Windows icon resource - 3 icons, 16x16, 32x32" (48x48 also present per PIL) | PASS |
| `apple-touch-icon.png` is exactly 180×180 | `python3 -c "from PIL import Image; print(Image.open('themes/minimal/static/apple-touch-icon.png').size)"` | `(180, 180)` | PASS |
| Source/deploy SVG parity | `diff -q images/brand-source/fav-icon.svg themes/minimal/static/favicon.svg` | (no output, exit 0) | PASS |
| No script-injection vectors in inlined SVG | `grep -ciE '<script|<foreignObject|onload=|onclick=|onerror=' themes/minimal/static/images/brand/logo.svg` | 0 | PASS |
| No script-injection vectors in favicon SVG | `grep -ciE '<script|<foreignObject|onload=|onclick=|onerror=' themes/minimal/static/favicon.svg` | 0 | PASS |
| No PNGs remaining in brand directory | `ls themes/minimal/static/images/brand/*.png 2>/dev/null \| wc -l` | 0 | PASS |
| `images/logos.png` is absent | `[ ! -f images/logos.png ]` | exit 1 (file EXISTS) | **FAIL** |
| Hugo build (full template render) | `hugo --minify --quiet` | n/a — Hugo not installed locally per CLAUDE.md | SKIP (CI gate) |

### Requirements Coverage

| Requirement | Source Plan(s) | Description | Status | Evidence |
|-------------|---------------|-------------|--------|----------|
| HEAD-01 | 05.1-01, 05.1-04 | Site header shows script wordmark image instead of plain text site title, on every page | SATISFIED | `header.html` includes inlined `logo.svg` via `readFile + safeHTML` inside the existing `<a href="{{ .Site.BaseURL }}">` anchor; partial is included by `baseof.html` for all routes |
| HEAD-02 | 05.1-01 | Wordmark swaps between dark/light variants automatically with active theme, no FOUC, no JS | SATISFIED (mechanism) | `currentColor` cascade through `var(--text)`; the requirement's original "two-image CSS toggle" wording is superseded by the inline-SVG-with-currentColor approach (a strictly better realization of the intent — single asset, no display-toggle, theme-token-driven). Visual smoothness deferred to UAT |
| HEAD-03 | 05.1-01 | Wordmark carries explicit `width`/`height` and meaningful alt text | SATISFIED | logo.svg root: `width="200" height="122"`; a11y label preserved via `role="img"`, `aria-label="Timo Bohnstedt"`, child `<title>` (a11y-equivalent of img alt for inline SVG) |
| HEAD-04 | 05.1-02, 05.1-03, 05.1-04 | favicon.ico (multi-size) + favicon.svg (with embedded prefers-color-scheme dark swap) + apple-touch-icon.png (180×180) | SATISFIED (template + asset level) | All 3 files exist at `themes/minimal/static/`; favicon.svg has embedded `@media (prefers-color-scheme: dark)`; favicon.ico is multi-frame 16/32/48; apple-touch is 180×180. Visual rendering across browsers/iOS deferred to UAT |
| HEAD-05 | (Phase 5 preserved) | Favicon `<link>` tags consolidated in `partials/favicon.html` and included from `baseof.html` `<head>` | SATISFIED (Phase 5 preserved) | `partials/favicon.html` unchanged (3 link tags); `baseof.html` retains `{{ partial "favicon.html" . }}` (1 match) |

All 5 phase requirements (HEAD-01 through HEAD-05) have implementation evidence in the codebase. No requirement IDs orphaned: every plan's `requirements:` field is accounted for, and every requirement maps to verified evidence.

**Cross-reference of plan-level `requirements:` declarations:**
- Plan 1: HEAD-01, HEAD-02, HEAD-03 — all VERIFIED
- Plan 2: HEAD-04 — VERIFIED
- Plan 3: HEAD-04, HEAD-05 — both VERIFIED
- Plan 4: HEAD-01, HEAD-04 — both VERIFIED (cross-cut cleanup)

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| (none) | — | — | — | — |

No TODO/FIXME/PLACEHOLDER comments found in any of the 6 modified/created files (`logo.svg`, `header.html`, `style.css`, `favicon.svg`, `fav-icon.svg`, `build_brand_assets.py`). No empty implementations, no stubs, no console-log-only handlers, no hardcoded empty arrays acting as stubs. All assets have substantive content. The build script exits with explicit error messages on missing pngquant or missing source SVG. Threat-model gates (script-injection greps) all clean.

### Human Verification Required

These items are inherent to the phase scope (visual rendering, OS-level theme APIs, browser CSSOM, external tooling) and cannot be verified programmatically. They are post-deploy UAT items, not code gaps:

#### 1. Theme toggle wordmark recolor smoothness

**Test:** Click the theme toggle in the deployed site header.
**Expected:** Wordmark recolors smoothly between Flexoki ink (`#100F0F`) and Flexoki cream (`#FFFCF0`) following body's `color 150ms ease`. No flicker, no FOUC, no layout shift.
**Why human:** Visual smoothness, FOUC presence, and transition behavior require browser rendering against a live deploy.

#### 2. OS dark-mode favicon swap

**Test:** Open `tbohnstedt.cloud` in OS-light mode → tab favicon shows ink. Switch OS to dark + hard-refresh → favicon swaps to cream. Verify in Chrome, Firefox, Safari.
**Expected:** Browser tab chrome reflects the SVG `<style>` `@media (prefers-color-scheme: dark)` rule.
**Why human:** Browser tab chrome SVG rendering uses an isolated CSSOM whose responsiveness varies by browser engine; cannot test without live deploy + OS theme switch.

#### 3. iOS home-screen apple-touch-icon render

**Test:** On iOS Safari, navigate to `tbohnstedt.cloud` → Share → Add to Home Screen.
**Expected:** Home-screen icon shows 180×180 TB monogram (Flexoki ink on transparent bg, with iOS-applied white frame). Not a generic web preview or screenshot.
**Why human:** iOS home-screen icon selection and rendering can only be confirmed on a real iOS device.

#### 4. Lighthouse Mobile CLS < 0.1

**Test:** Run Lighthouse Mobile audit against deployed `/`.
**Expected:** CLS < 0.1 (structurally guaranteed by explicit `width="200" height="122"` on the inline SVG and matching CSS dimensions).
**Why human:** Lighthouse requires a live URL and Google's tooling.

#### 5. Header layout regression at desktop and 600px viewport

**Test:** Compare deployed header at >=601px (desktop) and <=600px (mobile) against Phase 5 baseline.
**Expected:** `.site-nav` baseline alignment, theme-toggle position, mobile column stack unchanged. Wordmark renders at 200×122 (desktop) / 160×98 (mobile) with no overflow.
**Why human:** Layout regression requires DevTools responsive mode + visual diff against baseline.

#### 6. Hugo build smoke test (CI)

**Test:** Next push to `main` triggers GitHub Actions: `hugo --minify --baseURL "$base_url/"` with Hugo Extended 0.157.0.
**Expected:** Build emits no errors related to `readFile`, missing assets, or favicon link references. Generated `public/index.html` contains the inlined `logo.svg` markup.
**Why human:** Hugo not installed locally per CLAUDE.md; CI is the validation surface.

### Gaps Summary

**One automated gap blocks phase closure:**

1. **`images/logos.png` still exists in the working tree (398,977 bytes, untracked).** Plan 4 Task 1 Step 3 specified `rm images/logos.png`, and Plan 4 SUMMARY claimed the file was "already absent at this base commit" — this is contradicted by the current working-tree state. The file was never tracked by git (verified via `git log --all -- images/logos.png` returning empty), so the rm either never executed or was undone. Plan 4's "must-have truth" line `images/logos.png no longer exists in the repo` and UI-SPEC § Verification Gates `images/logos.png does not exist` both fail.

   **Resolution:** Run `rm images/logos.png` from project root (no `git rm` needed since the file is untracked). After deletion, `[ ! -f images/logos.png ]` should exit 0 and `git status` should no longer list it as untracked.

   **Severity:** Low operational impact — the file is never referenced by any template, CSS, script, or content (verified by full repo grep returning 0 matches in scoped directories), so the deploy is unaffected. However, the truth condition fails and the ~399 KB working-tree weight remains.

**Visual gates (6 items) are deferred to post-deploy UAT** — these are inherent to the phase scope, not code gaps. The phase architecture (currentColor + var(--text), embedded `<style>` + @media, multi-frame raster) is structurally proven; the UAT pass confirms the perceptual/runtime behavior that automation cannot reach.

---

_Verified: 2026-04-29T19:45:00Z_
_Verifier: Claude (gsd-verifier)_
